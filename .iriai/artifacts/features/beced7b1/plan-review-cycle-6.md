# Plan Review Report

**304 concerns, 150 gaps** across 7 subfeatures and 10 edges.

**Failed SF reviews:** declarative-schema (completeness), declarative-schema (security), dag-loader-runner (completeness), dag-loader-runner (security), testing-framework (completeness), workflow-migration (completeness), workflow-migration (security), composer-app-foundation (completeness), composer-app-foundation (security), workflow-editor (completeness), workflow-editor (security), libraries-registries (completeness), libraries-registries (security)

**Failed edge reviews:** SF-1 → SF-2, SF-1 → SF-3, SF-2 → SF-3, SF-1 → SF-4, SF-2 → SF-4, SF-3 → SF-4, SF-1 → SF-6, SF-5 → SF-6, SF-5 → SF-7, SF-7 → SF-6


## Per-Subfeature Findings


### composer-app-foundation [FAIL]

**completeness** [FAIL]: SF-5 artifacts have significant internal inconsistencies and cross-artifact gaps. The plan is missing an entire step for the tools hub (REQ-17/AC-3), has zero journey verification blocks, is missing key design components (EditorSchemaBootstrapGate, YAMLContractErrorPanel, GridCard), and has leaked step references from a broader plan. The design journey annotations are off-by-one from J-3 onward, mapping content to the wrong PRD journey IDs. The mutation hook interface is inconsistently specified across PRD (4 entity types, created/updated/soft_deleted/restored events), system design (workflows only, created/imported/version_saved/deleted events), and the SF-7 consumer (D-SF7-2 uses YAML parsing on delete, not a materialized ref table from hooks). The starter template approach contradicts between plan (DB rows with user_id='__system__') and system design (filesystem assets). React Query is required by REQ-15 but missing from plan dependencies. Multiple missing files prevent a bootable SPA or functioning Alembic chain.

- [critical] Tools hub (platform/toolshub/frontend/) has NO plan step. REQ-17 requires a static authenticated SPA with hardcoded tool cards and dev_tier gating. The file appears in file_manifest with action 'create' but no step covers building it. AC-3 (tools hub → compose navigation) is unverifiable.
- [critical] Plan journey_verifications is completely empty ([]). None of the 7 PRD journeys (J-1 through J-7) have verification blocks, making journey-level acceptance untestable from the plan.
- [critical] Design journey annotations J-3 through J-6 are mapped to wrong PRD journey IDs. Design J-3 annotates editor bootstrap behavior (should be J-2 expansion or separate), J-4 annotates save/export (PRD J-3), J-5 annotates schema unavailability (PRD J-4), J-6 annotates import (PRD J-5). PRD J-6 (cross-user access) and J-7 (tools hub missing session) have NO design annotations at all.
- [critical] Mutation hook event types are inconsistent across artifacts. PRD REQ-18 specifies events: created, updated, soft_deleted, restored. System design D-SF5-7 specifies: created, imported, version_saved, deleted. 'updated' and 'restored' are missing from system design; 'imported' and 'version_saved' are missing from PRD. The names 'soft_deleted' vs 'deleted' also conflict.
- [critical] PRD REQ-18 requires mutation hooks on ALL FOUR entity types (Workflow, Role, OutputSchema, CustomTaskTemplate). System design only defines WorkflowMutationHook for workflows — no hook interface for roles, schemas, or templates. D-SF5-7 only mentions 'WorkflowMutationHook(workflow_id, operation)'. AC-13 requires hooks on all four types.
- [critical] SF-5's entire mutation hook infrastructure (REQ-18, AC-13, D-SF5-R3) may be unnecessary. SF-7's design decision D-SF7-2 says 'YAML parsing on delete (no materialized ref table)' — meaning SF-7 checks references by parsing workflow YAML at delete time, NOT by maintaining workflow_entity_refs via mutation hooks. The hook subscriber SF-5 is building infrastructure for does not plan to use it.
- [critical] Design components CMP-18 (EditorSchemaBootstrapGate) and CMP-19 (YAMLContractErrorPanel) have no files in the plan's scope or file_manifest. These are core schema-gate and error-messaging components described as 'new' in the design with detailed states and accessibility notes, but no plan step creates them.
- [high] SF-4 design summary expects 'is_example flag distinguishes system vs user content' for pre-seeded data, but SF-5 plan STEP-40 counterexample explicitly says 'Do NOT add is_example column to base tables — system starters use user_id=__system__'. Direct cross-subfeature interface mismatch.
- [high] React Query (@tanstack/react-query) is required by PRD REQ-15 ('React 18 + TypeScript + Vite SPA using React Router, React Query') and listed in system design SVC-SF5-3, but is completely absent from the plan's dependencies list and STEP-41 instructions.
- [high] Starter template approach contradicts between plan and system design. Plan STEP-40 says 'System-seeded starters use user_id=__system__; returned by ?starter=true query parameter' (DB rows). System design SVC-SF5-8 and CP-SF5-4 say starters are 'Bundled JSON/YAML assets checked into tools/compose/backend/app/data/' read from filesystem. These are mutually exclusive approaches.
- [high] Plan architectural risks reference non-existent steps: RISK-81 cites STEP-36/STEP-39, RISK-82 cites STEP-42, RISK-83 cites STEP-37, RISK-85 cites STEP-43, RISK-86 cites STEP-45. The plan only contains STEP-40 and STEP-41. These are contamination from a broader multi-subfeature plan.
- [high] Import endpoint path inconsistency: Plan STEP-40 AC says 'POST /api/workflows/{id}/import' (with workflow ID), but system design API endpoint says 'POST /api/workflows/import' (no ID, creates new workflow). These have different semantics — one imports into existing workflow, the other creates a new one.
- [high] Import error status code inconsistency: System design CP-SF5-5 says 'Malformed YAML returns 400', but plan STEP-40 AC says 'POST /api/workflows/{id}/import with unparseable YAML returns 422'. 400 vs 422 for the same error condition.
- [high] Plan STEP-40 scope has only app/schemas/workflow.py but no Pydantic schema files for the other three entity types (roles, output_schemas, custom_task_templates). CRUD endpoints for all four entity types need request/response schemas. Also missing app/schemas/errors.py for REQ-8 standard error envelope. (tools/compose/backend/app/schemas/)
- [high] Plan STEP-41 scope is missing critical React app entry points: no App.tsx, main.tsx, or index.html. Without these, the Vite SPA cannot boot. Also missing a router configuration file despite REQ-15 requiring React Router. (tools/compose/frontend/src/)
- [high] Missing Alembic configuration files: plan creates alembic/versions/0001_foundation_tables.py but not alembic.ini, alembic/env.py, or alembic/script.py.mako. These are required to configure the isolated alembic_version_compose version table per REQ-3. (tools/compose/backend/alembic/)
- [high] PUT /api/workflows/{id} in system design accepts optional yaml_content and updates the workflow snapshot without creating a new version row. This allows silent YAML mutations that bypass the append-only audit trail required by REQ-5. The PRD says 'save-version behavior must remain append-only and immutable' but PUT provides a version-free YAML update path.
- [high] PRD REQ-7 mentions soft-delete and REQ-18 mentions 'restored' event, but no restore endpoint exists in the system design API. The plan mentions 'baseline role/schema/template CRUD with restore' but no PATCH or POST restore endpoint is defined anywhere. Soft-deleted entities have no recovery path.
- [high] SF-6 design summary says 'four atomic node types (Ask/Branch/Plugin/Error)' including ErrorNodeDefinition. SF-5 design explicitly mandates 'Three atomic node types — only Ask, Branch, and Plugin' and states 'SwitchFunctionEditor and ErrorFlowNode do not appear.' SF-5 claims these are 'persistence/bootstrap contract requirements owned by SF-5' that cascade to SF-6, but SF-6 violates the 3-node constraint.
- [high] SF-7 plan summary references repo topology 'Backend at platform/compose/backend/app/, frontend at platform/compose/frontend/src/' but SF-5 PRD REQ-1 defines topology as 'tools/compose/backend' and 'tools/compose/frontend'. Different root paths (platform/ vs tools/) across subfeatures.
- [medium] Design CMP-15 (GridCard) has no explicit file in STEP-41 scope. The plan has GridView.tsx but no GridCard.tsx component. The design defines GridCard with three states (default, warning, selected) and specific contract-warning behavior for imports.
- [medium] Plan STEP-40 scope has both app/auth.py AND app/dependencies/auth.py. The existing platform pattern (cited in PRD) uses app/dependencies/auth.py only. Having two auth files with unclear responsibility split creates confusion. (tools/compose/backend/app/)
- [medium] Plan STEP-40 scope has both app/routers/schemas.py (output schema CRUD) and app/routers/schema_export.py (GET /api/schema/workflow). Similarly app/services/schema_service.py is ambiguous about which responsibility it covers. If it serves the workflow schema endpoint, there's no service for OutputSchema CRUD. (tools/compose/backend/app/routers/)
- [medium] System design SVC-SF5-7 describes PostgreSQL technology as 'psycopg (asyncpg driver)' which conflates two different drivers. psycopg3 (used via postgresql+psycopg://) has its own async support — it is NOT asyncpg. The parenthetical is technically wrong.
- [medium] System design SVC-SF5-8 (Starter Template Bundle) is labeled kind='database' but is described as 'Bundled JSON/YAML assets checked into tools/compose/backend/app/data/' — filesystem files, not a database entity.
- [medium] Design adds four 'contract requirements' (synthetic root phase normalization, three atomic node types, cross-phase edge storage, blocking schema gate with no view-only fallback) that are not in the PRD's 18 requirements. The design claims these are 'non-negotiable persistence/bootstrap requirements owned by SF-5' but they have no REQ-* IDs, no acceptance criteria, and no plan coverage.
- [medium] Plan testid_registry has no entries for EditorSchemaBootstrapGate or YAMLContractErrorPanel, and no testid for GridCard. Only 'entity-card' exists which may partially cover GridCard but the design's warning/selected states aren't testable.
- [medium] Plan files_to_create and files_to_modify arrays are both empty despite file_manifest listing ~47 files. These appear to be legacy/unused fields but create confusion about canonical file tracking.
- [medium] SF-6 PRD summary says 'React Flow DAG canvas in iriai-workflows frontend' using the stale tools/iriai-workflows path that SF-5 REQ-1 explicitly prohibits. Indicates potential drift in the downstream consumer.
- [low] Plan STEP-40 instructions mention 'auth-python' library for JWKS validation but this package isn't a well-known Python package. The PRD citations reference deploy-console's app/dependencies/auth.py as the pattern, suggesting inline implementation rather than an external library. 'auth-python' also appears in plan dependencies without version pinning.
- [low] System design mentions 'structlog' in SVC-SF5-4 technology but it is not listed in the plan's dependencies. Plan mentions 'structured JSON logging' via middleware but doesn't specify which library to use.
- [low] No CORS middleware file in plan scope. REQ-8 requires 'explicit production CORS allow-list' but neither a middleware file nor explicit CORS setup instructions appear in STEP-40. CORS is presumably configured in main.py but the counterexample doesn't cover it either.
- [gap/critical] REQ-17 (tools hub SPA at platform/toolshub/frontend/) has no implementation step. The file_manifest entry exists but no step covers building the static authenticated SPA with hardcoded tool cards, dev_tier gating, and same-tab navigation to compose. (Missing plan step)
- [gap/critical] All 7 PRD journeys (J-1 through J-7) have zero verification blocks in the plan. journey_verifications is an empty array, providing no automated or manual verification coverage for any user journey. (Missing journey verification)
- [gap/high] PRD J-6 (cross-user workflow access denied without leaking existence) has no design annotation. This is a security-critical journey covering 404-not-403 semantics and session integrity after failed cross-user lookup. (Missing design annotation)
- [gap/high] PRD J-7 (tools hub session missing — unauthenticated user must authenticate) has no design annotation. The tools hub authentication flow, redirect handling, and post-auth return path have no design-level UX specification. (Missing design annotation)
- [gap/high] EditorSchemaBootstrapGate (CMP-18) — route-level blocking gate with loading/ready/error states, retry action, accessibility (aria-busy, focus management) — is defined in design with full state descriptions and interaction patterns but has no file in plan scope. (Missing component implementation)
- [gap/high] YAMLContractErrorPanel (CMP-19) — shared error/warning surface for stale-contract import failures with path/message rows — is defined in design with warning/error/dismissed states but has no file in plan scope. (Missing component implementation)
- [gap/high] Alembic configuration files (alembic.ini, alembic/env.py) are not in plan scope. These are required to set up the alembic_version_compose version table isolation specified by REQ-3. (Missing plan files)
- [gap/high] React SPA entry points (App.tsx, main.tsx, index.html) missing from STEP-41 scope. The Vite SPA cannot boot without these fundamental files. (Missing plan files)
- [gap/high] React Query / @tanstack/react-query is required by PRD REQ-15 and listed in system design SVC-SF5-3 technology, but absent from plan dependencies and STEP-41 instructions. The plan uses only Axios + Zustand with no query caching layer. (Missing dependency)
- [gap/high] No restore endpoint exists for soft-deleted entities. PRD REQ-7 defines soft-delete, REQ-18 lists 'restored' as an event type, and plan STEP-40 mentions 'baseline role/schema/template CRUD with restore', but no POST/PATCH restore endpoint appears in the system design API. (Missing API surface)
- [gap/high] Pydantic schema files for roles (app/schemas/role.py), output_schemas (app/schemas/output_schema.py), custom_task_templates (app/schemas/custom_task_template.py), and error responses (app/schemas/errors.py) are missing from STEP-40 scope. Only app/schemas/workflow.py exists. (Missing plan files)
- [gap/critical] SF-5 builds mutation hook infrastructure (REQ-18) assuming SF-7 will use it to maintain a materialized workflow_entity_refs table. But SF-7 design decision D-SF7-2 specifies 'YAML parsing on delete (no materialized ref table)' — SF-7 checks references by parsing YAML at delete time, not via hooks. The hook consumer doesn't exist as designed. (Cross-subfeature interface mismatch)
- [gap/high] SF-4 expects pre-seeded content with is_example flag to distinguish system vs user content, but SF-5 plan counterexample explicitly prohibits is_example column, using user_id='__system__' instead. SF-4 implementers will find no is_example flag. (Cross-subfeature interface mismatch)
**security** [FAIL]: The SF-5 artifacts establish clear security intent (JWT auth, user scoping, 404-not-403, structured logging, explicit CORS) but the plan has significant gaps translating those intentions into verifiable implementation tasks. Critical omissions include: no YAML deserialization safety requirements (safe_load, size limits, bomb protection), no request body size limits on any endpoint, no input sanitization for entity name fields, no CORS implementation file or explicit instruction, no protection of the __system__ sentinel user_id, no JWT audience/algorithm validation requirements, no security headers, and the tools hub (REQ-17) has no implementation step at all despite appearing in the file manifest. Several architectural risks reference non-existent steps from other subfeatures (contamination).

- [high] YAML deserialization safety not enforced in plan. The system design mentions 'safe_load' in one call path (CP-SF5-5 step 3) but the plan's STEP-40 instructions, acceptance criteria, and counterexamples never require yaml.safe_load() over yaml.load(). An unsafe yaml.load() call allows arbitrary Python object instantiation (code execution). No YAML document size limit or billion-laughs/entity-expansion protection is specified anywhere. (PLAN STEP-40)
- [high] No request body or field size limits specified. The import endpoint accepts 'multipart/form-data file upload or application/x-yaml body' with no max file size. yaml_content (TEXT columns) have no upper bound. JSON fields (Role.tools, Role.metadata, OutputSchema.json_schema, CustomTaskTemplate interfaces) have no size limit. SF-7 defines 256KB per-field and 1MB total body limits (REQ-42) but SF-5 ships first with none, creating a window where large payloads can exhaust memory or storage. (PLAN STEP-40)
- [high] The __system__ sentinel user_id for starter templates has no guard preventing a JWT with sub='__system__' from accessing or manipulating system-seeded content. The plan says 'System-seeded starters use user_id=__system__' but neither the auth dependency nor any middleware is instructed to reject this reserved value from incoming JWTs. An attacker with a crafted token could impersonate the system user. (PLAN STEP-40 instructions)
- [high] No input validation for entity name fields. SF-7 defines REQ-43 with regex patterns and length limits for names (1-100 chars for most, 200 for tools, specific character restrictions), but SF-5 ships baseline CRUD for all four entity types with no equivalent name validation. Workflow, Role, OutputSchema, and CustomTaskTemplate names have no length limit, no character restriction, and no sanitization against path traversal, XSS injection, or SQL meta-characters in plan instructions or acceptance criteria. (PLAN STEP-40 and STEP-41)
- [medium] CORS configuration has no implementation file or explicit instruction. REQ-8 and AC-11 require 'explicit production CORS allow-list for compose and tools-hub browser origins', but the plan scope lists no CORS middleware file (e.g., app/middleware/cors.py). The instructions mention middleware for 'correlation ID, structured JSON logging, per-user rate limiting' but omit CORS entirely. CORS presumably goes in main.py but nothing ensures the allow-list is environment-driven or that credentials are restricted. (PLAN STEP-40 scope)
- [medium] JWT validation specifics missing from plan. The instructions say 'JWKS RS256 via auth-python using AUTH_SERVICE_PUBLIC_URL' but do not require: audience claim validation (preventing tokens issued for other services), algorithm pinning (preventing alg:none or HS256 confusion attacks), explicit expiration validation, or JWKS cache TTL/rotation handling. These are delegated to 'auth-python' implicitly but never verified in acceptance criteria. (PLAN STEP-40 instructions)
- [medium] No rate limiting on public endpoints /health and /ready. REQ-9 specifies 'per-user rate limiting' which inherently applies only to authenticated endpoints. The public /health and /ready endpoints have no rate limit, making them targets for denial-of-service. /ready is particularly expensive since it checks database connectivity on every call. (PLAN STEP-40)
- [medium] No security response headers specified. Neither the plan nor system design mentions Content-Security-Policy, X-Content-Type-Options, X-Frame-Options, Strict-Transport-Security, or Referrer-Policy headers for the backend API or either frontend SPA. The tools hub and compose frontend are separate-origin apps that could be clickjacked or have content type sniffing issues. (PLAN STEP-40 and STEP-41)
- [medium] YAML export endpoint lacks Content-Disposition and Content-Type hardening. GET /api/workflows/{id}/export returns 'application/x-yaml attachment' per system design, but the plan has no instruction to set Content-Disposition: attachment or to ensure the response cannot be rendered as HTML by browsers. The PRD security profile notes 'YAML exports can leave the platform when users download or share them' but no download safety headers are planned. (SYSTEM-DESIGN api_endpoints /api/workflows/{id}/export)
- [medium] Tools hub (platform/toolshub/frontend/) has no implementation step. It appears in the file_manifest with action 'create' and is required by REQ-17, AC-3, and J-1, but neither STEP-40 nor STEP-41 covers it. This means the tools hub's authentication flow (OAuth redirect, dev_tier gating, same-tab navigation to compose) has no planned implementation, leaving an entire user-facing surface unplanned including its security posture. (PLAN file_manifest)
- [medium] No database connection encryption requirement. The plan specifies postgresql+psycopg:// URL normalization but never mentions SSL/TLS for the database connection (e.g., sslmode=require). Railway-hosted PostgreSQL may default to encrypted connections, but this is not documented or enforced in the connection configuration instructions. (PLAN STEP-40 instructions)
- [medium] Import file upload has no file type validation. POST /api/workflows/import accepts 'multipart/form-data file upload or application/x-yaml body' but the plan has no instruction to validate file extension, MIME type, or content magic bytes. An attacker could upload executable files or other non-YAML content. (SYSTEM-DESIGN api_endpoints /api/workflows/import)
- [low] Migration rollback not tested in acceptance criteria. STEP-40 instructions say 'include working downgrade()' for the Alembic migration, but no acceptance criterion verifies that the downgrade actually works (e.g., 'Running alembic downgrade base succeeds and removes all 5 tables'). A broken downgrade discovered in production is unrecoverable without manual SQL. (PLAN STEP-40 acceptance_criteria)
- [low] Frontend token storage strategy not specified. STEP-41 mentions '@homelocal/auth with compose_ token prefix' but does not specify whether tokens are stored in memory, localStorage, or sessionStorage. localStorage is vulnerable to XSS. The PRD says 'bearer-token API calls rather than cookie-bound mutation flows' but doesn't address storage location. (PLAN STEP-41 instructions)
- [low] Structured logging field redaction scope too narrow. REQ-9 and the plan say 'no raw YAML or prompt-body logging', but the plan doesn't specify what IS logged. Structured JSON logging with request correlation could inadvertently log query parameters containing workflow names, search terms, or description text that might contain sensitive information. No explicit allowlist of logged fields is defined. (PLAN STEP-40 instructions)
- [low] Architectural risks RISK-81, RISK-82, RISK-83, and RISK-86 reference non-existent steps (STEP-36, STEP-37, STEP-39, STEP-42, STEP-45). These appear to be contamination from other subfeatures' plans and do not belong in the SF-5 plan artifact. (PLAN architectural_risks)
- [gap/medium] PRD REQ-9 requires 'per-user rate limiting' but the plan has no specification of rate limit values (requests/minute, burst limits), rate limit storage backend (in-memory, Redis), or per-endpoint differentiation. The acceptance criterion only says 'returns 429 with Retry-After' without specifying what the limits are. (security)
- [gap/medium] PRD security_profile says 'SF-5 foundation must not store plugin runtime secrets, custom tool configs, or runner-managed credentials' but the plan has no acceptance criterion or counterexample that verifies no secret-bearing columns or fields exist in the five foundation tables. The Role.metadata JSON field and CustomTaskTemplate interfaces could inadvertently store secrets. (security)
- [gap/medium] No plan task implements dev_tier claim gating on the backend. REQ-6 says 'expose dev_tier for tools-hub gating' and the PRD describes tools-hub tier-gated card visibility, but the plan's auth dependency (STEP-40) only extracts user_id from sub. No instruction says to parse or propagate dev_tier, and no endpoint uses it for access control. (security)
- [gap/low] No CSRF protection discussion. While Bearer tokens mitigate traditional CSRF, the PRD mentions '@homelocal/auth' for browser clients. If any auth flow uses cookies (e.g., refresh token rotation, OAuth state), CSRF protection would be needed. The plan doesn't confirm the auth flow is purely bearer-token based end-to-end. (security)
- [gap/high] The tools hub SPA (platform/toolshub/frontend/) is in the file manifest but has no implementation step. REQ-17 requires a static authenticated SPA with dev_tier-gated cards and same-tab navigation. Without a step, the tools hub's OAuth flow, token handling, and tier gating are unplanned. (implementation)
- [gap/low] No search query sanitization or parameterization requirement. STEP-40 mentions 'search' on workflow list endpoints and STEP-41 mentions '300ms debounce on name filter', but neither specifies backend search input sanitization, max query length, or that search must use parameterized SQL queries (even with ORM, raw SQL search is common for full-text). (security)

### dag-loader-runner [FAIL]

**completeness** [FAIL]: The SF-2 Plan is fundamentally misaligned with the SF-2 PRD and System Design. The Plan appears to implement the stale SF-1 Plan/Design schema (with switch_function, merge_function, stores, plugin_instances, interaction actor type) rather than the canonical SF-1 PRD schema that the SF-2 PRD was specifically revised to enforce. This is exactly the "stale artifact survival" scenario the PRD's problem statement was written to prevent. Additionally, the PRD-required validate() function is entirely missing from the Plan, the Plan's PortDefinition lacks the schema_def field required for XOR enforcement, the Plan's BranchNode uses an entirely different routing model (switch_function/per-port conditions) than the PRD's (condition_type/condition/paths), and the Plan's WorkflowConfig includes six root fields the PRD explicitly forbids. At least 13 of 18 PRD requirements cannot be satisfied by the Plan as written.

- [critical] Plan's BranchNode implements switch_function and merge_function, which PRD REQ-8 and REQ-14 explicitly require SF-2 to REJECT. The PRD says 'switch_function, merge_function, and per-path alternate routing models are not valid.' The System Design decision D-SF2-4 also says these must be rejected. Yet the Plan's Schema Entity Reference defines switch_function and merge_function as core BranchNode fields with full execution semantics (D-SF2-22, D-SF2-23, D-SF2-47), and STEP-13/14/15 implement them. (Plan — BranchNode section)
- [critical] Plan's BranchNode has no condition_type, condition, or paths fields — the three fields the PRD REQ-8 defines as the ONLY valid routing surface. The PRD's BranchNode entity specifies 'condition_type: expression | output_field', 'condition (str)', and 'paths (dict[str, PortDefinition])'. The Plan instead uses switch_function for exclusive routing and per-port output conditions for non-exclusive routing — a completely different routing model that the PRD was revised to eliminate. (Plan — BranchNode section)
- [critical] Plan's WorkflowConfig includes 'stores' and 'plugin_instances' as valid root fields, directly contradicting PRD REQ-2 which says 'The loader MUST reject unapproved root additions such as stores, plugin_instances' and the System Design decision D-SF2-1 which says 'Root stores and plugin_instances are explicitly rejected.' The Plan's WorkflowConfig table lists both: stores (dict[str, StoreDefinition]) and plugin_instances (dict[str, PluginInstanceConfig]). (Plan — WorkflowConfig section)
- [critical] Plan uses actor type 'interaction' instead of 'human'. The Plan's ActorDefinition says type ('agent'|'interaction'), directly contradicting PRD REQ-3 ('actor_type is only agent or human'), the System Design decision D-SF2-8 ('actor_type: human is the serialized schema contract'), and the PRD's explicit prohibition: 'The loader MUST reject stale actor discriminators including interaction.' (Plan — ActorDefinition in Supporting Types)
- [critical] PRD REQ-12 requires SF-2 to expose 'validate(workflow) for structural validation without live runtimes.' The System Design lists it as an explicit API endpoint (sf2_public_api). The Design lists a 'Validation facade' component. But no Plan step implements validate(). STEP-10 has validate_runtime_requirements() (runtime-specific), STEP-17 implements only run(). STEP-18 exports do not include validate. The sf2_validator service from the System Design has no corresponding implementation step. (Plan — missing STEP)
- [critical] Plan's PortDefinition lacks 'schema_def' field, making PRD REQ-5's XOR enforcement impossible. PRD requires 'Each port must define exactly one of type_ref or schema_def.' The Plan's PortDefinition has only: name, type_ref, description, condition. The SF-1 PRD Summary confirms 'PortDefinition enforces mutually exclusive type_ref | schema_def via Pydantic validator.' Without schema_def, typed port validation (REQ-5, AC-6) cannot be implemented. (Plan — PortDefinition section)
- [critical] Plan adds 'inputs' and 'outputs' to WorkflowConfig as '[SF-2 addition]', violating PRD REQ-2's closed root set. The PRD defines exactly twelve approved root fields (schema_version, workflow_version, name, description, metadata, actors, phases, edges, templates, plugins, types, cost_config). The Plan also adds context_keys and context_text to WorkflowConfig root — four additional fields beyond the PRD's closed set. (Plan — WorkflowConfig section)
- [high] Plan's nested phase field is named 'phases' but PRD and Design both use 'children'. PRD PhaseDefinition entity says 'children (list[PhaseDefinition])'. Design says 'recurse only through children[]' and 'phases[].children for nested child phases.' Plan's PhaseDefinition uses 'phases (list[PhaseDefinition])' with comment 'Nested sub-phases (recursive)'. This naming mismatch would cause the loader to look for the wrong field. (Plan — PhaseDefinition section)
- [high] Plan's ExecutionResult lacks 'history' and 'phase_metrics' fields required by PRD REQ-15. The PRD requires 'ExecutionResult plus ExecutionHistory / phase metrics keyed by logical phase ID.' The System Design's ExecutionResult entity includes both 'history: list[ExecutionHistory]' and 'phase_metrics: dict[str, Any]'. The Plan's ExecutionResult dataclass has neither field. (Plan — ExecutionResult in STEP-17)
- [high] Plan's WorkflowConfig omits 'workflow_version' field, which is in the PRD's closed root set. PRD data entity WorkflowConfig lists 'workflow_version (int)' as a required field. System Design lists it as 'workflow_version: int, constraints: required'. Plan's WorkflowConfig table jumps from schema_version to name, omitting workflow_version entirely. (Plan — WorkflowConfig section)
- [high] Plan's PhaseDefinition uses separate mode config fields (sequential_config, map_config, fold_config, loop_config) instead of a single mode_config field. The System Design's PhaseDefinition entity has 'mode_config: ModeConfig | None'. The PRD's PhaseDefinition lists 'mode-specific config' as a single field. The Plan uses four separate optional fields, which is a different data model. (Plan — PhaseDefinition section)
- [high] PRD ports are dicts (dict[str, PortDefinition]), but Plan ports are lists (list[PortDefinition]). PRD PhaseDefinition: 'inputs (dict[str, PortDefinition])'. Plan PhaseDefinition: 'inputs: list[PortDefinition]'. This structural difference affects how ports are indexed and referenced throughout the system. The PRD's dict model keys ports by name; the Plan's list model requires name lookup. (Plan — PhaseDefinition/NodeBase port fields)
- [high] Plan's expression evaluation uses exec() with 'no explicit timeout' (D-SF2-5), contradicting the System Design's sf2_expression_runtime service which specifies 'AST allowlist, size limits, timeout, scoped builtins' and the PRD REQ-16 which references 'the shared expression security contract.' The SF-1 PRD Summary confirms 'Expression evaluation security contract (AST allowlist, blocked builtins, size/depth/comprehension limits, 5s timeout).' (Plan — STEP-13 transforms.py)
- [high] System Design RuntimeConfig requires artifacts (ArtifactStore) and context_provider (ContextProvider) as non-optional. Plan's RuntimeConfig makes both optional with InMemory defaults: 'artifacts: ArtifactStore | None = None', 'context_provider: ContextProvider | None = None'. This discrepancy means the Plan's validation surface differs from the System Design. (Plan — STEP-11 RuntimeConfig)
- [high] Plan's WorkflowConfig field 'cost' differs from PRD's 'cost_config'. PRD closed root set explicitly includes 'cost_config'. Plan has 'cost: CostConfig | None'. Field name mismatch would cause the loader to fail on canonical workflows or accept a field name not in the PRD contract. (Plan — WorkflowConfig section)
- [high] Cross-SF contradiction: SF-4 Plan Summary says 'no artifact_key on NodeBase [D-GR-14]' while SF-2 Plan extensively implements artifact_key dual read+write on NodeBase (D-SF2-40, D-SF1-29, C-4). These decisions directly conflict — SF-4 expects explicit ArtifactPlugin writes while SF-2 implements auto-write via artifact_key. Migrated workflows will behave differently depending on which decision is followed. (Plan — artifact_key semantics vs SF-4 summary)
- [medium] PRD AC-2, AC-4, AC-6, AC-8 all require validation to fail with specific field-path errors for stale contract drift. Since validate() is not implemented in the Plan, none of these acceptance criteria can be verified. AC-2 (stores/plugin_instances rejection), AC-4 (interaction actor rejection), AC-6 (XOR typed ports), AC-8 (switch_function/merge_function rejection) are all unverifiable. (Plan — missing validate() implementation)
- [medium] System Design's sf2_validator service ('Structural Validator') has no corresponding Plan step. The System Design describes it as: 'Shared validation surface used by both validate() and run(). Enforces nested children[] containment, typed ports including hooks, branch-path existence, hook-edge transform rejection, cross-phase boundary rules, and stale-field rejection with field-path errors.' No Plan step creates this validation surface. (System Design — sf2_validator service)
- [medium] Design lists 'Schema export bridge' as a component. No Plan step implements schema export functionality or bridges to /api/schema/workflow. While the backend endpoint is SF-5's responsibility, the PRD REQ-13 requires SF-2 to ensure the models powering this endpoint are the same ones used for validation/execution. (Design — components list)
- [medium] PRD's PortDefinition has 'required (Optional[bool]) — for input ports' field. Plan's PortDefinition has no 'required' field. This means the Plan cannot enforce required vs optional input ports as the PRD contract requires. (Plan — PortDefinition section)
- [medium] System Design's ValidationError entity has 'severity: error | warning' and 'field_path', 'message' fields. Plan's errors.py creates exception classes (WorkflowLoadError, ExpressionEvalError, etc.) but no ValidationError dataclass for structured validation results. The PRD data entity ValidationError (field_path, message, severity, code) has no implementation in any Plan step. (Plan — STEP errors.py)
- [medium] System Design's BranchNode entity specifies 'paths: min 2' constraint. Plan's BranchNode uses 'outputs: 1+' constraint. The PRD says BranchNode paths must have typed entries. These are different minimum cardinality requirements — the System Design requires at least 2 branch paths while the Plan allows a single-output 'passthrough' branch. (System Design — branch_node entity vs Plan BranchNode)
- [medium] Plan Journey J-3 (Reject stale actor/root-shape drift) and J-4 (Reject stale hook/branch serialization) are failure paths that depend on validate() producing field-path errors for stale fields. Since the Plan doesn't implement validate() and actually implements the stale fields (switch_function, merge_function, interaction, stores, plugin_instances) as valid, these journeys are completely unaddressable. (PRD Journeys J-3, J-4)
- [medium] PRD REQ-14 lists 14 specific stale fields that must be rejected: stores, plugin_instances, top-level nodes, interaction actor, missing typed hook ports, switch_function, merge_function, unknown branch paths, serialized port_type, separate hook sections, invalid nested containment, hook edges carrying transform_fn. The Plan only addresses the last item (hook transform_fn rejection in STEP-16). All others are either implemented as valid features or not addressed. (Plan — no comprehensive stale-field rejection logic)
- [medium] PRD REQ-16 requires BranchNode.condition to use 'the shared expression security contract only when condition_type=expression; output_field remains declarative lookup.' Since the Plan has no condition_type field on BranchNode, this security boundary between expression evaluation and declarative lookup cannot be enforced. All branch routing in the Plan goes through exec()-evaluated expressions. (Plan — BranchNode execution model)
- [medium] Plan's STEP-19 integration tests include 14 branch tests that test switch_function behavior (tests 11-14), but the PRD requires switch_function to be rejected. These tests validate the wrong behavior. Similarly, branch tests 2 (merge_function) and 10 (merge with artifact) test merge_function which the PRD prohibits. (Plan — STEP-19 branch tests)
- [medium] System Design call path CP-3 (Reject Stale YAML) step 3 says validator must 'reject port_type, separate hooks, switch_function, merge_function, stores, plugin_instances, and interaction actor alias.' The Plan implements switch_function, merge_function, stores, plugin_instances, and interaction as valid features rather than rejecting them. (System Design CP-3 vs Plan implementation)
- [low] Plan's PhaseDefinition has 'name' not explicitly listed in the field table (missing from the Schema Entity Reference table), though PRD and System Design both include it as required. The Plan's NodeBase has 'summary' but no 'name'. The Plan appears to use 'id' as the primary identifier with optional 'summary' for display, while the PRD expects both id and name. (Plan — PhaseDefinition section)
- [low] PRD mentions TemplateDefinition in the WorkflowConfig closed root set (as 'templates'), but the Plan has no template expansion or instantiation logic. No Plan step handles template resolution. STEP-19 has no template-related tests. (Plan — missing template handling)
- [low] System Design's RuntimeConfig entity has 'plugin_registry: dict[str, Any] | None' while Plan's RuntimeConfig has 'plugin_registry: PluginRegistry | None'. These are different types — the System Design uses a plain dict while the Plan uses a typed PluginRegistry class. (System Design — runtime_config entity vs Plan RuntimeConfig)
- [gap/critical] The Plan implements the stale SF-1 Plan/Design schema rather than the canonical SF-1 PRD schema. The entire SF-2 PRD revision was made to prevent this exact scenario. The BranchNode model (switch_function/merge_function vs condition_type/condition/paths), actor type (interaction vs human), root fields (stores/plugin_instances), and PortDefinition (missing schema_def) all reflect the stale artifacts the PRD was written to reject. (PRD-Plan alignment)
- [gap/critical] validate(workflow) function — required by PRD REQ-12, described in Design, System Design, and multiple acceptance criteria (AC-2, AC-4, AC-6, AC-8) — has no implementation step in the Plan. This is the primary structural validation surface that makes SF-2 usable without live runtimes. (Missing implementation)
- [gap/critical] No Plan step implements stale-field rejection logic for the 14 specific items listed in PRD REQ-14. The Plan not only fails to reject these fields but actively implements several of them (switch_function, merge_function, stores, plugin_instances, interaction). (Missing implementation)
- [gap/high] Plan's ExecutionResult is missing the 'history' (list[ExecutionHistory]) and 'phase_metrics' (dict keyed by logical phase ID) fields required by PRD REQ-15 and defined in the System Design's execution_result entity. (Missing data model)
- [gap/high] Plan has no ValidationError dataclass (field_path, message, severity, code) defined in the PRD data entities and System Design. Without this, validate() cannot return structured field-path errors as required by multiple acceptance criteria. (Missing data model)
- [gap/high] Plan STEP-19 has zero tests for stale-field rejection. No tests verify that stores, plugin_instances, interaction, switch_function, merge_function, port_type, or separate hook sections are rejected. Instead, tests validate switch_function as a working feature. (Missing test coverage)
- [gap/high] SF-4 Plan Summary states 'no artifact_key on NodeBase [D-GR-14]' expecting explicit ArtifactPlugin writes. SF-2 Plan builds extensive artifact_key dual read+write semantics on NodeBase. These contradict each other and will produce incompatible migration output. (Cross-SF contradiction)
- [gap/high] PRD Journeys J-3 (Reject stale actor/root drift) and J-4 (Reject stale hook/branch serialization) have no verification path in the Plan since the Plan implements the stale fields as valid rather than rejecting them. (Journey coverage)
- [gap/medium] PRD REQ-16's expression security boundary (condition_type=expression uses sandbox; output_field uses declarative lookup) has no Plan implementation since BranchNode lacks condition_type. All branch evaluation goes through exec() with the same trust level. (Missing implementation)
**security** [FAIL]: Critical security gaps between the PRD security profile and the plan's implementation. The expression sandbox contract is contradicted by the plan's "full exec(), trust author" approach. The standalone validate() function required by REQ-12 is never implemented, removing the pre-execution security gate. The plan actively implements switch_function/merge_function and accepts stores/plugin_instances — all of which the PRD explicitly mandates rejecting. Plugin ExecutionContext grants unrestricted runner access with no capability model.

- [critical] Expression evaluation uses bare exec() with restricted builtins instead of the AST allowlist + size/depth limits + 5s timeout mandated by the PRD security profile (REQ-16) and SF-1 REQ-21. Plan decision D-SF2-5 ('Full exec(), trust author') directly contradicts the security contract. Python exec() with only restricted builtins is escapable via class hierarchy traversal (e.g., ().__class__.__bases__[0].__subclasses__()) without AST validation. (PLAN STEP-13)
- [critical] The standalone validate(workflow) function required by PRD REQ-12 is never implemented in any plan step and is absent from STEP-18's public exports. This removes the pre-execution structural security gate that ACs 2, 4, 5, 6, 8 and journeys J-3, J-4 depend on. Without validate(), stale/malicious workflow shapes cannot be caught before execution. (PLAN STEP-18)
- [critical] Plan actively implements switch_function (D-SF2-47, STEP-14 branch executor, STEP-19 tests 11-14) and merge_function (STEP-14 merge multi-input) which PRD REQ-8 and REQ-14 explicitly mandate rejecting. The PRD says 'switch_function, merge_function... are not valid' and validation MUST reject them. This completely undermines the PRD's branch execution security model. (PLAN STEP-14)
- [critical] Plan's WorkflowConfig Schema Entity Reference includes plugin_instances and stores as valid root fields, but PRD REQ-2 says 'The loader MUST reject unapproved root additions such as stores, plugin_instances... with actionable field-path errors.' The plan's entity reference is the canonical reference for the runner — having these fields present means they will be accepted, not rejected. (PLAN Schema Entity Reference - WorkflowConfig)
- [high] PRD REQ-16 specifies that output_field branch routing 'uses only declarative field lookup with no Python evaluation.' The plan does not implement condition_type/condition/paths routing at all — it implements switch_function/per-port-condition routing instead. The security boundary between sandboxed expression evaluation and safe declarative lookup described in REQ-16 is entirely absent. (PLAN STEP-14)
- [high] ExecutionContext grants plugins unrestricted access via 'runner: Any' field (D-SF2-34). No capability model, sandboxing, or permission boundary is defined. A plugin with runner access can execute arbitrary workflows, access other plugins' data, or manipulate the artifact store without constraint. The plan has no task to restrict what plugins can do with the runner reference. (PLAN STEP-11)
- [high] Plan STEP-13 explicitly states 'Timeout: no explicit timeout (same trust as .py files per D-SF2-5)' for expression evaluation. The system design's sf2_expression_runtime service description specifies 'timeout' as part of the security controls. SF-1 REQ-21 specifies a 5s timeout. The plan's decision to omit timeouts enables DoS via infinite-loop expressions in workflow YAML. (PLAN STEP-13)
- [medium] ActorDefinition includes identity and channel fields for human actors that may contain PII (email addresses, user identifiers, phone numbers). No plan step addresses masking these values in ExecutionResult, ExecutionHistory, hook_warnings, error messages, or any logging output. STEP-15.5 (Actor Hydration) and STEP-14 (Node Executors) handle these fields without any PII controls. (PLAN STEP-15.5)
- [medium] No YAML document size or nesting depth validation before parsing. The plan delegates to SF-1's yaml.safe_load() which prevents deserialization attacks but does not limit document size. An extremely large or deeply nested YAML workflow could cause memory exhaustion or stack overflow during recursive DAG construction (STEP-12 graph builder, STEP-15 recursive phase execution). (PLAN STEP-10)
- [medium] STEP-13 allowed builtins list includes 'type' and 'isinstance' which, combined with unrestricted attribute access, can be used to traverse Python's type system and escape the restricted builtins sandbox. Without AST validation blocking attribute access patterns like __class__, __bases__, __subclasses__, the exec() sandbox is insufficient. (PLAN STEP-13)
- [medium] The system design describes sf2_expression_runtime as applying 'AST allowlist, size limits, timeout, scoped builtins' but the plan's STEP-13 implements none of AST validation, size limits, or timeout. The system design and plan are architecturally inconsistent on the expression security boundary. (System Design sf2_expression_runtime vs PLAN STEP-13)
- [low] TrackingArtifactStore (STEP-17) records all put() keys but does not validate key format. Artifact keys use dot-notation (e.g., 'artifacts.prd') but no regex validation or character sanitization is specified, potentially allowing injection through crafted key names if the underlying store uses keys in file paths or queries. (PLAN STEP-17)
- [gap/critical] PRD REQ-16 and SF-1 REQ-21 mandate a shared expression security contract with AST allowlist, blocked dangerous builtins, size/depth/comprehension limits, 5s timeout, and per-context variable scopes. No plan step implements AST validation, size limits, depth limits, comprehension limits, or timeout. STEP-13 uses raw exec() with only a builtins allowlist. (Expression Security)
- [gap/critical] PRD REQ-12 requires validate(workflow) as a standalone structural validation function without live runtimes. System design lists it. ACs 2/4/5/6/8 and journeys J-3/J-4 call it. No implementation step exists in the plan. STEP-18 exports omit it entirely. (Validation API)
- [gap/critical] PRD REQ-2 and REQ-14 require rejecting stores, plugin_instances, switch_function, merge_function with actionable errors. Plan implements switch_function/merge_function as core features and includes stores/plugin_instances in the WorkflowConfig entity. No plan step implements the rejection logic the PRD mandates. (Stale Field Rejection)
- [gap/high] PRD REQ-16 defines a two-mode branch security model: condition_type=expression uses sandbox, condition_type=output_field uses declarative lookup (no eval). Plan does not implement condition_type at all — uses switch_function/per-port-conditions instead, collapsing both into exec()-evaluated expressions with no declarative-only path. (Branch Security Model)
- [gap/high] ExecutionContext.runner gives plugins unrestricted runner access for nested DAG execution (D-SF2-34). No plan step defines a capability model, permission boundary, or sandboxing for what plugins can do with this reference. A malicious or buggy plugin could execute arbitrary workflows. (Plugin Capability Boundary)
- [gap/medium] No plan step addresses masking or redacting human actor identity/channel fields in execution results, logs, error messages, or traces. PRD security profile acknowledges human actors contain 'identity, channel' but no implementation task ensures these aren't leaked. (PII Handling)
- [gap/medium] No plan step validates YAML document size or nesting depth before parsing. While yaml.safe_load() prevents deserialization attacks, unbounded document size/depth can cause DoS during recursive graph construction. (Input Size Validation)

### declarative-schema [FAIL]

**completeness** [FAIL]: SF-1 artifacts are largely well-aligned on the core schema contract (nested phases, three node types, edge-based hooks, closed root, actor_type discriminator), but contain multiple material contradictions and stale references that would produce implementation drift. The most critical issues are: (1) Plan uses four separate mode config fields while Design decision D-SF1-38 mandates a single discriminated-union mode_config field; (2) Plan adds PluginNode.instance_ref referencing the explicitly-removed plugin_instances concept; (3) Plan carries three stale store-related error codes for a stores registry the PRD removes; (4) Plan's AskNode field names (task, context_text) contradict Design/SystemDesign (prompt, output_type); (5) Design introduces synthetic root phase normalization that neither the PRD specifies nor the Plan implements; (6) System Design has workflow-level input/output entities and D-SF1-30 declaring them, contradicting the PRD's closed root set; (7) Two PRD journeys (J-6, J-7) have no plan verification blocks. Collectively these gaps would cause downstream SFs to implement against inconsistent contracts.

- [high] Plan STEP-2 PhaseDefinition uses four separate mode config fields (sequential_config, map_config, fold_config, loop_config) but Design decision D-SF1-38 explicitly says 'PhaseDefinition uses a single mode_config field (discriminated union of SequentialModeConfig | MapModeConfig | FoldModeConfig | LoopModeConfig) replacing the previous four separate config fields.' The PRD data entity for PhaseDefinition also lists a single 'ModeConfig' entity. The system design entity also uses a single 'mode_config' field. (Plan STEP-2 (phases.py))
- [high] Plan STEP-2 adds instance_ref to PluginNode with XOR against plugin_ref: 'PluginNode(NodeBase) with ... instance_ref: str | None = None ... and a model_validator enforcing XOR between plugin_ref and instance_ref.' But the PRD (REQ-1, REQ-10) explicitly forbids plugin_instances as a root registry, and the system design entity for PluginNode has only plugin_ref with no instance_ref. instance_ref has nowhere to resolve to in the PRD-canonical schema. (Plan STEP-2 (nodes.py))
- [high] Plan STEP-3 defines three stale error codes referencing stores: invalid_store_ref, invalid_store_key_ref, store_type_mismatch. The PRD (REQ-1, REQ-12) explicitly excludes stores from WorkflowConfig's closed root field set. These error codes reference a concept that does not exist in the PRD-canonical schema and would confuse implementers. (Plan STEP-3 (validation.py))
- [high] Plan STEP-2 AskNode uses field names 'summary', 'task', 'context_text' for prompt-related fields, but Design CMP-11 specifies 'actor: str, prompt: str, output_type: Optional[str | dict]' and the System Design entity ask_node has 'actor: str, prompt: str'. The PRD REQ-11 references 'task prompt' but the canonical field name in the design is 'prompt'. This naming mismatch will cause incompatibility with SF-6 editor inspectors that expect 'prompt'. (Plan STEP-2 (nodes.py))
- [high] Design introduces synthetic root phase normalization (__root__, mode: sequential) as a first-class schema contract: 'loading a WorkflowConfig whose phases list is empty normalizes a synthetic root phase.' This is referenced extensively in CMP-9 verifiable states, J-1/J-2/J-4 annotations, and the approach section. However, the PRD has no mention of synthetic root normalization, and the Plan implements no such behavior in any step. If this is a required contract, the Plan is incomplete; if it's not, the Design introduces an ungrounded concept. (Design approach + CMP-9 synthetic-root state)
- [high] System Design decision D-SF1-30 states 'WorkflowConfig carries typed workflow-level inputs and outputs declarations' and defines separate workflow_input_definition and workflow_output_definition entities with entity_relations to workflow_config. But the PRD (REQ-1) explicitly closes the root field set to: schema_version, workflow_version, name, description, metadata, actors, phases, edges, templates, plugins, types, cost_config — with NO inputs or outputs fields. The Plan's WorkflowConfig also has no inputs/outputs fields, matching the PRD. D-SF1-30 directly contradicts REQ-1. (System Design decisions + entities)
- [high] Plan STEP-3 defines error code 'invalid_workflow_io_ref' with check '_check_workflow_io_refs(): Verify WorkflowConfig.inputs and outputs type refs resolve correctly' (also in System Design CP-3 step 19). But WorkflowConfig has no inputs or outputs fields in the PRD or the Plan's own STEP-2 definition. This validation check references fields that don't exist. (Plan STEP-3 (validation.py) + System Design CP-3)
- [high] PRD journeys J-6 (Composer detects schema-source drift or endpoint failure) and J-7 (Migration output using stale hook or branch fields fails fast and is corrected) have no corresponding verification blocks in the Plan's journey_verifications section. The plan only verifies J-1 through J-5. (Plan journey_verifications)
- [medium] System Design CMP-10 WorkflowConfig props_variants lists 'workflow_version: str' but the PRD data entity specifies 'workflow_version: int', the Plan STEP-2 uses 'workflow_version: int', and the System Design's own WorkflowConfig entity field says 'workflow_version: int, default 1'. The CMP-10 type annotation contradicts all other sources. (System Design CMP-10 props_variants)
- [medium] System Design entities workflow_input_definition and workflow_output_definition both define a 'name' field, but Plan STEP-1 explicitly forbids it: 'No name field on any of these models; the dict key in the parent mapping is the port's name.' The Plan's counterexamples section reinforces: 'Do NOT add a name field onto PortDefinition, WorkflowInputDefinition, or WorkflowOutputDefinition.' (System Design entities vs Plan STEP-1)
- [medium] System Design entity workflow_input_definition has a 'default: Any | None' field 'allowed only when required=False'. The Plan's WorkflowInputDefinition model has no default field (only type_ref, schema_def, description, required). This field is missing from the Plan's implementation. (System Design entity workflow_input_definition)
- [medium] System Design FoldModeConfig entity defines 'accumulator_init: str, required' (an expression string), but Plan STEP-2 FoldConfig has 'accumulator_init: dict | None = None' (an optional dict). The PRD ModeConfig entity lists 'accumulator_init' without specifying the type, so Plan and Design disagree on both type (str vs dict) and optionality (required vs optional). (Plan STEP-2 (phases.py) vs System Design fold_config entity)
- [medium] Design extensively specifies cross-phase edge ownership validation: 'every edge in a PhaseDefinition.edges list is checked to confirm that both source and target resolve to nodes within the same phase' with error message 'edge[N] crosses phase boundary; move to workflow.edges'. But the Plan's 29 error codes do not include a specific code for cross-phase edge misplacement in phase.edges. The closest code is 'phase_boundary_violation' which only covers $input/$output pseudo-port wiring. (Design J-3 step annotations vs Plan STEP-3)
- [medium] System Design entity for AgentActorDef/actor_definition defines 'role: RoleDefinition | None, required when actor_type=agent', but Plan STEP-1 defines it as 'role: str | RoleDefinition' (union accepting string reference OR inline definition). These are incompatible — the Design allows None but not str; the Plan allows str but presumably not None. (Plan STEP-1 (actors.py) vs System Design actor_definition entity)
- [medium] PRD defines HookPortEvent data entity with 9 fields (source_id, source_type, event, status, result, error, timestamp, duration_ms, cost_usd) but the Plan never defines this model. System Design default hook ports use type_ref='HookPortEvent', but Plan STEP-1 default hooks use type_ref='any'. If HookPortEvent is never defined as a registered type, hook port type validation will either fail or lose typed guarantees. (Plan STEP-1/STEP-2 (missing HookPortEvent model))
- [medium] Plan STEP-2 BranchNode inherits 'outputs: dict[str, WorkflowOutputDefinition]' from NodeBase alongside its own 'paths: dict[str, WorkflowOutputDefinition]'. The Design says 'BranchNode replaces data-routing outputs with paths' (system design entity constraint). But the Plan does not enforce that BranchNode.outputs is empty or overridden. Without validation, a BranchNode could have both regular outputs and paths, creating ambiguous routing. (Plan STEP-2 (nodes.py BranchNode))
- [medium] PRD AC-16 (composer backend serves /api/schema/workflow and frontend fetches it) is mapped to REQ-16 and listed as an SF-1 acceptance criterion, but it requires a running backend endpoint that is outside SF-1's file scope (the Plan only creates iriai-compose/schema/ package). This AC is unverifiable within SF-1's plan steps alone — it requires SF-5 backend infrastructure. (PRD AC-16 vs Plan scope)
- [medium] PRD REQ-18 (backward compatibility with existing imperative iriai-compose API) is listed as a must-have but the Plan has no backward compatibility test. STEP-4 doesn't include any test verifying that the new schema/ package doesn't break existing imports from iriai_compose.actors, iriai_compose.tasks, iriai_compose.workflow, or iriai_compose.runner. (Plan STEP-4 (missing backward compatibility tests))
- [medium] Plan STEP-4 has no dedicated fixture or test for PRD AC-6 (loop-mode phase without max_iterations validates successfully with both exit ports still available). The loop_exits.yaml fixture likely includes max_iterations. AC-6 specifically tests the case where max_iterations is omitted. (Plan STEP-4 (missing AC-6 fixture))
- [medium] Plan STEP-4 has no dedicated fixture or test for PRD AC-22 (YAML shorthand with bare string type name normalized to typed port definition). The Plan STEP-3 mentions shorthand normalization in yaml_io.py but STEP-4 has no round-trip test exercising it. (Plan STEP-4 (missing AC-22 test))
- [low] PRD EdgeDefinition data entity lists only three fields (source, target, transform_fn) but Plan and Design both add a 'description: Optional[str]' field. While reasonable, this field is not in the PRD's canonical entity definition. (PRD data entity EdgeDefinition vs Plan/Design)
- [low] System Design RoleDefinition entity has only 3 fields (name, prompt, tools) but Plan STEP-1 defines 6 fields (name, prompt, tools, model, effort, metadata) 'mirroring the existing iriai_compose.actors.Role field-for-field.' The system design's entity is incomplete relative to the plan. (System Design role_definition entity)
- [low] Plan STEP-2 creates PluginInstanceConfig and TemplateRef models that are not in the PRD data entities or system design. PluginInstanceConfig is particularly problematic given the explicit removal of plugin_instances from the root. (Plan STEP-2 (plugins.py, templates.py))
- [low] Plan STEP-4 has a fixture 'invalid_root_stores.yaml' and an acceptance criterion for plugin_instances rejection, but no 'invalid_root_plugin_instances.yaml' fixture file is listed in the file manifest despite being tested. The plugin_instances test would need to be inline rather than fixture-based. (Plan STEP-4 file_manifest)
- [low] Design references decision 'D-GATE-2' in journey J-1 not_criteria and J-3 step annotations ('decision: D-GATE-2, code: .iriai/artifacts/features/beced7b1/plan-review-discussion-3.md:131') but this decision ID is not defined in any of the SF-1 artifacts. It appears to be an external review decision that should be traceable but is not grounded here. (Design J-1 not_criteria, J-3 annotations)
- [low] Plan STEP-4 has no dedicated fixture or test for PRD AC-14 (inter-phase cycle outside loop mode detected with clear cycle path). Cycle detection is mentioned in STEP-3 but no cycle-producing negative fixture is in the STEP-4 file manifest. (Plan STEP-4 (missing AC-14 fixture))
- [gap/high] Synthetic root phase normalization (__root__ phase created when WorkflowConfig.phases is empty) is specified in the Design's approach, CMP-9 verifiable states, and multiple journey annotations, but has no corresponding PRD requirement and no Plan implementation step. (missing implementation)
- [gap/medium] PRD Journey J-6 (composer detects schema-source drift / endpoint failure) has no verification blocks in the Plan's journey_verifications. This journey verifies the blocking bootstrap gate behavior specified in REQ-16. (missing verification)
- [gap/medium] PRD Journey J-7 (migration output with stale hook/branch fields fails fast) has no verification blocks in the Plan's journey_verifications. This journey validates that stale translation output is rejected. (missing verification)
- [gap/medium] PRD defines HookPortEvent as a data entity with 9 fields to serve as the typed payload for hook port edges. Neither the Plan nor any step creates this model. System Design references it as the default hook port type_ref, but the Plan defaults hooks to type_ref='any' instead. (missing model)
- [gap/medium] No backward compatibility test verifying REQ-18 (existing imperative iriai-compose subclass API not broken). The Plan's STEP-4 test scope covers the new schema package but never imports or exercises the existing actors.py, tasks.py, workflow.py, or runner.py modules to confirm they still work. (missing test coverage)
- [gap/high] System Design includes WorkflowConfig → workflow_input_definition and WorkflowConfig → workflow_output_definition entity relations, a decision (D-SF1-30) declaring workflow-level I/O, entities with name/default fields, and a validation step (_check_workflow_io_refs) — none of which exist in the PRD's closed root field set or the Plan's WorkflowConfig model. These should be removed or reconciled. (stale design concept)
- [gap/medium] Design specifies that cross-phase edges found in PhaseDefinition.edges must produce a specific error directing the user to move them to WorkflowConfig.edges. No corresponding error code or validation check exists in the Plan's 29-code list. (missing validation)
**security** [FAIL]: The SF-1 Declarative Schema artifacts have multiple security gaps. The expression sandbox contract (REQ-21) is only partially implemented — the plan checks character length but defines none of the required contract constants (AST allowlist, blocked builtins, complexity bounds, timeout, scope contexts) that SF-2 needs to consume. YAML loading has no document-size or alias-expansion limits, leaving it vulnerable to billion-laughs DoS. Recursive PhaseDefinition.children has no depth bound, risking stack overflow. Free-text fields (name, prompt, condition, transform_fn, identity, channel) and arbitrary dicts (metadata, config, schema_def) have no max_length or size constraints in Pydantic models. The expression character limit value is inconsistent between PRD AC-17 (10,000) and SF-2 (2,000), and the plan doesn't specify which to use. No task verifies auth enforcement on /api/schema/workflow despite the PRD security profile requiring it.

- [high] Expression sandbox contract (REQ-21) is not formalized as importable constants/configuration. Plan STEP-3 only checks expression character length but doesn't define AST_ALLOWLIST, BLOCKED_BUILTINS, MAX_AST_NODES, EXPRESSION_TIMEOUT, or SCOPE_CONTEXTS. SF-2 needs to consume these from SF-1 to implement the 'formal contract' REQ-21 describes. Without them, the contract is documentation-only and cannot be programmatically enforced or tested. (iriai-compose/iriai_compose/schema/validation.py)
- [high] No YAML document size limit or alias expansion protection. load_workflow() calls yaml.safe_load() which prevents arbitrary object instantiation but does NOT protect against YAML bombs (billion laughs via anchor/alias expansion). A crafted YAML file with nested aliases can expand to gigabytes of memory. Plan STEP-3 yaml_io.py has no max file size check, no alias count limit, and no expansion depth limit before parsing. (iriai-compose/iriai_compose/schema/yaml_io.py)
- [high] No maximum recursion depth on PhaseDefinition.children. The recursive _walk_phases() helper in validation.py and Pydantic's model_validate() will both recurse without bound on deeply nested documents. Python's default recursion limit is ~1000 frames, and a document with >500 nesting levels could crash the process. Plan should define MAX_PHASE_NESTING_DEPTH and validate it. (iriai-compose/iriai_compose/schema/phases.py)
- [medium] Expression character limit inconsistency between PRD and SF-2. PRD AC-17 tests rejection at 10,000 characters. SF-2 PRD summary REQ-33 specifies 2,000 character limit plus 200 AST node limit. Plan STEP-3 instruction 7 says 'expression-length checks' but never specifies the actual limit value. This means the implementer must guess, and the schema-level check may disagree with SF-2 runtime enforcement. (iriai-compose/iriai_compose/schema/validation.py)
- [medium] No max_length constraints on free-text Pydantic string fields. WorkflowConfig.name, WorkflowConfig.description, PhaseDefinition.name, AskNode.prompt/task/context_text/summary, HumanActorDef.identity, HumanActorDef.channel, EdgeDefinition.transform_fn, BranchNode.condition (for output_field type), RoleDefinition.prompt — none have Field(max_length=...) constraints. A workflow YAML with a 1GB prompt field would pass Pydantic validation. (iriai-compose/iriai_compose/schema/nodes.py)
- [medium] schema_def dict has no content validation. PortDefinition.schema_def accepts arbitrary dicts. JSON Schema allows $ref with external HTTP URLs — a schema_def containing '$ref: https://evil.com/schema.json' could cause SSRF when consumed by downstream validators. No validation for external references, recursive $ref loops, or maximum schema size/depth. (iriai-compose/iriai_compose/schema/base.py)
- [medium] No size bounds on arbitrary dict fields: WorkflowConfig.metadata, PhaseDefinition.metadata, PluginNode.config, PluginInterface.config_schema, TemplateDefinition.bind, and TypeDefinition.schema_def. These accept dicts of unlimited depth and size. Combined with no YAML document size limit, this enables memory exhaustion via technically-valid documents. (iriai-compose/iriai_compose/schema/workflow.py)
- [medium] transform_fn field has no length limit or syntax validation at schema level for non-hook edges. Plan STEP-3 validates that hook edges don't have transform_fn, but data edges can carry arbitrarily long Python code strings with no bounds. While SF-2 performs AST validation at runtime, the schema contract should bound the input size to prevent deserialization cost. (iriai-compose/iriai_compose/schema/edges.py)
- [medium] No collection size limits. The plan defines no maximum for: number of phases (WorkflowConfig.phases), nodes per phase (PhaseDefinition.nodes), edges per phase/workflow, actors, types, plugins, or templates. A valid-but-enormous workflow with 100K nodes and 500K edges would pass all validation checks while causing severe performance degradation in validation, JSON Schema generation, and downstream consumers. (iriai-compose/iriai_compose/schema/workflow.py)
- [low] /api/schema/workflow auth enforcement is not cross-referenced in any plan task. PRD security_profile says endpoint 'should inherit the backend authenticated API policy.' System design says 'Bearer (inherits backend auth policy).' Plan creates generate_json_schema() and notes it's 'for build/test use only' but has no acceptance criterion or cross-reference verifying the downstream SVC-7 endpoint enforces authentication. If SF-5 misses this, the schema endpoint could be public. (iriai-compose/iriai_compose/schema/json_schema.py)
- [low] No credential/secret detection warning in validation. PRD third_party_exposure says YAML 'should expose workflow structure and types only; it should not require embedded credentials or secrets.' REQ-2 says HumanActorDef should not embed 'environment-specific credentials.' But validation.py has no check (even a warning-level one) for patterns that look like credentials (API keys, bearer tokens, connection strings) in prompt, identity, channel, config, or metadata fields. (iriai-compose/iriai_compose/schema/validation.py)
- [gap/high] REQ-21 requires a formal expression sandbox contract defining AST allowlist, blocked builtins, bounded complexity, timeout, and scope contexts. The plan only implements character-length checking. No importable constants (e.g., SAFE_AST_NODES, BLOCKED_BUILTINS, MAX_AST_COMPLEXITY, EXPRESSION_TIMEOUT_SECONDS, ALLOWED_SCOPE_KEYS) are created for SF-2 to consume. This means the 'formal contract' is purely documentation, not code. (Expression Security Contract)
- [gap/high] No YAML document size limit before parsing. yaml.safe_load() does not protect against alias-expansion bombs. Plan STEP-3 yaml_io.py should check file/string size before parsing and configure PyYAML with alias expansion limits if available, or use a wrapper that limits expansion. (Input Size Bounds)
- [gap/high] No maximum nesting depth for PhaseDefinition.children. _walk_phases() recurses without bound. Should define MAX_NESTING_DEPTH constant and validate during load/validate. Both Pydantic model_validate and the recursive walker need protection. (Recursion Safety)
- [gap/medium] No max_length on any string field in Pydantic models (name, description, prompt, condition, transform_fn, identity, channel, etc.). Pydantic v2 Field(max_length=...) is the standard mechanism. Without it, arbitrarily large strings pass model validation. (Field Size Validation)
- [gap/medium] PortDefinition.schema_def accepts arbitrary dicts without validating for dangerous JSON Schema constructs ($ref with external URLs, recursive $ref, extreme depth). Should validate schema_def at minimum for external references and maximum depth/size. (Schema Definition Safety)
- [gap/medium] PRD AC-17 tests expression rejection at 10,000 characters. SF-2 REQ-33 specifies a 2,000 character limit. Plan STEP-3 says 'expression-length checks' but never specifies the actual constant value. This creates ambiguity for the implementer and potential inconsistency between schema-level and runtime validation. (Expression Limit Value)
- [gap/medium] No maximum element count on any collection field (phases, nodes, edges, actors, types, plugins, templates, children, paths). Should define reasonable upper bounds (e.g., max_length on Pydantic list/dict fields) to prevent resource exhaustion from enormous but structurally valid documents. (Collection Size Bounds)
- [gap/low] PRD security profile requires /api/schema/workflow to be authenticated. Plan has no task, acceptance criterion, or explicit cross-reference ensuring SF-5 applies auth middleware to this endpoint. The json_schema.py module docstring mentions the endpoint but not its auth requirement. (Auth Verification Cross-Reference)

### libraries-registries [FAIL]

**completeness** [FAIL]: SF-7 artifacts contain multiple serious inter-artifact contradictions: actor_slots is a JSONB column in PRD/Plan but a separate table in System Design; the task-template API path is `/api/task-templates/` in Plan but `/api/templates/` in System Design; tool source enum values differ across artifacts; three design components have no plan implementation; the system design uses journey IDs (J-23, J-24, J-25, J-27, etc.) that don't exist in the PRD; and the repo path is `tools/compose/` in SF-7 but `platform/compose/` in SF-5's plan. These inconsistencies would cause implementation conflicts if not resolved.

- [critical] actor_slots storage model contradicts across artifacts: PRD says 'actor_slots must be a JSON array of unique slot definitions' (JSONB column on custom_task_templates); Plan STEP-1 says 'actor_slots JSONB NOT NULL DEFAULT []' column; but System Design defines actor_slots as a SEPARATE TABLE with fields (id, template_id, slot_key, default_role_id, description, user_id, created_at, updated_at) and per-slot CRUD endpoints (GET/PUT/DELETE /api/templates/{id}/actor-slots/{slot_key}). These are fundamentally different persistence models that cannot both be implemented. (system-design)
- [critical] Task template API path inconsistency: Plan STEP-2 explicitly says 'Use /api/task-templates not /api/templates' and Plan STEP-6 says 'Build the task-template list/editor against /api/task-templates'. But System Design uses /api/templates/{id} throughout (DELETE, actor-slots endpoints, check-name). The references endpoint also differs: Plan says GET /api/task-templates/references/{id}, System Design says entity is 'templates'. This would cause routing failures. (system-design api_endpoints)
- [critical] System Design uses journey IDs (J-5, J-16, J-18, J-20, J-23, J-24, J-25, J-27, J-28, J-29) in services, connections, entities, and call paths that do not exist in the PRD. The PRD defines only J-1 through J-5. Call paths CP-1→J-23, CP-2→J-27, CP-3→J-29, CP-6→J-25 all reference non-existent journeys, making the system design untrackable to PRD requirements. (system-design)
- [high] workflow_entity_refs schema three-way disagreement. PRD data entity: unique on (workflow_id, entity_type, entity_id), no phase_id or node_id fields. Plan STEP-1: adds phase_id VARCHAR(255) and node_id VARCHAR(255) columns, unique on (workflow_id, entity_type, entity_id, node_id). System Design entity: only (id, workflow_id, entity_type, entity_id, user_id, created_at) — no phase_id, no node_id. Implementers cannot determine the correct schema. (plan STEP-1)
- [high] Tool table source enum values differ: Plan STEP-1 specifies source as ('builtin'|'custom'), System Design specifies CHECK IN ('mcp', 'custom_function'). The System Design also says built-in tools have NO DB rows, making 'builtin' an invalid DB value — contradicting the Plan. (plan STEP-1)
- [high] Tool soft-delete mechanism contradicts: Plan STEP-1 uses 'is_deleted BOOL DEFAULT FALSE'. PRD data entity lists 'deleted_at' field. System Design tool entity uses 'deleted_at TIMESTAMP nullable' with partial unique index on deleted_at IS NULL. Plan and PRD/System Design use incompatible deletion models. (plan STEP-1)
- [high] Repository path mismatch with SF-5 foundation: All SF-7 artifacts use 'tools/compose/backend' and 'tools/compose/frontend'. But SF-5's plan summary states D-A3: 'Repo topology — platform/compose/backend, platform/compose/frontend, platform/toolshub/frontend (not tools/)'. SF-6's plan summary also references 'platform/compose/backend/'. SF-7 code would land in the wrong directory. (plan file_manifest)
- [high] Three design components have no corresponding plan file: CMP-134 LibraryCollectionPage.tsx (features/libraries/shared/LibraryCollectionPage.tsx), CMP-137 ActorSlotsEditor.tsx (features/libraries/templates/ActorSlotsEditor.tsx), and CMP-138 ResourceStateCard.tsx (features/libraries/shared/ResourceStateCard.tsx) are all absent from the plan's file_manifest and files_to_create. (design component_defs)
- [high] Design component filenames contradict plan filenames: CMP-135 is 'RoleEditorForm.tsx' in design but 'RoleEditorView.tsx' in plan. CMP-136 is 'ToolEditorForm.tsx' in design but 'ToolEditorView.tsx' in plan. Implementers would create misnamed files. (design component_defs CMP-135, CMP-136)
- [high] System Design CP-5 (tool delete) skips the preflight GET step, going directly to DELETE /api/tools/{id} at sequence 1. But the design's EntityDeleteDialog (CMP-133) is described as 'Shared alert dialog for delete preflight and confirmation across roles, schemas, templates, and tools' and always starts in 'reference-check state'. CP-3 (role delete) correctly shows a preflight GET. The tool delete call path contradicts the shared component design. (system-design call_paths CP-5)
- [high] Plan STEP-5 says 'treat custom tool names as immutable after creation' but System Design PUT /api/tools/{id} explicitly allows name changes: 'If the name changes, the UI warns that roles using the old string must be updated manually' and the response includes a 'renamed' field. Mutability of tool names is unresolved between plan and system design. (plan STEP-5 vs system-design PUT /api/tools/{id})
- [high] SF-6 depends on SF-7 providing 9 visual primitive components (AskNodePrimitive CMP-102, BranchNodePrimitive CMP-103, PluginNodePrimitive CMP-104, NodePortDot CMP-105, EdgeTypeLabel CMP-106, ActorSlot CMP-81, PhaseBadge, InlineCodeEditor, TaskTemplateThumbnail CMP-84) per D-58 ownership rule. SF-7's PRD, design, and plan contain zero references to any of these primitives. SF-6 would be blocked on SF-7 for these components. (plan (missing scope))
- [medium] PRD REQ-6 (500ms warm-cache, 2s cold-load performance targets) has no dedicated plan step. STEP-3 is incorrectly mapped to REQ-6 but only covers auth/validation middleware — not performance. No step implements stale-while-revalidate query configuration, measures page load time, or sets up performance testing. (plan STEP-3 requirement_ids)
- [medium] PRD journey J-5 (Reject Invalid Actor Slot Definitions) has no verification blocks in the plan's journey_verifications. The plan only covers J-1 through J-4. J-5's validation failure path (duplicate slot names, invalid bindings, server-side rejection) is untested. (plan journey_verifications)
- [medium] System Design includes PromotionDialog in the libraries-feature service description and has a complete call path CP-2 'Promote Inline Role to Library' with idempotent POST /api/roles { promote: true }. But the PRD has no promotion requirement, no promotion journey, and no promotion acceptance criterion. SF-6's plan summary also expects promotion endpoints. This is either undocumented scope or scope creep. (system-design CP-2)
- [medium] Payload size limit contradiction: PRD REQ-8 citation says '256KB JSON payload size limits', but Plan STEP-3 instructions say 'max 512 KB' for library POST/PUT payloads. System Design tool endpoint says 'input_schema max 256KB (REQ-8)'. The limit should be consistent. (plan STEP-3)
- [medium] Plan STEP-1 specifies user_id as UUID type for both tools and workflow_entity_refs tables. But System Design uses VARCHAR for user_id across all entities, and SF-5's existing tables (workflows, roles, etc.) all use VARCHAR/String for user_id. The Plan's UUID type would cause FK incompatibilities with the existing schema. (plan STEP-1)
- [medium] Tool name VARCHAR length differs: Plan STEP-1 says 'name VARCHAR(255) UNIQUE per user', System Design says 'name VARCHAR(200)'. These would create different column constraints. (plan STEP-1 vs system-design tool entity)
- [medium] Plan STEP-5 incorrectly maps to REQ-5 (actor_slots persistence). STEP-5 implements Roles and Tools library flows; actor_slots is implemented in STEP-6. REQ-5 should map to STEP-1 (migration) and STEP-6 (editor), not STEP-5. (plan STEP-5 requirement_ids)
- [medium] Plan STEP-1 requirement_ids lists REQ-1, REQ-3, REQ-4 but omits REQ-2 (workflow_entity_refs) and REQ-5 (actor_slots) even though STEP-1 creates both the workflow_entity_refs table and the actor_slots column. Missing requirement traceability. (plan STEP-1 requirement_ids)
- [medium] Plan testid_registry includes 'entity-delete-dialog-validation-codes' but the Design explicitly states in J-2 not_criteria: 'The blocked state must NOT show validation-code chips or plugin-specific warnings outside the current SF-7 scope.' A testid for a feature that must not exist creates confusion. (plan testid_registry)
- [medium] System Design hook mechanism (D-SF7-3, D-SF7-6) describes SF-5 REQ-18 post-commit mutation hooks with typed events and a separate SF-7 transaction after SF-5 commits. But the Plan STEP-2 says to 'call the service function in-transaction' within the caller's (SF-5's) transaction. These are fundamentally different: post-commit separate transaction vs. in-transaction call. RISK-1 in the system design acknowledges the post-commit callback can fail after SF-5 commits. (plan STEP-2 vs system-design D-SF7-6)
- [medium] Design D-SF7-4 says 'Sidebar contains exactly 5 entity-type folders: Workflows, Roles, Schemas, Templates, Tools'. Plan STEP-4 acceptance criteria says 'existing top-level sidebar still shows only Workflows, Roles, Output Schemas, and Task Templates, while the Tools view is reachable without restoring a fifth SF-5 foundation folder'. Plan STEP-4 counterexample says 'Do not restore a top-level Tools folder in the SF-5 shell'. The design wants 5 sidebar folders; the plan explicitly avoids a 5th folder. (plan STEP-4 vs system-design D-SF7-4)
- [low] System Design mentions 'homelocal-auth' as compose-backend technology but this dependency is not referenced in the PRD, Design, or Plan. May be a stale or undocumented dependency. (system-design compose-backend)
- [low] System Design includes JSON Schema Draft 2020-12 validation for output schemas ('JSON Schema server-side validation (Draft 2020-12)' in compose-backend, and POST /api/schemas validates against Draft 2020-12). This validation requirement doesn't appear in the SF-7 PRD. (system-design compose-backend)
- [gap/critical] SF-6 expects SF-7 to provide 9 primitive visual components (AskNodePrimitive, BranchNodePrimitive, PluginNodePrimitive, NodePortDot, EdgeTypeLabel, ActorSlot, PhaseBadge, InlineCodeEditor, TaskTemplateThumbnail) per D-58 ownership rule. None appear in any SF-7 artifact. This is either missing scope or a cross-subfeature contract error. (missing-implementation)
- [gap/medium] PRD REQ-6 (performance: 500ms warm-cache, 2s cold-load) has no plan step that configures stale-while-revalidate behavior, sets TanStack Query staleTime/cacheTime, or validates performance targets. STEP-4 mentions staleTime:0 for delete preflight but not list-page caching. (missing-plan-step)
- [gap/medium] PRD journey J-5 (Reject Invalid Actor Slot Definitions) has no journey_verification entry in the plan. No test data-testids or verify_blocks exist for the invalid actor-slot validation flow. (missing-verification)
- [gap/medium] Design component CMP-134 LibraryCollectionPage (shared list shell for all 4 entity types) is not in the plan's file manifest. The individual list pages (RolesListPage, ToolsListPage, etc.) presumably need this shared component but it was never planned for creation. (missing-plan-file)
- [gap/medium] Design component CMP-137 ActorSlotsEditor (structured repeater for actor_slots) is not in the plan's file manifest. Plan STEP-6 mentions 'actor-slot side panel' inline within TaskTemplateEditorView but the design specifies a standalone reusable component. (missing-plan-file)
- [gap/medium] Design component CMP-138 ResourceStateCard (loading/not-found/error/validation fallback) is not in the plan's file manifest. This component handles 404 cross-user access display (REQ-7) and validation error display (REQ-8) across all library routes. (missing-plan-file)
- [gap/medium] SF-6's plan expects promotion endpoints (POST /api/roles with promote flag, POST /api/schemas promotion, POST /api/templates save-template) from SF-7. System Design CP-2 details the flow. But SF-7's PRD has no promotion requirement, journey, or acceptance criterion. Either the PRD needs to add promotion scope or SF-6's dependency is unmet. (missing-scope)
- [gap/medium] AC-9 (warm-cache 500ms, cold-load 2s) has no plan step that implements performance measurement or optimization, no test infrastructure for timing assertions, and no file in the manifest for performance configuration. (unverifiable-ac)
**security** [FAIL]: Multiple security gaps found across SF-7 artifacts: rate limiting absent for all new endpoints, payload size limit contradictions (PRD 256KB vs Plan 512KB), endpoint path inconsistencies creating potential auth bypass surfaces (/api/templates vs /api/task-templates), no safe YAML loading specified for the reindex service that parses untrusted workflow content, actor_slots persistence model contradicts between PRD/Plan (JSONB column) and System Design (separate table) affecting validation strategy, tool name mutability contradiction between Plan (immutable) and System Design (allows rename), and missing implementation tasks for several PRD security requirements.

- [high] No rate limiting specified for any SF-7 endpoint. SF-5 provides slowapi infrastructure (100/min default, 30/min create, 10/min import) but no plan step configures rate limits for the 15+ new SF-7 endpoints (tools CRUD, references, actor-slots, check-name). An attacker could abuse GET /api/{entity}/references/{id} or POST /api/tools to exhaust database resources. STEP-3 covers auth and payload validation but never mentions rate limiting. (tools/compose/backend/app/routers/tools.py)
- [high] Endpoint path inconsistency creates auth/validation gap risk. The Plan (STEP-2) explicitly states 'Use /api/task-templates not /api/templates', but the System Design lists all template endpoints as /api/templates/{id} (DELETE, actor-slots GET/PUT/DELETE, check-name). If implementation follows the system design's paths while STEP-3's auth middleware is configured for /api/task-templates, the /api/templates routes would be unprotected. This affects 5 endpoints: DELETE /api/templates/{id}, GET/PUT/DELETE /api/templates/{id}/actor-slots/{slot_key}, GET /api/templates/check-name.
- [high] YAML parsing in reindex_workflow_refs does not specify safe loading. STEP-2 creates workflow_reference_index.py that 'parses the workflow's saved YAML to extract role, schema, and task-template IDs from node definitions.' No instruction to use yaml.safe_load() vs yaml.load(). YAML deserialization attacks (arbitrary code execution via Python object tags) are well-documented. Since this service processes saved workflow content that originates from user input, unsafe YAML loading is a code execution risk. (tools/compose/backend/app/services/workflow_reference_index.py)
- [high] Payload size limit contradicts between PRD and Plan. PRD REQ-8 citation states '256KB JSON payload size limits' and System Design specifies 'input_schema max 256KB (REQ-8)' on the Tool entity. But STEP-3 instructions say 'request-size middleware for library POST/PUT payloads (max 512 KB)'. A 512KB limit is 2x the PRD requirement, allowing larger attack payloads than intended. (tools/compose/backend/app/middleware/request_size.py)
- [medium] actor_slots persistence model is fundamentally contradictory between artifacts, affecting validation surface. PRD says 'actor_slots must be a JSON array of unique slot definitions' (JSONB column). Plan STEP-1 says 'actor_slots JSONB NOT NULL DEFAULT []' column on custom_task_templates. But System Design defines actor_slots as a SEPARATE TABLE (id, template_id, slot_key, default_role_id, description, user_id) with dedicated CRUD endpoints (GET/PUT/DELETE /api/templates/{id}/actor-slots/{slot_key}). A JSONB column has completely different validation, access control, and injection surfaces than a normalized table with FK constraints. Implementation cannot satisfy both. (tools/compose/backend/alembic/versions/0002_sf7_libraries_registry_extensions.py)
- [medium] Tool name mutability contradiction creates data integrity and potential reference hijacking risk. Plan STEP-5 says 'treat custom tool names as immutable after creation so existing Role.tools string references remain valid.' But System Design PUT /api/tools/{id} says 'If the name changes, the UI warns that roles using the old string must be updated manually' and response includes 'renamed' field. If renames are allowed, an attacker could: (1) create tool 'trusted_tool', (2) get roles to reference it, (3) rename to 'other', (4) create new 'trusted_tool' with malicious config. No server-side guard prevents this name-swapping attack. (tools/compose/backend/app/routers/tools.py)
- [medium] No protection against custom tool names shadowing built-in tool names. GET /api/tools merges 'hardcoded built-in tools (backend constants)' with 'user-registered custom tools.' Neither the plan nor system design prevents a user from registering a custom tool with a name matching a built-in (e.g., 'Read', 'Grep', 'Glob'). Role.tools stores string names, so a shadow tool could replace a built-in's behavior in the checklist. STEP-5's counterexample says 'Do not allow editing or deleting built-in tools' but says nothing about name collision prevention. (tools/compose/backend/app/routers/tools.py)
- [medium] No JSON Schema complexity limits for tool input_schema field. System Design specifies tool input_schema as 'nullable, max 256KB' JSON, which is stored and potentially used for validation. A malicious JSON Schema with deeply nested $ref chains, recursive definitions, or ReDoS-vulnerable patterns could cause denial of service when the schema is later processed. Plan STEP-3 only covers general payload size and entity-name sanitization, not structural validation of stored schemas. (tools/compose/backend/app/dependencies/library_validation.py)
- [medium] Post-commit hook failure silently leaves workflow_entity_refs stale, allowing improper deletion. System Design RISK-1 acknowledges this: 'SF-7's post-commit callback fails... the workflow save succeeds but the reference index is stale.' The plan's RISK-1 mitigation mentions 'a periodic reconciliation job can resync' but no plan step implements this job. A stale index means GET /api/{entity}/references/{id} returns 0 references when references actually exist, and DELETE would succeed with the re-check also hitting the stale index. No monitoring, alerting, or reconciliation task is scoped. (tools/compose/backend/app/services/workflow_reference_index.py)
- [medium] Delete race condition lacks database-level atomicity. Plan RISK-2 identifies that 'a zero-reference preflight result can go stale between GET preflight and DELETE mutation.' Mitigation says 'every DELETE handler re-check the same persisted reference service.' But neither the plan nor system design specifies using SELECT FOR UPDATE or any database lock during the re-check + soft-delete sequence. A concurrent workflow save could insert a new workflow_entity_refs row between the re-check SELECT and the UPDATE setting deleted_at, allowing deletion of a referenced entity. (tools/compose/backend/app/routers/roles.py)
- [medium] check-name endpoints exist in System Design but are absent from Plan. System Design defines GET /api/roles/check-name, GET /api/schemas/check-name, GET /api/templates/check-name, GET /api/tools/check-name (used in call paths CP-1, CP-2, CP-4). No plan step creates these endpoints. If they're needed but unimplemented, users bypass name uniqueness checks pre-save; if they're implemented ad-hoc without being in STEP-3's validation scope, they may lack auth/rate-limiting.
- [medium] Output schema json_schema field lacks server-side JSON Schema Draft 2020-12 validation in plan. System Design compose-backend description says 'JSON Schema server-side validation (Draft 2020-12)' and POST /api/schemas says 'Validates json_schema against JSON Schema Draft 2020-12 before persisting.' But no plan step (STEP-2 through STEP-6) includes instructions to validate the json_schema field. STEP-6 creates SchemaEditorView with 'dual-pane JSON editor' but only mentions routing deletes through EntityDeleteDialog. An invalid or malicious JSON Schema could be persisted. (tools/compose/backend/app/routers/schemas.py)
- [low] No audit logging specified for security-relevant library operations. PRD security profile mentions 'standard platform auth, tenancy isolation, and input-validation controls' but no plan step includes structured logging for delete attempts (blocked or successful), cross-user access attempts (404s), or payload validation rejections (413/422s). SF-5 provides structlog infrastructure with 'security events' but SF-7 never hooks into it. (tools/compose/backend/app/routers/references.py)
- [low] No Content-Type enforcement specified. Plan STEP-3 creates request_size.py middleware and library_validation.py but neither specifies rejecting non-application/json Content-Type on POST/PUT endpoints. Without this, content-type confusion attacks or multipart uploads that bypass JSON payload size limits could reach handlers. (tools/compose/backend/app/middleware/request_size.py)
- [low] Soft-delete filtering not enforced at query layer. Multiple entities use deleted_at soft-delete pattern but no plan step establishes a query-level filter (e.g., SQLAlchemy default filter or base query mixin) ensuring all queries exclude soft-deleted records. Each endpoint must remember to add 'WHERE deleted_at IS NULL'. A single missed filter would expose soft-deleted entities, violating the tenancy model since a 'deleted' role could still appear in reference checks or picker lists. (tools/compose/backend/app/models/tool.py)
- [gap/high] No rate limiting configuration for any of the 15+ new SF-7 API endpoints despite SF-5 providing slowapi infrastructure. All new endpoints (tools CRUD, references, actor-slots, check-name) are unthrottled. (rate-limiting)
- [gap/medium] Server-side JSON Schema Draft 2020-12 validation for output_schemas.json_schema field is specified in system design but has no corresponding plan step or instruction. (input-validation)
- [gap/medium] actor_slots validation rules (duplicate names, blank names, invalid default_role_id bindings) are described in design but no plan step implements server-side validation for actor_slots content structure. (input-validation)
- [gap/medium] workflow_entity_refs reconciliation job mentioned as RISK-1 mitigation but not implemented in any plan step. No mechanism exists to detect or recover from post-commit hook failures. (monitoring)
- [gap/medium] Four check-name endpoints (GET /api/{roles,schemas,templates,tools}/check-name) defined in system design and used in call paths but absent from all plan steps. (endpoint-coverage)
- [gap/low] No structured security event logging for delete attempts, cross-user 404s, validation rejections, or rate limit hits on SF-7 endpoints despite SF-5 providing structlog infrastructure. (audit-logging)

### testing-framework [FAIL]

**completeness** [FAIL]: SF-3 testing-framework artifacts have strong internal coherence on the core ABI-alignment narrative but contain numerous cross-artifact inconsistencies, missing implementation coverage for design-defined components, mismatched type signatures, and unresolved contradictions with SF-2's published data contract. The most critical blocker is that MockPluginRuntime (CMP-3) is fully designed but has zero plan steps or file manifest entries. Additional blocking issues include ContextVar type/name disagreement between plan and system design, RuntimeConfig field mismatches with SF-2, and the plan's incomplete coverage of the 4-strategy mock resolution model that the system design specifies.

- [high] MockPluginRuntime (CMP-3) is fully defined in Design and System Design (ENT-MockPluginRuntime with when_ref(), default_response(), calls history) at location iriai_compose/testing/mock_plugin.py, but NO plan step creates, modifies, or even mentions mock_plugin.py. The file is absent from the plan's file_manifest and files_to_create lists. This component has no implementation path. (plan.md)
- [high] ContextVar type and name mismatch between Plan and System Design. Plan's verified contract defines `current_node_var: ContextVar[str]` with default `""`. System Design defines `_current_node: ContextVar[str | None]` with default `None`. These are different names, different types, and different defaults — implementers will hit an ImportError or type mismatch depending on which artifact they follow. (plan.md)
- [high] PRD REQ-4 explicitly states 'The Architect must REMOVE plan decision D-SF3-16'. PRD AC-5 verifies 'Plan decision D-SF3-16 has been removed'. However, the Plan REDEFINED D-SF3-16 with new content (ContextVar-based propagation) instead of removing it. The ID still exists, which means AC-5 would fail as written. (plan.md)
- [high] RuntimeConfig field `agent_runtime` (singular) in SF-3 plan's verified contract contradicts SF-2 PRD summary which uses `agent_runtimes` (plural) to support multi-provider agent routing (REQ-20). If SF-2 ships with plural, SF-3's run_test() wrapper and all mock configuration will fail. (plan.md)
- [high] Plan STEP-2 only implements a 3-priority mock resolution (node > role > default) while the System Design specifies a 4-strategy model (node > role+prompt > role > default). STEP-2 instructions define when_role(role_name: str) with no prompt parameter, but System Design defines when_role(name: str, *, prompt: str | None = None) which creates Strategy 2 (role+prompt) when prompt is present. The entire role+prompt matching strategy is missing from implementation instructions. (plan.md)
- [high] Plan STEP-2 only mentions .respond(value) as a terminal method on matcher builders. System Design defines 6 additional terminals: respond_sequence(), respond_with(), raise_error(), then_crash(), with_cost(), on_call(). These are extensively used in System Design call paths (CP-SF3-3 uses respond_sequence, CP-SF3-5 uses respond_with) but have no implementation instructions in any plan step. (plan.md)
- [high] SF-2 PRD summary lists ExecutionResult fields `branch_paths_taken` and `final_output`, but SF-3 plan's verified contract uses `branch_paths` and `workflow_output`. The SF-2 plan summary uses `branch_paths` and `workflow_output`, contradicting its own PRD. SF-3 implementers must know which field name SF-2 will actually ship — if SF-2 ships `branch_paths_taken`, all SF-3 assertion code breaks. (plan.md)
- [medium] MockInteractionRuntime has contradictory constructor APIs across artifacts. Plan STEP-2 acceptance criteria says 'MockInteraction(approve=False).resolve(Pending(...))' — using constructor arguments. System Design ENT-MockInteractionRuntime specifies '__init__()' as no-arg only, with 'when_node()' fluent API for configuration. Design CMP-2 also says no-arg constructor. Plan STEP-2 has both patterns coexisting, which is self-contradictory. (plan.md)
- [medium] run_test() signature mismatch between Plan and System Design. Plan STEP-6: `run_test(workflow, *, runtime=None, interaction=None, plugins=None, inputs=None, feature_id='test')`. System Design API: `run_test(workflow, *, runtime=None, interaction=None, plugin=None, artifacts=None, plugins=None, inputs=None, feature_id=None)`. System Design adds `plugin` and `artifacts` parameters, and `feature_id` defaults differ ('test' vs None). (plan.md)
- [medium] System Design references journey IDs J-7, J-8, J-9, J-10 throughout services, connections, entities, and call paths. The PRD only defines J-1 and J-2. These four journey IDs are phantom references with no PRD backing — the system design invented journeys that don't exist in the product specification. (design-decisions.md)
- [medium] System Design defines assert_loop_iterations(result, phase_id, expected_count) as a published API endpoint accessing result.history.loop_progress[phase_id].completed_iterations. This assertion helper is not implemented in any plan step — STEP-5 covers assert_node_reached, assert_artifact, assert_branch_taken, assert_node_count, assert_phase_executed but not assert_loop_iterations. (plan.md)
- [medium] SF-2 PRD summary lists loop_iterations, fold_progress, map_fan_out as TOP-LEVEL ExecutionResult fields. SF-3 system design puts them inside ExecutionHistory (accessed via result.history.loop_progress). The plan's verified ExecutionResult contract does not include these as top-level fields. If SF-2 ships them as top-level fields, SF-3's assertion helpers targeting history.loop_progress will fail. (plan.md)
- [medium] Design CMP-1 (MockAgentRuntime) and CMP-2 (MockInteractionRuntime) both list location as 'iriai_compose/runner.py' — the production runner module. Plan creates them at 'iriai_compose/testing/mock_runtime.py'. The design locations are wrong and could mislead implementers. (design-decisions.md)
- [medium] SF-2 PRD REQ-35 defines AgentRuntime.invoke_tracked() returning InvocationResult with cost fields, suggesting an ABC extension. SF-3's MockAgentRuntime only implements invoke(). If invoke_tracked() is added to the ABC in SF-2, MockAgentRuntime will need to implement it too, but SF-3 artifacts are completely silent on this method. (plan.md)
- [medium] Plan STEP-2 uses private class names _WhenNodeClause and _WhenRoleClause with __slots__. System Design uses public entity names NodeMatcher and RoleMatcher. The naming divergence means implementers following the plan will produce code that doesn't match the system design's entity definitions and documented API surface. (plan.md)
- [medium] PRD data entity MockCall lists fields: node_id, role, prompt, output_type, response, cost, timestamp. Plan STEP-2 records: node_id, role, prompt, output_type, workspace, session_key, matched. Missing from plan: response, cost, timestamp. Added in plan: workspace, session_key, matched. The call record schemas don't match. (plan.md)
- [medium] SF-2 PRD summary's RuntimeConfig includes checkpoint and resume fields. SF-3 plan's verified RuntimeConfig contract explicitly excludes them ('NOTE: no history field'). But SF-2 plan summary mentions resume on RuntimeConfig. If SF-2 ships with checkpoint/resume on RuntimeConfig, SF-3's run_test() construction of RuntimeConfig without these fields could either error or silently lose functionality. (plan.md)
- [low] PRD data entities mark MockAgentRuntime and MockCall as 'is_new: false', implying they are existing entities being modified. But the plan creates them from scratch in new files under testing/. This metadata is misleading. (prd.md)
- [low] PRD open question 'Should the enhanced MockAgentRuntime extend the existing test MockAgentRuntime from iriai-compose/tests/conftest.py, or remain a fresh implementation?' is unresolved in the PRD but the plan implements it as a fresh implementation. Open questions should be resolved before planning to avoid implementation churn. (prd.md)
- [low] System Design defines cost_summary on ExecutionResult as 'dict[str, Any]' but SF-2 PRD defines CostSummary as a dedicated dataclass with total_input_tokens, total_output_tokens, total_cost_usd, per_node fields. The type in SF-3's verified contract is weaker than SF-2's actual type. (plan.md)
- [low] Plan STEP-4 maps to REQ-3 and REQ-5, but REQ-5 is about execution-path assertions using SF-2 observability (ExecutionResult/ExecutionHistory). STEP-4 implements SF-1 validation re-exports and assert_validation_error() — these are schema validation, not execution assertions. The requirement mapping is incorrect. (plan.md)
- [low] No assertion helpers exist for error routing (errors_routed field on ExecutionResult) in any plan step or system design API. SF-4 migration tests need error-port assertions per the SF-4 PRD summary. SF-3 should provide assert_error_routed() or similar. (plan.md)
- [low] Design journey_annotations reference only J-2, but the PRD defines both J-1 and J-2. J-1 (the happy-path node-aware test journey) has no design journey annotation, meaning the design doesn't provide UX/DX guidance for the primary happy-path journey. (design-decisions.md)
- [gap/high] MockPluginRuntime (CMP-3, ENT-MockPluginRuntime) is fully specified in Design and System Design with when_ref(), default_response(), respond/respond_sequence/raise_error/then_crash/with_cost terminals, and call recording. No plan step creates mock_plugin.py, and the file is absent from file_manifest. (missing_implementation)
- [gap/high] respond_sequence(), respond_with(), raise_error(), then_crash(), with_cost() terminal methods are defined in System Design on both NodeMatcher and RoleMatcher entities but have no implementation instructions in any plan step. Plan STEP-2 only describes .respond(value). (missing_implementation)
- [gap/medium] assert_loop_iterations() is defined as a System Design API endpoint but is not implemented in any plan step. STEP-5 implements assert_node_reached, assert_artifact, assert_branch_taken, assert_node_count, assert_phase_executed — but not assert_loop_iterations. (missing_implementation)
- [gap/medium] PRD REQ-2 (hierarchical context merge order workflow->phase->actor->node) is not mapped to any plan step's requirement_ids. The plan discusses it in architecture text but no step is accountable for verifying or implementing merge-order-aware behavior. (missing_requirement_coverage)
- [gap/low] PRD REQ-6 (no wrapper/adapter carrying node identity other than ContextVar) is not mapped to any plan step's requirement_ids. It is implicitly addressed by STEP-2 but no step is explicitly accountable. (missing_requirement_coverage)
- [gap/medium] No assertion helper for errors_routed (ErrorRoute list on ExecutionResult). SF-4 migration needs error-port assertions. Neither plan nor system design defines assert_error_routed() or equivalent. (missing_assertion)
- [gap/low] PRD open question about whether MockAgentRuntime should extend existing conftest.py mock or be fresh implementation was never formally resolved before plan was written. Plan assumes fresh implementation without documenting the decision. (unresolved_open_question)
- [gap/medium] No plan step tests respond_sequence exhaustion (MockExhaustedError), respond_with dynamic handlers, raise_error injection, then_crash simulation, or with_cost metadata attachment — all of which are core System Design behaviors used in call paths CP-SF3-3 and CP-SF3-5. (missing_test_coverage)
- [gap/high] SF-2 PRD specifies agent_runtimes (plural, dict for multi-provider routing per REQ-20) while SF-3's entire artifact set assumes agent_runtime (singular). This fundamental RuntimeConfig shape difference is not flagged as a risk anywhere in SF-3 artifacts. (cross_sf_contract_drift)
**security** [PASS]: SF-3 is a testing-only Python library with an explicitly minimal security surface (no HTTP endpoints, no database, no auth, no PII). The PRD security profile correctly scopes this as low-risk. However, several gaps exist between the PRD's risk-mitigation requirements and the plan's implementation: the PRD mandates D-SF3-16 removal (REQ-4/AC-5) but the plan reuses the ID with corrected content instead of removing it; the PRD requires a pre-implementation verification gate that no plan step implements; and the design's MockPluginRuntime (CMP-3) has no corresponding plan step. The YAML handling in STEP-7 correctly specifies yaml.safe_load() but doesn't verify upstream SF-1 loader safety. Overall approved because the security profile is inherently minimal for a testing library and the core ABI-widening risk is well-mitigated through STEP-8 regression tests.

- [medium] PRD REQ-4 and AC-5 explicitly require D-SF3-16 to be REMOVED ('Plan decision D-SF3-16 has been removed'), but the plan reuses the D-SF3-16 identifier with corrected content instead of removing it and assigning a new ID. The PRD risk_mitigation_notes identify D-SF3-16 as 'the primary non-compliance risk' — reusing the same ID creates audit confusion and makes it harder to verify compliance during review. An auditor checking 'is D-SF3-16 gone?' will find it still present. (plan (D-SF3-16 decision log entry))
- [medium] PRD risk_mitigation_notes state: 'D-SF3-16 is the primary non-compliance risk; its removal must be verified before any SF-3 implementation file is written.' No plan step implements this pre-implementation verification gate. STEP-1 jumps directly to scaffolding without a verification checkpoint. The plan should include a STEP-0 or pre-condition that explicitly verifies the stale ABC block and D-SF3-16 are resolved before any code is written. (plan (missing verification gate step))
- [low] STEP-7 assert_yaml_round_trip(path) accepts a filesystem path with no validation or sanitization. While this is a testing utility unlikely to receive untrusted input, path traversal is possible if the function is misused in CI/CD pipelines or automated test generation. The plan should note that path should be validated to exist within the project tree. (iriai-compose/iriai_compose/testing/snapshot.py)
- [low] STEP-7 calls load_workflow() from SF-1 for YAML round-trip testing but the plan does not verify that SF-1's YAML loader uses yaml.safe_load() (not yaml.load() or yaml.unsafe_load()). If SF-1 uses unsafe YAML loading, the testing surface inherits that vulnerability. The plan's own yaml.safe_load() call is correct, but the delegated load_workflow() call is unverified. (iriai-compose/iriai_compose/testing/snapshot.py)
- [low] MockAgentRuntime.calls records all prompts verbatim in memory (calls list contains prompt text). The PRD security profile says 'No PII expected; mocks and fixtures use synthetic inputs' but provides no enforcement mechanism. If a developer accidentally passes real credentials, API keys, or PII through mock invocations during testing, these are stored unredacted in the call log. No scrubbing or warning mechanism is specified. (iriai-compose/iriai_compose/testing/mock_runtime.py)
- [low] The design specifies MockPluginRuntime (CMP-3) at iriai_compose/testing/mock_plugin.py with node-aware observability, but no plan step creates or implements this file. The file is absent from the plan's file_manifest and files_to_create lists. If plugin mocking is needed before this gap is addressed, developers may create ad-hoc unsafe implementations outside the controlled testing surface. (iriai-compose/iriai_compose/testing/mock_plugin.py)
- [gap/medium] PRD REQ-4 requires removal of D-SF3-16 and the stale ABC block. The plan rewrites D-SF3-16 content but keeps the ID, violating the literal requirement 'The Architect must remove plan decision D-SF3-16.' AC-5 verification criterion states 'Plan decision D-SF3-16 has been removed' — this criterion cannot pass when D-SF3-16 still exists in the decision log. (PRD compliance)
- [gap/medium] PRD risk_mitigation_notes require verification that D-SF3-16 and stale ABC are resolved 'before any SF-3 implementation file is written.' No plan step implements this gate. STEP-1 proceeds directly to scaffolding. A pre-implementation verification step is missing. (Security gate)
- [gap/medium] Design CMP-3 (MockPluginRuntime at iriai_compose/testing/mock_plugin.py) and system design ENT-MockPluginRuntime are fully specified but have no corresponding plan implementation step. The plan's run_test() accepts a plugins= parameter but never creates MockPluginRuntime. This leaves the plugin mocking surface unimplemented. (Design-plan alignment)
- [gap/low] assert_yaml_round_trip(path) and load_workflow(path) in STEP-7 accept file paths without validation that they reside within the project boundary. No path traversal guard is specified. (Input validation)
- [gap/low] STEP-7 delegates to SF-1's load_workflow() for YAML parsing but does not specify verification that the upstream function uses safe YAML deserialization. The plan only ensures its own yaml.safe_load() usage. (Dependency security verification)

### workflow-editor [FAIL]

**completeness** [FAIL]: SF-6 artifacts contain multiple critical internal contradictions and cross-artifact alignment failures. The most severe: (1) Plan STEP-9 creates SwitchFunctionEditor.tsx which the design explicitly prohibits ("must not be created"); (2) The design and system design define irreconcilable BranchNode models — design uses D-GR-12 per-port conditions with non-exclusive fan-out while system design D-SF6-9 mandates node-level exclusive routing and forbids per-port predicates; (3) Plan says "view-only" on schema failure while PRD/design both require "strictly blocking with no view-only fallback"; (4) Plan journey IDs (J-16/J-17/J-18/J-20/J-21/J-22) reference journeys that don't exist in the PRD (J-1 through J-6 only); (5) Three design-defined components (SchemaBootstrapGate, MergeFunctionEditor, PortConditionRow) are absent from the plan's file manifest and step scopes; (6) Plan references phantom REQ-15 which doesn't exist in the PRD. Additionally, a repo topology mismatch exists between SF-6 (tools/compose/) and SF-5/SF-7 plan summaries (platform/compose/).

- [critical] Plan STEP-9 scope includes creating SwitchFunctionEditor.tsx, and the file_manifest/files_to_create list it. The design explicitly says 'SwitchFunctionEditor does not exist and must not be created' and lists it under REMOVED components. The plan also doesn't mention SwitchFunctionEditor in STEP-9's instructions, suggesting the file entry is contamination from a prior revision. (tools/compose/frontend/src/features/editor/inspectors/SwitchFunctionEditor.tsx)
- [critical] Irreconcilable BranchNode model contradiction within SF-6. Design CMP-36 says 'each carrying D-GR-12 per-port condition data for non-exclusive fan-out' and defines OutputPathsEditor (CMP-64) + PortConditionRow (CMP-66) for per-port condition authoring. System design D-SF6-9 says 'Exactly one path fires at runtime. switch_function, dual-routing, and per-port branch predicates are forbidden' with node-level condition_type and condition. These are fundamentally different routing models that produce different YAML and different runtime behavior.
- [critical] Schema failure behavior contradiction. PRD AC-10 requires 'A blocking error state appears and editing is deferred.' Design CMP-69 states 'Strictly blocking: ...no view-only fallback, no partial initialization.' But Plan STEP-15 instructions say 'on schema failure, keep the canvas view-only and show retry controls.' Plan architecture section repeats: 'If schema fetch fails, the page stays view-only.' View-only is explicitly a NOT criterion in the PRD and design.
- [critical] All plan journey IDs are misaligned with the PRD. Plan steps reference J-16, J-17, J-18, J-20, J-21, J-22; plan journey_verifications cover J-16, J-17, J-20, J-22. The PRD defines only J-1 through J-6. No plan step or verification block references any PRD journey by its correct ID. This means zero PRD journeys have traceable plan coverage.
- [critical] Design journey annotation J-4 doesn't match PRD J-4. Design annotates J-4 with hook edge creation steps ('Steps 1–2 — Create hook edge', 'Step 3 — Inspect hook edge', 'Step 4 — Export hook wiring'). PRD J-4 is 'Schema Endpoint Unavailable On Editor Load' with 2 steps about blocking error state and retry. The design is annotating a journey that doesn't exist in the PRD under this ID.
- [critical] Design annotates journey J-8 which doesn't exist in the PRD. PRD defines only J-1 through J-6. The design's J-8 annotation covers import flows (confirmation, normalization, canvas hydration) which partially maps to PRD J-3 (Import Malformed YAML), but uses the wrong journey ID.
- [critical] SchemaBootstrapGate (CMP-69) — a design-defined component with its own file path, 3 verifiable states, and critical blocking-gate behavior enforcing REQ-7/REQ-13/AC-1/AC-10 — is completely absent from the plan's file_manifest, files_to_create, and all step scopes. The plan's STEP-15 (WorkflowEditorPage) partially absorbs the gate logic but doesn't create the component file the design specifies. (tools/compose/frontend/src/features/editor/schema/SchemaBootstrapGate.tsx)
- [high] MergeFunctionEditor (CMP-65) defined in design at tools/compose/frontend/src/features/editor/inspectors/MergeFunctionEditor.tsx with 3 states and detailed behavior. Not present in any plan step scope, file_manifest, or files_to_create. The system design references merge_function editing in BranchInspector but the implementing component has no plan coverage. (tools/compose/frontend/src/features/editor/inspectors/MergeFunctionEditor.tsx)
- [high] PortConditionRow (CMP-66) defined in design at tools/compose/frontend/src/features/editor/inspectors/PortConditionRow.tsx with 3 states and per-port condition editing behavior. Not present in any plan step scope, file_manifest, or files_to_create. This compounds the BranchNode model contradiction: the design needs PortConditionRow for D-GR-12 per-port conditions, but the system design forbids per-port predicates. (tools/compose/frontend/src/features/editor/inspectors/PortConditionRow.tsx)
- [high] Plan references phantom REQ-15 in STEP-1, STEP-10, and STEP-11 requirement_ids. The PRD defines only REQ-1 through REQ-14. Meanwhile, most actual PRD requirements (REQ-1 through REQ-12) appear in zero plan steps' requirement_ids — plan steps overwhelmingly cite only REQ-13 and REQ-14.
- [high] Repo topology mismatch with SF-5 and SF-7. SF-6 all artifacts use 'tools/compose/frontend' and 'tools/compose/backend'. SF-5 plan summary D-A3 specifies 'platform/compose/backend', 'platform/compose/frontend' (explicitly 'not tools/'). SF-7 plan summary uses 'platform/compose/backend/app/', 'platform/compose/frontend/src/'. SF-6 cites D-SF5-R1 as authority for 'tools/compose' but the parent SF-5 plan says otherwise.
- [high] Cross-subfeature BranchNode schema drift. SF-1 design summary: 'BranchNode: switch_function, inputs, outputs (min 2)'. SF-1 plan summary: 'BranchNode: switch_function + merge_function'. SF-2 design summary: 'D-GR-12: BranchNode = gather + non-exclusive fan-out per port (no switch_function)'. SF-6 system design: 'switch_function is forbidden, per-port branch predicates are forbidden.' SF-6 design: 'per-port conditions (D-GR-12)'. These three different BranchNode contracts across SF-1/SF-2/SF-6 will produce incompatible YAML at save/load boundaries.
- [high] Palette position contradiction. PRD REQ-1 cites D-2: 'Left palette, center canvas, no YAML pane.' System design palette service says 'Right-side drag source for Ask, Branch, Plugin, templates, and role chips.' Left vs right is a fundamental layout decision that affects the entire editor wireframe.
- [high] Plan testid_registry includes 'branch-inspector-{id}-switch-editor' which references SwitchFunctionEditor that the design and system design both prohibit. This stale test ID indicates incomplete cleanup of the switch_function concept from the plan.
- [medium] PRD J-3 (Import Malformed YAML) has no plan verification block. J-22 partially covers import failure but doesn't map to J-3's specific 3-step flow (confirmation before replacement, parse error with line number, structurally invalid YAML loads with warnings).
- [medium] PRD J-4 (Schema Endpoint Unavailable On Editor Load) has no plan verification block. Plan J-22 step 4 partially covers schema failure but doesn't test the retry-and-recover flow from PRD J-4 step 2.
- [medium] PRD J-5 (Core Editor Works On Five-table Foundation) and J-6 (Optional Library Expansion Not Available) have no plan verification blocks and no design journey annotations. These journeys test the SF-5/SF-7 boundary enforcement which is a critical PRD concern (REQ-14).
- [medium] System design EdgeDefinition entity only has 3 fields (source, target, transform_fn) but the PRD data entity Edge specifies 4 fields (source, target, transform_fn, description). The plan's workflowContract.ts definition correctly includes 'description?' but the system design entity omits it.
- [medium] System design defines duplicate service entries for the same physical backend: 'compose-backend' (id: compose-backend) and 'sf1-backend' (id: sf1-backend) both at tools/compose/backend, port 8000. Similarly 'compose-frontend' and 'sf5-shell' both represent tools/compose/frontend. Multiple connections target different IDs for the same service, making the architecture diagram confusing and error-prone.
- [medium] No plan step explicitly implements or tests synthetic root phase normalization for AC-7. The design describes this as a first-class contract, and AC-7 says 'The saved YAML normalizes loose nodes under a synthetic root phase.' Plan STEP-1's acceptance criteria test nested phase serialization but not synthetic root normalization of loose top-level nodes. The AC about synthetic root normalization is untraceable to a specific plan step's verification.
- [medium] Plan step requirement_ids coverage is extremely thin. REQ-1 (React Flow canvas), REQ-2 (three node types), REQ-3 (lifecycle hooks), REQ-4 (selection-rectangle phases), REQ-5 (nested YAML normalization), REQ-6 (edge serialization), REQ-7 (schema endpoint), REQ-8 (two-tier validation), REQ-9 (round-trip fidelity), REQ-10 (CollapsedGroupCard), REQ-11 (undo/redo/auto-save), REQ-12 (performance) are not explicitly cited in ANY plan step's requirement_ids. Only REQ-13 and REQ-14 (plus phantom REQ-15) appear.
- [low] Design CMP-42 DataEdge references 'EdgeTypeLabel (CMP-106, SF-7)' as an external dependency positioned at midpoint. The plan has no stub or integration strategy for this SF-7 primitive dependency, though the system design does acknowledge it under sf7-node-primitives.
- [gap/critical] PRD journeys J-1 through J-6 have zero direct references in any plan step's journey_ids or journey_verifications. The plan uses a completely different journey numbering scheme (J-16, J-17, J-18, J-20, J-21, J-22) with no mapping table or cross-reference. (PRD→Plan traceability)
- [gap/critical] Three design-defined components with CMP IDs, file paths, and verifiable states have no corresponding plan implementation: SchemaBootstrapGate (CMP-69), MergeFunctionEditor (CMP-65), PortConditionRow (CMP-66). (Design→Plan component coverage)
- [gap/critical] No single authoritative BranchNode model is agreed upon across the four SF-6 artifacts. Design follows D-GR-12 (per-port conditions, non-exclusive fan-out). System design defines D-SF6-9 (node-level condition, exclusive routing). Plan creates SwitchFunctionEditor (which both other artifacts prohibit). Implementation cannot proceed until the branch routing model is resolved. (BranchNode contract)
- [gap/high] REQ-1 through REQ-12 are never cited in any plan step's requirement_ids array. Plan steps only reference REQ-13, REQ-14, and non-existent REQ-15. This makes it impossible to trace which steps satisfy which PRD requirements. (PRD requirement coverage)
- [gap/medium] PRD AC-4 (fold context variables in child inspectors), AC-5 (nested loop exits), and AC-9 (undo/redo inspector sync) have no corresponding verification blocks in the plan's journey_verifications and no explicit acceptance criteria in the implementing steps. (Acceptance criteria verification)
- [gap/medium] No artifact addresses how SF-6 will handle the BranchNode model conflict with SF-1 (which defines switch_function) at the import/export boundary. If a user imports YAML authored against the SF-1 schema with switch_function, the editor's import flow has no documented handling strategy beyond generic 'legacy contract rejection.' (Cross-subfeature alignment)
- [gap/medium] REQ-12 requires responsive canvas at 50+ visible nodes but no plan step has an acceptance criterion or verification block that tests performance at scale. The only performance strategy is CollapsedGroupCard (STEP-3) which is an optimization lever, not a verification. (Performance verification)
**security** [FAIL]: The SF-6 Workflow Editor artifacts contain multiple security gaps across export sanitization, import validation, input handling, and contradictions between design and plan that could widen the attack surface. The PRD security profile identifies several requirements ("must not embed secrets" in exports, "never execute inline Python locally," blocking gate on schema failure) that lack corresponding implementation tasks in the plan. A critical plan-vs-design contradiction (SwitchFunctionEditor.tsx created despite explicit design prohibition) introduces unnecessary code surface area. The plan's STEP-15 view-only fallback directly violates the PRD's REQ-13 blocking requirement, potentially exposing users to editing against stale schemas. YAML import lacks bomb/entity-expansion protection, file size limits, and safe-parsing directives. No plan step addresses client-side XSS sanitization of user-supplied node names, edge labels, or transform_fn content rendered in CodeMirror or DOM elements.

- [high] Plan STEP-15 implements 'view-only' fallback when /api/schema/workflow fails, directly contradicting PRD REQ-13 ('blocks editing when the canonical runtime schema endpoint is unavailable'), AC-10 ('A blocking error state appears'), and Design CMP-69 ('No view-only fallback, no partial initialization'). A view-only mode exposes potentially stale inspector metadata and node data to the user, undermining the security guarantee that editing only occurs against a verified runtime contract. (tools/compose/frontend/src/features/editor/WorkflowEditorPage.tsx)
- [high] Plan STEP-9 file manifest and scope include 'tools/compose/frontend/src/features/editor/inspectors/SwitchFunctionEditor.tsx' with action 'create', but the Design explicitly prohibits this component: 'SwitchFunctionEditor does not exist and must not be created' and lists it under REMOVED components. The PRD's D-GR-12 and system design D-SF6-9 both forbid switch_function. Creating this component introduces an unnecessary code path that processes untrusted user input for a feature the runtime explicitly rejects. (tools/compose/frontend/src/features/editor/inspectors/SwitchFunctionEditor.tsx)
- [high] YAML import in STEP-11 specifies js-yaml for parsing but includes no directive to use safe loading mode, no file size limit on imported YAML, and no protection against YAML bomb attacks (billion-laughs, recursive anchors, entity expansion). A crafted YAML file could cause browser tab OOM or hang. The plan's dependency list includes 'js-yaml' but no step constrains parsing options. (tools/compose/frontend/src/features/editor/dialogs/ImportConfirmDialog.tsx)
- [medium] PRD security_profile states 'Users may export YAML externally; the editor must not embed secrets.' No plan step implements export sanitization to strip auth tokens, internal service URLs, or other sensitive metadata that might be present in workflow YAML content (e.g., inline_role system prompts containing API keys, plugin_config with credentials). (tools/compose/frontend/src/features/editor/serialization/serializeWorkflow.ts)
- [medium] PRD REQ-13 requires the editor 'never execute inline Python locally' and 'stores transform_fn and other expressions as data.' No plan step includes a security verification task to audit that zero eval(), new Function(), or dynamic script injection paths exist in the frontend codebase. No Content Security Policy (CSP) task restricts unsafe-eval. CodeMirror renders user-supplied Python strings that could contain script injection payloads if reflected into DOM without sanitization. (tools/compose/frontend/src/features/editor/inspectors/EdgeInspector.tsx)
- [medium] No plan step addresses XSS sanitization of user-supplied content rendered in DOM elements: workflow names, node summaries, phase names, edge descriptions, branch path names, port names, and validation issue messages are all rendered in React components but no sanitization or escaping task is specified. While React's JSX escaping provides baseline protection, dangerouslySetInnerHTML or href injection through crafted dot-notation edge refs (e.g., 'javascript:...' in source/target strings) could bypass this. (tools/compose/frontend/src/features/editor/nodes/AskFlowNode.tsx)
- [medium] Auto-save (STEP-11, useAutoSave.ts) fires PUT /api/workflows/:id every 30s of inactivity but the plan specifies no handling for: (a) HTTP 429 rate limit responses from SF-5's slowapi middleware, (b) auth token expiry during auto-save (no retry-after-refresh), (c) concurrent save conflicts (no optimistic locking or version check). A failed silent auto-save could lose user work without notification. (tools/compose/frontend/src/features/editor/hooks/useAutoSave.ts)
- [medium] The plan references REQ-15 in STEP-1, STEP-10, and STEP-11 requirement_ids, but the PRD only defines REQ-1 through REQ-14. REQ-15 is a phantom requirement with no PRD backing, making it impossible to verify security or functional compliance for the steps that claim to implement it.
- [medium] Plan journey_ids reference J-16, J-17, J-18, J-20, J-22 throughout all steps, but the PRD only defines J-1 through J-6. These phantom journey IDs break traceability, making it impossible to verify that all PRD security-relevant journeys (especially J-4: Schema Endpoint Unavailable and J-3: Import Malformed YAML) have corresponding implementation steps.
- [low] The system design's BranchNode inspector test ID registry includes 'branch-inspector-{id}-switch-editor' which references SwitchFunctionEditor—a component the design itself prohibits. This stale test ID could lead to implementation of the forbidden component or dead test code.
- [low] PRD data_sensitivity notes workflow definitions contain 'proprietary prompts, role definitions, and process logic' but no plan step addresses secure client-side caching. If TanStack Query or Zustand persists workflow YAML to localStorage/sessionStorage (common patterns), sensitive content would be exposed to XSS attacks on the same origin. (tools/compose/frontend/src/features/editor/store/editorStore.ts)
- [gap/medium] No plan step implements the PRD security requirement that exported YAML must not embed secrets. The export path in STEP-11 and serializeWorkflow.ts serialize the full workflow including potentially sensitive transform_fn, inline_role system_prompt, and plugin_config fields without any sanitization or user warning. (Security Implementation)
- [gap/high] No plan step adds YAML parsing size/depth limits or safe-mode configuration for js-yaml import. STEP-11 and deserializeWorkflow.ts are the import entry points but neither specifies maxAliasCount, maximum document size, or any yaml bomb mitigation. (Security Implementation)
- [gap/medium] No plan step includes a CSP (Content Security Policy) configuration task or a frontend security audit to verify the 'never execute inline Python locally' guarantee from REQ-13. Without CSP unsafe-eval restriction, a supply chain attack or XSS could dynamically evaluate user-supplied transform_fn content. (Security Implementation)
- [gap/medium] No plan step addresses client-side handling of HTTP 401 (auth expired) or HTTP 429 (rate limited) responses during editor operations (save, auto-save, validate, import). SF-5 provides rate limiting and JWT auth, but the editor has no error-handling task for these security responses. (Security Implementation)
- [gap/medium] Plan references phantom REQ-15 (appears in STEP-1, STEP-10, STEP-11) that does not exist in the PRD (which ends at REQ-14). This breaks requirement traceability and could mean actual PRD security requirements are not mapped to implementation steps. (Traceability)
- [gap/medium] All plan steps reference journey IDs J-16 through J-22 that do not exist in the PRD (which defines J-1 through J-6). Security-relevant PRD journeys J-3 (Import Malformed YAML) and J-4 (Schema Endpoint Unavailable) have no verifiable mapping to plan steps. (Traceability)
- [gap/high] Plan creates SwitchFunctionEditor.tsx (STEP-9 scope + file_manifest) which the design explicitly prohibits and the PRD's branch model (D-GR-12) does not support. This introduces a security-relevant inconsistency: the component would process user-supplied switch function expressions that the runtime rejects. (Design-Plan Consistency)
- [gap/high] Plan STEP-15 implements 'view-only' mode on schema failure, contradicting PRD REQ-13/AC-10 and Design CMP-69 which mandate strictly blocking with no fallback. This security control (preventing editing against unknown schema contract) is downgraded in the plan. (Design-Plan Consistency)
- [gap/medium] No plan step validates or constrains the content of user-authored transform_fn, merge_function, or branch condition strings beyond structural schema checks. These fields accept arbitrary Python expressions that are stored as data—but no length limit, character restriction, or content policy is specified in any step. (Input Validation)

### workflow-migration [FAIL]

**completeness** [FAIL]: SF-4 artifacts have severe internal inconsistencies. The R13 PRD narrowed to 4 requirements (REQ-34/40/53/54) but the plan still references ~45 requirement IDs from prior PRD revisions. The design uses journey IDs that don't exist in the PRD. The plan's journey_verifications array is empty. AC-23 references a --declarative CLI flag but the plan/system-design implement --yaml. The system design's ExecutionResult.nodes_executed type (list[str]) contradicts the plan's "tuple-based" statement and upstream SF-2/SF-3 contracts (list[tuple]). The plan uses stale SF-3 mock class names (MockRuntime/MockInteraction) while the system design correctly uses MockAgentRuntime/MockInteractionRuntime/MockPluginRuntime. Step ordering is broken: STEP-3 reads template files that STEP-6 creates.

- [critical] Plan references ~45 requirement IDs (REQ-1 through REQ-49) that do not exist in the R13 PRD. The current PRD only defines REQ-34, REQ-40, REQ-53, REQ-54. The plan appears frozen at a prior PRD revision and has not been updated to match R13's narrowed scope. This makes it impossible to verify plan-to-requirement traceability. (plan (workflow-migration))
- [critical] REQ-54 (must-priority: SF-4 must treat SF-2 as ABI owner, no stale assumptions, SF-4 is consumer not co-owner) has NO corresponding plan step. No step lists REQ-54 in its requirement_ids array. This is the central artifact-hygiene requirement of R13 and it is completely unaddressed by any implementation step. (plan (workflow-migration))
- [critical] AC-23 references running the bridge 'through run_declarative() or the CLI --declarative flag' but STEP-10 implements a '--yaml' flag and the system design also describes '--yaml'. The acceptance criterion names a flag that does not exist in the plan or system design. (PRD (workflow-migration) AC-23)
- [critical] The plan's journey_verifications array is empty ([]) despite the PRD defining 4 journeys (J-1 through J-4) that require verification. No journey has verification blocks, making it impossible to confirm journey coverage through the plan. (plan (workflow-migration))
- [critical] Design journey_annotations reference journey IDs 'workflow-migration-planning', 'workflow-migration-develop', 'workflow-migration-bugfix' that do not exist in the PRD. The PRD defines J-1 ('Translate Planning Workflow Against The Canonical SF-2 ABI'), J-2 ('Remove A Stale node_id Consumer Assumption'), J-3 ('Run Declarative Bridge Without A Core Resume Contract'), J-4 ('Consumer Expects Core Checkpoint/Resume'). The design's journeys are organized by workflow type; the PRD's journeys are organized by ABI-alignment concerns. These are fundamentally different structures. (design (workflow-migration))
- [critical] STEP-3 lists template files (gate_and_revise.yaml, broad_interview.yaml, interview_gate_review.yaml) with action 'read', but these files are created in STEP-6. Since STEP-3 executes before STEP-6, these files do not exist when STEP-3 tries to reference them. This is a broken step ordering dependency. (plan (workflow-migration) STEP-3)
- [high] System design entity 'execution_result' defines nodes_executed as list[str], but the plan's STEP-10 acceptance criteria say 'tuple-based nodes_executed'. SF-2 PRD summary defines nodes_executed as 'list of phase_id, node_id tuples' and SF-3 PRD summary defines it as 'list[tuple[phase_id, node_id]]'. The system design's type directly contradicts both the plan and upstream contracts. (system-design (workflow-migration) entity execution_result)
- [high] Plan's Cross-Subfeature Contract section says SF-3 must provide 'MockRuntime, MockInteraction' but the SF-3 PRD (R14) and SF-4's own system design use MockAgentRuntime, MockInteractionRuntime, MockPluginRuntime. The plan uses stale class names that don't match the upstream SF-3 artifacts or SF-4's own system design. MockPluginRuntime is also entirely missing from the plan's SF-3 contract. (plan (workflow-migration))
- [high] AC-24 ('Review the revised SF-4 migration artifact and downstream parity expectations against the SF-2 PRD') is an artifact-review/document-hygiene criterion. No plan step creates a mechanism for verifying this, no test file addresses it, and no automated check is defined. This acceptance criterion is unverifiable given the plan's scope. (PRD (workflow-migration) AC-24)
- [high] Plan's Cross-Subfeature Contract says SF-3 provides '5 base assertions' but the SF-3 plan summary exports 6 specific assertion functions (assert_node_reached, assert_artifact, assert_branch_taken, assert_phase_executed, assert_node_count, assert_validation_error) plus assert_yaml_round_trip. The system design references '12+ assertion functions'. The count is inconsistent across SF-4's own artifacts. (plan (workflow-migration))
- [high] Plan STEP-4 maps to journey J-2 ('Remove A Stale node_id Consumer Assumption') and STEP-5 maps to J-3 ('Run Declarative Bridge Without A Core Resume Contract'). But STEP-4 is about translating the develop workflow and STEP-5 is about translating the bugfix workflow — neither step is specifically about removing stale assumptions or running the bridge. The journey-to-step mapping is forced and doesn't reflect the PRD's journey definitions. (plan (workflow-migration) STEP-4/STEP-5)
- [high] STEP-10 scope entry references path 'iriai_compose/iriai_compose/runner.py' with a doubled directory segment. Other scope entries use 'iriai_compose/plugins/...' (single directory). The correct path pattern for the runner should be 'iriai-compose/iriai_compose/runner.py' (repo-name/package-name) or just 'iriai_compose/runner.py' (package-relative), not the doubled form. (plan (workflow-migration) STEP-10)
- [medium] System design overview claims '~50-55 behavioral equivalence tests' but the itemized breakdown (test_planning: 15 + test_develop: 15 + test_bugfix: 12 + test_yaml_roundtrip: 5 + test_plugin_instances: 8 + test_edge_transforms: 10 = 65) already exceeds that estimate before counting the contract tests. Additionally, STEP-8 lists 18 test files, 12 of which are not counted in the system design's breakdown. The test count is materially understated. (system-design (workflow-migration))
- [medium] System design RuntimeConfig entity defines agent_runtime as type 'dict[str, AgentRuntime]' while the overview says 'RuntimeConfig uses authoritative field names per PRD R5: agent_runtime (singular)'. A dict type with a singular name is semantically confusing. The SF-2 PRD summary uses 'agent_runtimes' (plural) for RuntimeConfig. The correct name/type pairing is ambiguous across artifacts. (system-design (workflow-migration) entity runtime_config)
- [medium] Design component_defs (CMP-1, CMP-2, CMP-3) all point to the same markdown file (design-decisions.md) as their location. These are documentation-level 'components' about metadata interpretation and contract alignment, not actual code or UI components requiring implementation. CMP-1 ('Node Card Reads Metadata') has no corresponding plan step for implementation — its verifiable_states describe 'visual' aspects that are actually documentation concepts. (design (workflow-migration) CMP-1)
- [medium] System design API endpoints list Python function calls (e.g., run(), validate_workflow(), assert_node_reached()) as HTTP endpoints with POST/GET methods and URL paths (e.g., 'POST /declarative/run', 'POST /testing/assert_node_reached'). These are in-process Python function calls in a library, not HTTP API endpoints. This misrepresentation could mislead implementers about the integration model. (system-design (workflow-migration))
- [medium] Plan's architecture references D-GR-24 for checkpoint/resume exclusion, but the PRD only cites D-GR-23. D-GR-24 is not referenced or defined in the PRD. If D-GR-24 is a separate decision from D-GR-23, its absence from the PRD citations creates a traceability gap for the checkpoint/resume exclusion rationale. (plan (workflow-migration))
- [medium] Plan STEP-8 includes test_live_smoke.py but the plan instructions say to 'Treat SF-2's published observability surface as the only runtime boundary' and assert against mock-based results. A 'live smoke' test implies actual service calls, which contradicts the mock-only testing approach. The plan doesn't distinguish how test_live_smoke.py should behave differently from the mock-based test files. (plan (workflow-migration) STEP-8)
- [low] System design connection from iriai_build_v2_runner to iriai_compose_declarative describes 'RuntimeConfig with agent_runtime, plugin_registry, artifacts, sessions'. The field name 'sessions' is listed but the RuntimeConfig entity definition does not include a 'sessions' field — only agent_runtime, interaction_runtimes, artifacts, context_provider, plugin_registry, workspace, feature, max_cost, dry_run. Either the entity is missing the sessions field or the connection description is stale. (system-design (workflow-migration) entity runtime_config)
- [low] PRD open question asks 'Should the migrated YAML workflow files live in iriai-build-v2 or in iriai-compose as portable reference workflows?' but the plan already places all workflow files in tests/fixtures/workflows/migration/ within what appears to be the iriai-compose repo. The open question is answered by the plan without explicit acknowledgment. (PRD (workflow-migration) open_questions)
- [gap/critical] The plan was not updated to match the R13 PRD revision. It still carries requirement IDs, journey mappings, and step structures from earlier PRD versions. The 4 structured requirements in R13 (REQ-34, REQ-40, REQ-53, REQ-54) represent a fundamentally different focus (ABI alignment and artifact hygiene) than the ~45 requirements the plan implements (workflow translation, plugin catalogs, type definitions, seed data). A full plan revision is needed to establish traceability against R13. (requirement-traceability)
- [gap/critical] No journey verification blocks exist in the plan. The journey_verifications array is empty. All 4 PRD journeys (J-1: Translate Planning Workflow, J-2: Remove Stale Assumptions, J-3: Bridge Without Resume, J-4: Consumer Expects Checkpoint) lack explicit verification steps that map journey outcomes to test files or acceptance criteria. (journey-verification)
- [gap/high] The design artifact's journey_annotations use a completely different journey taxonomy (workflow-migration-planning/develop/bugfix) than the PRD (J-1 through J-4 focused on ABI alignment concerns). The design was not updated when the PRD's journeys were revised to focus on contract alignment rather than per-workflow translation. (design-journey-alignment)
- [gap/high] AC-24 (artifact hygiene review confirming all SF-4 language treats SF-2 as ABI owner) has no verification mechanism in the plan. No test file, linting rule, or review step is defined to validate that SF-4 artifacts don't contain stale downstream assumptions. This is the only acceptance criterion for REQ-54 (must-priority). (acceptance-criteria-verification)
- [gap/high] The plan and system design disagree on SF-3 mock runtime class names. Plan uses MockRuntime/MockInteraction (stale names from SF-3 plan). System design uses MockAgentRuntime/MockInteractionRuntime/MockPluginRuntime (current names from SF-3 PRD R14). MockPluginRuntime is entirely absent from the plan's SF-3 contract but used extensively in the system design's call paths and test descriptions. (cross-artifact-consistency)
- [gap/high] STEP-3 depends on reading template YAML files (gate_and_revise.yaml, broad_interview.yaml, interview_gate_review.yaml) that are not created until STEP-6. The implementation_steps list shows STEP-3 before STEP-6, creating an unresolvable ordering dependency. Either STEP-6 must move before STEP-3 or STEP-3's template references must be deferred. (step-ordering)
**security** [FAIL]: SF-4's security profile is thin and narrowly focused on ABI ownership. Multiple security gaps exist: a critical contradiction between SF-4's multi-statement transform testing (compile with 'exec' mode) and SF-2's REQ-33 eval()-only mandate, missing YAML deserialization safety for the --yaml CLI path, no path validation on file loading, unspecified credential handling for env_overrides plugin instances, seed loader with no auth/rollback/integrity verification, and a PRD security profile that ignores code execution, input validation, and injection threats entirely.

- [high] Transform execution model contradicts SF-2 security mandate. SF-4 system design CP-5 step 4 tests transforms via compile(transform_fn, '<string>', 'exec') — exec mode. But SF-2 design summary explicitly states 'D-GR-5 SUPERSEDED: AST-validated exec() overridden by REQ-33's eval()-only mandate.' SF-4's 7 transforms are described as '~5-25 lines each' (multi-statement), which cannot execute under eval()-only. Either transforms won't work at runtime (functionality gap) or they'll use exec() violating SF-2's security sandbox contract. SF-4 plan still cites D-GR-5 throughout (STEP-1, STEP-3-5 instructions) despite it being superseded. (system-design / plan STEP-8)
- [high] No YAML deserialization safety specified for --yaml CLI path. STEP-10 adds --yaml <path> to iriai-build-v2 CLI that loads arbitrary YAML via load_workflow(). Neither the plan nor system design specifies safe YAML loading (e.g., yaml.safe_load). PyYAML's yaml.load() without SafeLoader allows arbitrary Python object instantiation — a known remote code execution vector. SF-4 delegates to SF-1's load_workflow() but never verifies or constrains that it uses safe deserialization. (plan STEP-10)
- [medium] No path validation or sandboxing on --yaml file path argument. STEP-10 accepts an arbitrary filesystem path via CLI --yaml flag. There is no path sanitization, directory confinement, or symlink resolution described. A misconfigured or malicious invocation could load YAML from arbitrary locations (e.g., /etc/passwd-adjacent files, network mounts, or symlinked paths). The plan's counterexamples focus on runtime ABI concerns, not input path safety. (plan STEP-10)
- [medium] Credential storage for env_overrides plugin instances is unspecified. The env_overrides (config) plugin handles secrets per D-GR-10. STEP-5 says 'Keep environment secrets through the env_config plugin only,' and plugin instance configs are declared in workflow YAML. But neither the plan nor system design specifies WHERE secrets are stored at rest — if they're inline in YAML, they'd be in version-controlled test fixtures. The PRD security profile only says 'runtime/plugin auth remains inherited from the consuming environment' without defining the secret injection mechanism. (plan STEP-5 / system-design plugins_instances)
- [medium] Seed loader (seed_loader.py) lacks authentication, input validation, integrity verification, and explicit rollback handling. STEP-9 creates seed_loader.py that takes --database-url as a CLI parameter and performs direct database upserts. Issues: (1) database URL with credentials passed as CLI arg is visible in process listings; (2) no integrity verification (checksum/signature) on migration_seed.json before database writes — tampered seed file could inject malicious content; (3) CP-6 says 'Transaction committed' but no explicit rollback step on partial failure; (4) no schema validation of seed JSON structure before upserting. (plan STEP-9 / system-design CP-6)
- [medium] PRD security profile is narrowly scoped and ignores multiple threat vectors relevant to SF-4. The risk_mitigation_notes focus exclusively on ABI ownership and ContextVar patterns. Missing coverage: (1) code execution risks from inline Python transforms in YAML; (2) YAML deserialization attacks via the --yaml CLI entry point; (3) path traversal via file loading; (4) prompt injection in migrated workflow templates; (5) database injection via seed loader. The 'No PII' and 'Internal data sensitivity' classifications are appropriate but insufficient to justify the absence of code-execution and injection threat modeling. (PRD security_profile)
- [medium] No content security scanning for inline Python code embedded in migrated YAML workflows. Planning.yaml, develop.yaml, and bugfix.yaml all contain inline transform_fn Python strings (7 transforms, 5-25 lines each). STEP-3/4/5 author these files and STEP-8 tests them, but only for syntactic validity and purity (no I/O). There is no mention of static analysis, AST security scanning, or content policy enforcement on the authored transform code before it enters the test fixture directory and seed data. A compromised or carelessly written transform could persist in the seed database via STEP-9. (plan STEP-3/4/5/8)
- [low] Protocol-based adapter typing (D-SF4-25) accepts any structurally matching object as a plugin runtime without type verification. STEP-7 creates adapters with 'Protocol-based structural typing — accepts any service matching the structural interface without importing consumer types.' While this is a valid design pattern, the plan includes no runtime type validation or capability checking. A malicious or buggy object matching the protocol could be injected as a plugin runtime. (plan STEP-7 / system-design plugins_adapters)
- [low] All 31 API endpoints in system design have empty auth field ('auth': ''). While most are internal Python function calls rather than HTTP endpoints, the seed/load endpoint is a CLI entry point and /declarative/run processes untrusted YAML input. The system design should at minimum annotate which endpoints are internal-only vs externally accessible, and which require caller authentication. (system-design api_endpoints)
- [gap/high] No plan step implements or verifies safe YAML deserialization for the --yaml CLI entry point. The PRD security profile says 'No new auth requirement' but the CLI path is a new attack surface requiring input safety guarantees. (Security Implementation)
- [gap/high] No plan step reconciles the transform execution model (multi-statement Python strings, 5-25 lines) with SF-2's eval()-only security mandate (REQ-33). Plan still references superseded D-GR-5. A step or sub-task is needed to either (a) refactor transforms to single expressions, or (b) document an explicit security exception with compensating controls. (Security Implementation)
- [gap/medium] No plan step addresses secret management for env_overrides plugin instances. The PRD risk_mitigation_notes and plan counterexamples forbid secrets in transforms but never specify the approved secret injection path (env vars at runtime? vault? instance config at deploy time?). (Security Implementation)
- [gap/medium] No plan step validates seed_loader.py input integrity or implements rollback. STEP-9 acceptance criteria check JSON validity and idempotent loading but not content integrity, schema validation of individual records, or transactional rollback on partial failure. (Security Implementation)
- [gap/medium] STEP-8's 18 test files include no security-focused test module (e.g., test_transform_sandbox.py, test_yaml_safety.py). The test suite verifies behavioral equivalence and ABI compliance but not security properties like transform sandboxing, YAML injection resistance, or path traversal prevention. (Security Testing)
- [gap/medium] PRD security profile contains no threat model. For a subfeature that executes inline Python, loads arbitrary YAML files from CLI, and writes to a production database via seed loader, the security section should identify at minimum: code injection via transforms, deserialization attacks via YAML, path traversal via CLI, and database corruption via seed data. (Threat Modeling)

## Cross-Subfeature Edge Findings


### SF-1 → SF-2 (python_import) [FAIL]

The data contract for the SF-1 → SF-2 edge is severely mismatched against both the producer and consumer artifacts. Of the 12 exports listed in the data contract, 5 don't exist at all (MapNode, FoldNode, LoopNode, TransformRef, HookRef), 2 have wrong names (Phase→PhaseDefinition, CostConfig→WorkflowCostConfig/PhaseCostConfig/NodeCostConfig), and the module path is wrong (iriai_compose.declarative.schema vs actual iriai_compose.schema). Additionally, the SF-1 plan has a critical internal drift from its own PRD on the actor discriminator (plan uses type:'agent'|'interaction' while PRD and SF-2 both use actor_type:'agent'|'human'), and WorkflowConfig root fields in the plan include stores/plugin_instances which the PRD explicitly rejects and SF-2 would reject at validation time. The actual SF-1 PRD and SF-2 PRD/plan are well-aligned on the wire shape, but the SF-1 implementation plan contains contradictions that would produce an incompatible schema if followed literally.

- [critical] Data contract lists MapNode, FoldNode, LoopNode as exports, but SF-1 PRD REQ-6 explicitly states 'Iteration and fan-out semantics belong to phases, not to standalone Map/Fold/Loop node types.' SF-1 defines exactly three node types: AskNode, BranchNode, PluginNode. Map/Fold/Loop are phase modes on PhaseDefinition, not node classes. Any consumer importing MapNode/FoldNode/LoopNode will get ImportError.
- [critical] Data contract lists TransformRef and HookRef as exports, but neither model exists anywhere in SF-1 artifacts. Transforms are optional string fields (transform_fn on EdgeDefinition), and hooks are explicitly NOT separate models — hook behavior is inferred from port container membership. These are phantom exports.
- [critical] Data contract import path is iriai_compose.declarative.schema but SF-1 creates iriai_compose.schema (a top-level subpackage). SF-2 creates iriai_compose.declarative as a SEPARATE subpackage that imports FROM iriai_compose.schema. The path iriai_compose.declarative.schema does not exist in either subfeature.
- [critical] SF-1 plan STEP-1 instruction 2 defines ActorDefinition with discriminator field 'type' and values 'agent'|'interaction', but SF-1 PRD REQ-2 mandates discriminator 'actor_type' with values 'agent'|'human'. SF-2 plan explicitly consumes actor_type:'agent'|'human' throughout. If SF-1 is implemented per its plan, SF-2's actor hydration will fail because the field name and discriminator values don't match.
- [high] SF-1 plan STEP-2 instruction 5 creates WorkflowConfig with stores, plugin_instances, context_keys, context_text, inputs, and outputs as root fields. But SF-1 PRD REQ-1 and AC-27 explicitly reject stores and plugin_instances at root. SF-2 PRD REQ-2 and its validation will reject these fields. The plan's 'accepted baseline' at line 11 still mentions 'named stores plus hierarchical context', contradicting D-GR-22/D-GR-30.
- [high] Data contract says 'Phase' but SF-1 consistently defines and exports 'PhaseDefinition'. Both the SF-1 PRD (line 271) and the SF-1 plan STEP-2 export list (line 169) use 'PhaseDefinition'. SF-2 uses 'PhaseDefinition' throughout. Import of 'Phase' would fail unless an alias is added.
- [high] Data contract says 'CostConfig' but SF-1 plan STEP-1 defines three separate models: WorkflowCostConfig, PhaseCostConfig, and NodeCostConfig. There is no unified 'CostConfig' class. SF-2 references WorkflowCostConfig, PhaseCostConfig, and NodeCostConfig separately.
- [high] Data contract is missing many critical exports that SF-2 actually imports: ActorDefinition (actor hydration), WorkflowInputDefinition/WorkflowOutputDefinition (typed port validation), NodeDefinition (discriminated union), RoleDefinition (agent actor role), PortDefinition (shared port contract), mode configs (MapModeConfig, FoldModeConfig, LoopModeConfig), PluginInterface, TemplateDefinition, HookPortEvent, load_workflow, validate_workflow, and generate_json_schema. The contract lists 12 exports but the real interface has 20+.
- [medium] Edge class naming inconsistency: SF-1 plan STEP-2 creates 'Edge(BaseModel)' and exports it as 'Edge', while SF-1 PRD data entities section calls it 'EdgeDefinition'. SF-2's entity reference section uses 'EdgeDefinition' throughout. If SF-1 implements per the plan (Edge), SF-2 code that imports EdgeDefinition will fail. The data contract says 'Edge'.
- [medium] SF-1 plan STEP-2 BranchNode instruction includes 'optional merge_function' (line 156), but SF-1 PRD REQ-3 says 'switch_function, merge_function, and alternate branch-routing surfaces are not part of the declarative contract.' SF-2 explicitly validates that merge_function is absent. If SF-1 adds this field, SF-2 validation would need to reject it.
- [gap/critical] The data contract is fundamentally inaccurate — 5 of 12 listed exports are phantom (MapNode, FoldNode, LoopNode, TransformRef, HookRef), 2 have wrong names (Phase, CostConfig), the module path is wrong, and 10+ critical exports actually consumed by SF-2 are missing. The contract needs to be completely rewritten to reflect the actual SF-1 public API.
- [gap/critical] SF-1 plan has at least 3 material deviations from its own PRD that directly affect SF-2 compatibility: actor discriminator (type vs actor_type, interaction vs human), WorkflowConfig root fields (includes rejected stores/plugin_instances), and BranchNode merge_function presence. The plan must be reconciled with the PRD before implementation, or SF-2 will receive an incompatible schema.
- [gap/high] Entity naming is inconsistent across SF-1 artifacts: PRD says EdgeDefinition, plan says Edge. PRD says ActorDefinition with actor_type, plan says ActorDefinition with type. SF-2 follows the PRD naming. A canonical naming document or explicit re-export aliases are needed to prevent import failures.
- [gap/high] The data contract needs to clearly document both the SF-1 schema package path (iriai_compose.schema) and the SF-2 re-export convenience path (iriai_compose.declarative). Currently the contract conflates both into a nonexistent iriai_compose.declarative.schema path.

### SF-1 → SF-3 (python_import) [FAIL]

Cannot review: no artifacts for SF-1 or SF-3 were provided in context. The only context material is MEMORY.md for an unrelated project (iriai — Auto Chess game in Tauri/Rust/React). The edge description references Python schema models (Edge, Node, model_json_schema(), Edge.transform_ref, Node.output_type) and a testing framework, but zero source code for either subfeature was supplied. Review is impossible without the actual producer and consumer artifacts.

- [blocker] No SF-1 (producer) artifacts provided — cannot verify schema models, Edge class, Node class, model_json_schema(), Edge.transform_ref, or Node.output_type exist or have the described shape.
- [blocker] No SF-3 (consumer) artifacts provided — cannot verify the testing framework's imports, usage patterns, or assumptions about the producer's API.
- [info] The only project context (MEMORY.md) describes an unrelated Tauri/Rust/React auto-chess game, not a Python schema/testing system. This context appears to be contamination from the user's broader workspace and is irrelevant to the SF-1 → SF-3 interface.
- [gap/blocker] SF-1 producer source code (Python schema module defining Edge, Node, and related types) was not provided.
- [gap/blocker] SF-3 consumer source code (testing framework that imports and validates schema models) was not provided.
- [gap/blocker] No data contract specification beyond the brief edge description — cannot verify model_json_schema() output shape, Edge.transform_ref type/semantics, or Node.output_type type/semantics.

### SF-2 → SF-3 (python_import) [FAIL]

The edge data contract is severely stale and does not reflect either the SF-2 producer or the SF-3 consumer plans. Both plans have independently evolved past the decomposition-level contract and mostly agree with each other, but critical discrepancies remain in ExecutionResult fields, RuntimeConfig shape, and the AgentRuntime.invoke() node_id parameter.

- [critical] Edge data contract run() signature is completely stale: specifies run(yaml_path, runtime, workspace, transform_registry, hook_registry) but both SF-2 and SF-3 plans agree on run(workflow, config: RuntimeConfig, *, inputs=None). The 'transform_registry' and 'hook_registry' parameters do not exist at all — transforms are inline exec() and hooks are edge-based. This edge contract would mislead any implementer or reviewer relying on it.
- [critical] Edge says ExecutionResult contains 'branch_paths_taken' but SF-2 defines 'branch_paths'. SF-3 plan explicitly states 'branch_paths_taken is stale and must not be referenced'. The edge contract uses the wrong field name.
- [high] ExecutionResult schema disagrees between SF-2 and SF-3 plans. SF-2 defines 'phase_metrics: dict[str, Any]' which SF-3 omits. SF-3 expects 'history: ExecutionHistory | None' and 'errors_routed: list[ErrorRoute]' which SF-2 omits. Since SF-2 owns this dataclass, SF-3's expected fields (history, errors_routed) will not exist at runtime, causing AttributeError in assertion helpers and run_test() wrapper.
- [high] RuntimeConfig.history field expected by SF-3 (for resume testing via run_test(..., history=prior.history)) is not defined in SF-2's RuntimeConfig. SF-2 plan explicitly states resume/checkpoint is 'Out of Scope'. SF-3 STEP-6 constructs RuntimeConfig(history=history) which will fail if the field doesn't exist.
- [high] AgentRuntime.invoke() does not have a 'node_id' parameter in the existing codebase (runner.py:42-50), and SF-2's plan does not explicitly commit to adding one. SF-3 requires node_id for deterministic node-specific mock routing (MockRuntime.when(node_id='ask_1', role='pm')). SF-3 flags this as RISK-3 (high) but has no mitigation beyond hoping SF-2 implements it. This is an unresolved cross-subfeature dependency.
- [gap/critical] The decomposition-level edge contract has not been updated to reflect the actual agreed signature. Both plans converge on run(workflow, config, *, inputs=None) but the edge still says run(yaml_path, runtime, workspace, transform_registry, hook_registry). This document is the authoritative interface spec for reviewers and other subfeatures — it must be corrected.
- [gap/high] No single canonical ExecutionResult definition exists that both SF-2 and SF-3 agree on. SF-2 has phase_metrics but not history/errors_routed. SF-3 has history/errors_routed but not phase_metrics. One authoritative definition must be established and both plans updated.
- [gap/high] SF-2 plan never explicitly specifies adding node_id to AgentRuntime.invoke(), but SF-3's MockRuntime and all node-specific test routing depend on it. The SF-2 plan should include a step that adds this parameter to the ABC to formalize the contract.
- [gap/high] RuntimeConfig.history field is expected by SF-3 for resume testing but is absent from SF-2's RuntimeConfig definition. Even if resume execution is out of scope, the field must exist on the dataclass for SF-3's run_test() wrapper to function.

### SF-1 → SF-4 (yaml_schema) [FAIL]

The SF-1 → SF-4 yaml_schema interface is broadly aligned on the core schema primitives (three node types, four phase modes, edge definitions, templates, actor union) and the YAML I/O surface (load_workflow, dump_workflow, validate_workflow). However, five concrete mismatches were found. The most significant is that SF-4 assumes workflow-level `context_keys` exist on WorkflowConfig for the 4-level hierarchical merge (workflow → phase → actor → node), but SF-1's schema does not include `context_keys` on WorkflowConfig. Additionally, SF-1's internal inconsistency on AskNode fields (plan says summary/task/context_text; design decisions say prompt) creates ambiguity for SF-4 migration authors. Three more gaps — PluginNode.instance_ref with no schema-level resolution, no external type file import mechanism, and workflow_version type inconsistency — compound the integration risk.

- [high] SF-4 plan STEP-3 explicitly references 'workflow-level context_keys' and the 4-level merge order (workflow → phase → actor → node). But SF-1's WorkflowConfig root model has no `context_keys` field — it only exists on PhaseDefinition, AgentActorDef, and NodeBase. The 'workflow' scope of the hierarchy is unrepresentable in the schema. SF-4 migration authors cannot declare workflow-scoped context keys in YAML.
- [medium] SF-1 design decisions CMP-11 says AskNode 'Serializes actor, prompt (Jinja2 template)' but SF-1 plan STEP-2 defines AskNode with summary/task/context_text fields and NO prompt field. SF-4 must author AskNode YAML for all three migrated workflows — the actual field contract is ambiguous within SF-1 itself.
- [medium] PluginNode has instance_ref: str | None and PluginInstanceConfig model exists in SF-1, but WorkflowConfig explicitly forbids plugin_instances at root (REQ-1, extra='forbid'). There is no schema-level collection for plugin instance declarations, so instance_ref targets are unresolvable during load_workflow() validation. SF-4's 8 plugin instances are runtime-only (registered through SF-2), creating a validation gap at the schema boundary.
- [medium] SF-4 STEP-2 creates 4 separate type definition YAML files (types/common.yaml, types/planning.yaml, types/develop.yaml, types/bugfix.yaml) but SF-1's WorkflowConfig.types is inline-only (dict[str, TypeDefinition]). No import/include/reference mechanism exists in the schema for external type files. Migrated workflows would need all types inlined or a pre-processing step outside the schema contract.
- [low] SF-1 PRD and plan define workflow_version as int, but SF-1 design decisions CMP-10 defines it as str. SF-4 YAML files would need to use the correct type; the internal SF-1 inconsistency creates ambiguity.
- [gap/high] WorkflowConfig root model is missing a `context_keys: list[str]` field, preventing the schema from expressing the workflow-scope layer of the 4-level context hierarchy (workflow → phase → actor → node) that both SF-2's published ABI and SF-4's migration plan depend on.
- [gap/medium] No mechanism for referencing or importing external type definition files. SF-4 creates separate type YAML files but the schema only supports inline types on WorkflowConfig. Either a type-include mechanism is needed, or SF-4 must inline all types into each workflow YAML.
- [gap/medium] PluginNode.instance_ref cannot be validated at schema load time because plugin instances have no declarative home in the schema. Validation of instance_ref targets is deferred entirely to runtime (SF-2), creating a validation gap at the SF-1/SF-4 boundary.
- [gap/medium] AskNode field structure is contradictory within SF-1: design decisions specify a `prompt` field (Jinja2), plan specifies summary/task/context_text. SF-4 migration authoring depends on the correct field names.

### SF-2 → SF-4 (python_import) [FAIL]

Cannot verify the SF-2 → SF-4 interface contract. Zero artifacts were provided for either subfeature. The context contains only an unrelated project (iriai-command, an Auto Chess game). The edge describes a Python workflow migration system with run(), TransformRegistry, and HookRegistry — none of which appear anywhere in the provided context. Review is impossible without producer and consumer code.

- [critical] No artifacts for SF-2 (producer) were provided in the context. The review cannot proceed without the actual code that implements run(), TransformRegistry.register(), and HookRegistry.register().
- [critical] No artifacts for SF-4 (consumer) were provided in the context. The review cannot proceed without the actual code that consumes the migration interface.
- [high] Scope contamination: The only context provided (MEMORY.md for iriai-command, a Tauri/React auto-chess game) is entirely unrelated to the Python workflow migration system described in the edge contract. This suggests the artifacts were not correctly injected into the review context.
- [gap/critical] SF-2 producer source code (the module exposing run(), TransformRegistry, HookRegistry) is entirely absent from the review context.
- [gap/critical] SF-4 consumer source code (the migration module that imports and uses run(), registers transforms and hooks) is entirely absent from the review context.
- [gap/critical] No test files, type stubs, or interface definitions provided for either subfeature.

### SF-3 → SF-4 (python_import) [FAIL]

Cannot verify the SF-3 → SF-4 interface contract. No implementation artifacts were provided for either subfeature. The edge specifies that `iriai_compose.testing` should export MockRuntime, assert_node_reached, assert_artifact_produced, assert_branch_taken, and WorkflowTestCase — but without source code, module definitions, or design docs for SF-3 (producer) or SF-4 (consumer), none of the 7 verification checks can be performed. Additionally, the only contextual document (MEMORY.md) describes an unrelated Tauri/Rust/React project, not a Python `iriai_compose` package — a possible scope contamination issue.

- [critical] No implementation artifacts were provided for SF-3 (producer). The review cannot proceed without source code, module definitions, or at minimum a detailed design document showing what iriai_compose.testing actually exports.
- [critical] No implementation artifacts were provided for SF-4 (consumer). The review cannot proceed without source code showing how the testing framework exports are imported and used.
- [medium] Scope contamination: The only context document (MEMORY.md) describes 'iriai-command', a Tauri 2.x / Rust / React desktop game. The edge under review describes a Python package (iriai_compose.testing). These appear to be entirely different systems. If iriai_compose is part of the broader iriai ecosystem, the relationship is not documented anywhere in the provided context.
- [gap/critical] SF-3 producer artifacts are entirely absent. Need at minimum: the iriai_compose/testing/ module source (or __init__.py), MockRuntime class definition, assertion helper function signatures, and WorkflowTestCase base class.
- [gap/critical] SF-4 consumer artifacts are entirely absent. Need at minimum: migration test files or code that imports from iriai_compose.testing, showing usage of MockRuntime, assertion helpers, and WorkflowTestCase.
- [gap/high] No package structure (pyproject.toml, setup.py, or directory tree) provided to verify that the import path `iriai_compose.testing` is valid and resolvable.
- [gap/high] The edge specifies MockRuntime should have 'configurable responses per role/node' but without the class definition, we cannot verify the configuration API (constructor args, method signatures, response format).
- [gap/high] The edge specifies assert_artifact_produced takes a 'schema' parameter but no schema format/type is defined. Is it a JSON Schema dict, a Pydantic model, a marshmallow schema, or something else? Both sides must agree.
- [gap/high] The 'result' parameter passed to all assertion helpers is unspecified in type. Both producer and consumer must agree on what object type 'result' is (e.g., a WorkflowResult dataclass, a dict, etc.).

### SF-1 → SF-6 (json_schema) [FAIL]

Cannot verify the SF-1 → SF-6 interface contract. No artifacts for either subfeature were provided in the review context. The only project context available (MEMORY.md) describes an unrelated Tauri auto-chess game with no connection to workflow editors, JSON Schema generation, or Pydantic models. All 7 verification checks are blocked due to missing artifacts.

- [critical] No artifacts for SF-1 (producer) were provided in the review context. Cannot verify JSON Schema generation from Pydantic models, static artifact publishing, or backend endpoint exposure.
- [critical] No artifacts for SF-6 (consumer) were provided in the review context. Cannot verify workflow editor schema consumption, node inspector field generation, edge type validation, YAML serialization/deserialization, or export format handling.
- [medium] Context contamination: MEMORY.md describes an unrelated project (iriai-command auto-chess game). No workflow/schema-related project context exists in the provided materials.
- [gap/critical] SF-1 Pydantic model definitions that generate the JSON Schema (e.g., node types, edge types, configuration options)
- [gap/critical] SF-1 schema generation/publishing mechanism (static file export or API endpoint)
- [gap/critical] SF-6 workflow editor code that consumes the JSON Schema for node inspector field generation
- [gap/critical] SF-6 YAML pane serialization/deserialization code that uses the schema
- [gap/critical] SF-6 validation logic that uses the schema for type flow checking
- [gap/high] No concrete schema shape or example (workflow-schema.json) was provided to anchor the review

### SF-5 → SF-6 (api_and_components) [FAIL]

UNABLE TO VERIFY: No implementation artifacts were provided for either SF-5 (producer: App foundation) or SF-6 (consumer). The edge describes a rich contract (authenticated API client, React router shell, XP-themed component library, workflow CRUD endpoints, auth context), but without actual code artifacts for either side, none of the 7 review criteria can be evaluated. Additionally, the MEMORY.md context references an entirely unrelated project (iriai — an auto-chess agent orchestration game built with Tauri/Rust/React/Phaser), which has no bearing on this XP-themed workflow editor interface.

- [blocker] No SF-5 (producer) implementation artifacts were provided in context. Cannot verify that any part of the contract is actually produced: API client, router shell, component library, CRUD endpoints, or auth context.
- [blocker] No SF-6 (consumer) implementation artifacts were provided in context. Cannot verify that the consumer correctly imports and uses the contracted interfaces.
- [low] Context contamination: MEMORY.md references an unrelated project (iriai - auto-chess agent orchestration with Tauri/Rust/Phaser). This has no relevance to the XP-themed workflow editor interface being reviewed.
- [gap/blocker] SF-5 producer artifacts entirely missing — need source for: API client module (axios + JWT interceptor), React router configuration, XP component library (XPButton, XPPanel, XPInput, XPToolbar, XPSidebar), workflow CRUD API route handlers, and useAuth() hook implementation.
- [gap/blocker] SF-6 consumer artifacts entirely missing — need source showing: how the editor route is mounted, which XP components are imported and used, how workflow CRUD endpoints are called, and how auth context is consumed.
- [gap/high] Data contract lacks schema details: what is the shape of a Workflow object returned by GET /api/workflows/:id? What fields does the versions POST expect/return? What is the validation error response format from POST /api/workflows/:id/validate? Without these, even with artifacts, type compatibility would be hard to assess.
- [gap/medium] Component prop APIs are unspecified. The contract names XPButton, XPPanel, XPInput, XPToolbar, XPSidebar but does not define their prop interfaces, making it impossible to verify the consumer uses them correctly.

### SF-5 → SF-7 (api_and_components) [FAIL]

Cannot verify the SF-5 → SF-7 interface contract. No implementation artifacts for either subfeature were provided in context. The edge description specifies REST CRUD endpoints for 6 entity types (/api/roles, /api/schemas, /api/templates, /api/phases, /api/plugins, /api/transforms), a { items: [...], total: int } response format, authenticated user_id scoping, a React context, and a shared component library — but without producer and consumer code, none of these claims can be validated.

- [blocker] No implementation artifacts for SF-5 (producer) were provided in context. The review cannot proceed without the API route definitions, response type definitions, authentication middleware, and exported React components/context.
- [blocker] No implementation artifacts for SF-7 (consumer) were provided in context. The review cannot proceed without the code that calls the API endpoints and imports the shared components.
- [high] The edge description references 6 entity endpoints but the data contract also mentions '8 entity types' in the description text — only 6 are listed (/api/roles, /api/schemas, /api/templates, /api/phases, /api/plugins, /api/transforms). Two entity types are missing from the contract specification, creating ambiguity.
- [high] The edge description does not specify an error response format. Both producer and consumer need to agree on error shapes (e.g., { error: string, code: number } or { detail: string }). This is a contract gap even independent of the missing artifacts.
- [medium] The edge description says 'Same React context and component library as SF-6' — this is a cross-reference to SF-6 whose artifacts are also not in scope. SF-7's contract should be self-contained or the SF-6 contract should be explicitly included.
- [low] The MEMORY.md context is about an unrelated Iriai auto-chess project (Tauri/Rust/Phaser). This appears to be context contamination — it has no relevance to REST CRUD APIs for roles/schemas/templates/phases/plugins/transforms.
- [gap/blocker] SF-5 producer artifacts (API route handlers, middleware, type definitions, exported components) are entirely absent from context.
- [gap/blocker] SF-7 consumer artifacts (API client calls, component imports, type usage) are entirely absent from context.
- [gap/high] Edge description mentions '8 entity types' but only lists 6 endpoints. The 2 missing entity types need to be identified and documented.
- [gap/high] No error response format specified in the data contract. Pagination parameters (offset, limit, cursor) for list endpoints are also unspecified.
- [gap/medium] Authentication mechanism not specified — is it JWT Bearer token, session cookie, API key? Consumer needs to know how to attach credentials.

### SF-7 → SF-6 (react_components) [FAIL]

Cannot verify the SF-7 → SF-6 interface contract because no implementation or specification artifacts were provided for either subfeature. The edge description specifies 5 React components (RolePicker, SchemaPicker, TemplateBrowser, PluginPicker, TransformPicker) with specific prop contracts, but without producer or consumer artifacts, none of the 7 verification criteria can be assessed. This is a blocking gap.

- [blocker] No SF-7 (producer) artifacts were provided in context. Cannot verify that any of the 5 specified picker/selector components exist, are exported, or conform to the declared prop interfaces.
- [blocker] No SF-6 (consumer) artifacts were provided in context. Cannot verify that node inspectors import or use the picker components, or that callback prop usage matches the producer's contract.
- [high] The data contract specifies 'Each fetches from its own API endpoint' but no API endpoint definitions are provided. If SF-7 components make their own API calls, there is an implicit dependency on backend endpoints that is not captured in this edge and cannot be verified.
- [high] The data contract references 'XP design system' rendering but no design system artifacts or type contracts are available to verify visual/behavioral consistency between the pickers and the editor UI.
- [medium] The edge description mentions 'onCreateInline' on RolePicker which implies the consumer (SF-6) expects inline creation capability. This is a more complex contract than simple selection — it likely requires the producer to expose a creation form or modal, and the consumer to handle the resulting new entity. Without artifacts, this coupling cannot be verified.
- [gap/blocker] SF-7 producer artifacts (component source, exports, prop type definitions) are entirely absent from the review context.
- [gap/blocker] SF-6 consumer artifacts (node inspector source, import statements, component usage) are entirely absent from the review context.
- [gap/blocker] No TypeScript interfaces or type definitions provided for the callback props (onSelect, onCreateInline, onDrag). The shape of data passed through these callbacks is the core of the interface contract and is completely unspecified beyond prop names.
- [gap/high] Pickers 'each fetch from their own API endpoint' but no API specifications are included. This is an implicit backend dependency that could cause runtime failures if endpoints don't exist or return unexpected shapes.
- [gap/high] No package/module export map provided for SF-7. Cannot determine if components are individually importable, barrel-exported, or lazy-loadable — all of which affect how SF-6 can consume them.