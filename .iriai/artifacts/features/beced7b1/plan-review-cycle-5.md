# Plan Review Report

**250 concerns, 153 gaps** across 7 subfeatures and 10 edges.

**Failed SF reviews:** declarative-schema (completeness), declarative-schema (security), dag-loader-runner (completeness), dag-loader-runner (security), testing-framework (completeness), testing-framework (security), workflow-migration (completeness), workflow-migration (security), composer-app-foundation (completeness), composer-app-foundation (security), workflow-editor (completeness), workflow-editor (security), libraries-registries (completeness), libraries-registries (security)

**Failed edge reviews:** SF-1 → SF-2, SF-1 → SF-3, SF-2 → SF-3, SF-1 → SF-4, SF-2 → SF-4, SF-3 → SF-4, SF-1 → SF-6, SF-5 → SF-6, SF-5 → SF-7, SF-7 → SF-6


## Per-Subfeature Findings


### composer-app-foundation [FAIL]

**completeness** [FAIL]: Rejected. The SF-5 artifact set is not implementable or reviewable as a plan: the plan has no actionable steps or journey verifications, the system design still carries a stale SQLite/reference-index architecture that violates the PRD, and key cross-subfeature contracts remain unresolved.

- [critical] The plan is effectively empty. `steps` and `journey_verifications` are both empty, so none of the PRD's 20 requirements or 6 journeys is decomposed into implementation or verification work. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [critical] The manifest/files-to-modify scope only includes the local `plan.md`. That scope cannot produce or verify the migrations, FastAPI routers, auth middleware, React apps, or deployment assets required by AC-1 through AC-14, so the acceptance set is not actually verifiable from this plan artifact. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [critical] The system design contradicts REQ-2 and REQ-19 by specifying SQLite as the foundation database (`SVC-SF5-7`, D-SF5-5, RISK-3), while the PRD requires PostgreSQL with Alembic and Railway deployment. Readiness, migration, and data-residency assumptions are therefore inconsistent. (SYSTEM-DESIGN — composer-app-foundation)
- [critical] The system design introduces `WorkflowEntityRef`, reference-index rebuilds, and `/api/{entity}/references/{id}`. That creates a sixth SF-5 table and additional API surface, directly violating REQ-2/AC-13's 'exactly 5 tables' boundary. (SYSTEM-DESIGN — composer-app-foundation)
- [critical] The plan's `implementation_steps` claim rewrites to SF-1, SF-2, and SF-6 ('rewrote the SF-1 schema step', 'rewrote the SF-2 runner step', 'rewrote the SF-5/SF-6 frontend/editor steps'). That is scope contamination for an SF-5-only review, and it is self-inconsistent with the manifest listing only the local SF-5 plan file. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [critical] Dependency contracts are not actually stabilized. The provided SF-1/SF-2/SF-6/SF-7 summaries still contain stale interfaces (`phases` nesting, serialized `port_type`, plugin folders/endpoints, PUT-based workflow saves), but the plan only notes this as a risk instead of defining dependency gates or interface cutovers. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [high] REQ-18 and AC-1 require a tier-gated tools hub with cards for Workflow Composer, iriai CLI, and MCP Tools, driven by `dev_tier`. The design/system-design only cover navigation into compose; they do not define the tools-hub card set, gated states, or coming-soon behavior. (DESIGN — composer-app-foundation)
- [high] The import contract is internally inconsistent. REQ-9 allows schema-invalid YAML with explicit warnings, and CMP-15 assumes workflows can persist with warning state, but CP-SF5-5 says only schema-valid imports return 201. The warning-vs-rejection boundary is not defined well enough to implement. (SYSTEM-DESIGN — composer-app-foundation)
- [high] The audit/save contract is unresolved. REQ-3 and AC-10 make append-only version creation central, but the system design adds `PUT /api/workflows/{id}` that updates YAML without creating a version row, while the provided SF-6 summaries still save via PUT. The authoritative editor save path is therefore ambiguous. (SYSTEM-DESIGN — composer-app-foundation)
- [high] Reference-blocking delete semantics and the entire `/api/{entity}/references/{id}` surface are not in the PRD. Those behaviors import SF-7 library-management scope into SF-5 without requirement coverage or matching acceptance criteria. (SYSTEM-DESIGN — composer-app-foundation)
- [high] `dependencies` is empty even though the PRD explicitly depends on auth-service, deploy-console patterns, SF-1, SF-2, SF-6, and SF-7. Without dependency sequencing or ownership, the plan cannot manage integration risk or contract handoff. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [high] The design introduces `SidebarTree`, `NewDropdown`, `GridCard`, `EditorSchemaBootstrapGate`, and `YAMLContractErrorPanel`, and lists `XPToast` in `components`, but the plan contains no implementation tasks or verification hooks for any of them. `XPToast` is not even defined in `component_defs` or `verifiable_states`. (DESIGN — composer-app-foundation)
- [high] REQ-6 and REQ-7 are not grounded in the design/system plan. There is no implementation or verification coverage for the standardized `{ error, error_description, details }` envelope, per-user rate limiting, structured JSON security logging, correlation IDs, auth-failure logging, or delete/import security events. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/plan.md)
- [medium] Multiple cited decisions do not resolve anywhere in the provided artifact set: D-2, D-3, D-7, D-10, D-12, D-13, D-18, D-19, D-50, D-55, and the plan's `D-A12`. Key scope and security claims are therefore not traceable from the supplied materials. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/composer-app-foundation/prd.md)
- [medium] Repository and host topology drift is unresolved across the provided context. SF-5 system design uses `tools/iriai-workflows/...`, SF-6/SF-7 summaries use `platform/compose/...`, and the SF-1 summary still refers to `composer.iriai.app` while SF-5 uses `compose.iriai.app`. That breaks path and deployment traceability. (SYSTEM-DESIGN — composer-app-foundation)
- [medium] Traceability links already drift inside SF-5. CP-SF5-5 is attached to J-5 even though it models YAML import behavior from J-6, and AC-5 ties nested-YAML persistence to REQ-17 (schema bootstrapping) rather than just the serialization requirements. Verification mapping is unreliable. (SYSTEM-DESIGN — composer-app-foundation)
- [gap/critical] No plan step covers REQ-1 through REQ-3: FastAPI service structure, PostgreSQL/Alembic five-table schema, and WorkflowVersion audit/versioning behavior. (requirement coverage)
- [gap/critical] No plan step covers REQ-4 through REQ-7: ownership scoping, JWT/JWKS auth, standardized errors, health/readiness, rate limiting, and structured security logging. (requirement coverage)
- [gap/critical] No plan step covers REQ-8 through REQ-14: workflow CRUD/search/pagination, duplicate/import/export, `/api/schema/workflow`, validation, nested YAML persistence, edge-only hooks, and no serialized `port_type`. (requirement coverage)
- [gap/high] No plan step covers REQ-15 through REQ-18: compose SPA shell, four-folder Explorer layout, runtime schema bootstrapping, and the tier-gated tools hub. (requirement coverage)
- [gap/high] No plan step covers REQ-19 and REQ-20: Railway/Vite deployment shape and explicit enforcement of negative plugin scope. (requirement coverage)
- [gap/high] J-1 (tools hub to compose landing) has no verification block. (journey verification)
- [gap/high] J-2 (create workflow and open editor) has no verification block. (journey verification)
- [gap/high] J-3 (bootstrap editor from canonical schema) has no verification block. (journey verification)
- [gap/high] J-4 (save/export nested workflow with hook edges) has no verification block. (journey verification)
- [gap/high] J-5 (schema endpoint failure UX) has no verification block. (journey verification)
- [gap/high] J-6 (stale-contract YAML import failure) has no verification block. (journey verification)
- [gap/high] No implementation mapping exists for CMP-3, CMP-10, CMP-15, CMP-18, CMP-19, or the listed `XPToast` behavior. (component coverage)
- [gap/high] The plan records no dependencies or sequencing for auth-service, deploy-console references, SF-1, SF-2, SF-6, or SF-7 despite explicit cross-service impacts in the PRD. (dependency coverage)
- [gap/medium] `testid_registry` is empty and there is no artifact-level verification strategy for bootstrap errors, import warnings/errors, auth/rate-limit behavior, or the tools hub flows. (verification infrastructure)
**security** [FAIL]: The SF-5 artifacts leave multiple security requirements either unimplemented or contradicted by the system design. The largest issues are a storage-engine mismatch that drops the PRD’s PostgreSQL/Railway security assumptions, missing explicit auth/authorization coverage for several described flows, no concrete CORS/CSRF plan for two authenticated SPAs on separate origins, incomplete input-validation coverage on user-controlled payloads, and no migration rollback guidance despite Alembic being required.

- [high] The system design contradicts the PRD’s required PostgreSQL deployment by specifying SQLite as the production database. That breaks the PRD security profile’s Railway/PostgreSQL data-residency assumption and weakens concurrency/isolation expectations for authenticated multi-user writes. This is a blocking plan inconsistency, not just an implementation detail. (SYSTEM-DESIGN)
- [high] The PRD requires all non-health endpoints to use JWT auth with auth-service JWKS validation, but the plan artifact contains no implementation steps, verification items, or endpoint-by-endpoint auth coverage. The system design marks endpoints with `auth: JWT Bearer`, but that is descriptive only; there is no plan task for wiring auth dependencies/middleware across every router. (PLAN)
- [high] There is no explicit authorization task ensuring every user-owned endpoint filters by `user_id` and returns `404` instead of `403` on cross-user access. The PRD makes this mandatory, but the plan summary does not include ownership enforcement work or verification beyond a high-level rewrite note. (PLAN)
- [high] The API surface includes destructive and sensitive endpoints such as `DELETE /api/workflows/{id}`, `POST /api/workflows/import`, `POST /api/workflows/{id}/versions`, and all library CRUD endpoints, but the plan does not specify per-endpoint input validation rules, request-size limits, or schema constraints for user-controlled YAML/JSON/text fields. This is a gap for malformed or oversized payload abuse. (SYSTEM-DESIGN)
- [high] No CORS policy is defined for the backend even though two separate authenticated SPAs (`tools.iriai.app` and `compose.iriai.app`) call or navigate across origins. Without an explicit allowed-origin plan, the backend risks being deployed over-broad (`*`) or misconfigured in a way that enables cross-origin token-bearing requests. (SYSTEM-DESIGN)
- [high] No CSRF strategy is documented for the authenticated browser flows. The artifacts assume JWT/OAuth browser usage but never state whether tokens are stored in cookies or headers, and there is no explicit decision to avoid cookie auth or add CSRF protections. For browser-based authenticated mutation endpoints, that omission is a security gap. (DESIGN)
- [medium] The PRD requires per-user rate limiting and structured security logging, including auth-failure logging, rate-limit logging, and delete/import security events, but the plan has no concrete implementation tasks, no coverage table by endpoint, and no verification entries. The requirement exists only at PRD/system-design prose level. (PLAN)
- [medium] Rate limiting is not scoped for the anonymous `/health` and `/ready` endpoints. The review focus explicitly calls for rate limiting on public endpoints, but the artifacts provide no mitigation for unauthenticated probe abuse or readiness-endpoint hammering. (SYSTEM-DESIGN)
- [medium] The import flow allows 'schema-invalid YAML with explicit warnings' while also carrying internal workflow definitions and prompts. The artifacts do not define sanitization, redaction, or logging boundaries for imported content, creating risk that structured security logs capture sensitive prompt/YAML bodies during delete/import security-event logging. (PRD)
- [medium] `GET /api/schema/workflow` is authenticated per PRD, but the tools-hub tier-gating model relies on `dev_tier` from tokens and the artifacts never define authorization behavior for insufficient tiers. There is no plan for whether unauthorized tiers receive 403/404, are gated only in the frontend, or are blocked at the backend at all. (PRD)
- [medium] The system design introduces `GET /api/{entity}/references/{id}` and a `workflow_entity_refs` index/table even though the PRD requires exactly 5 SF-5 tables. Besides being a scope inconsistency, this creates an unreviewed authorization surface for relationship data without any explicit authz or privacy handling in the plan. (SYSTEM-DESIGN)
- [medium] The system design adds `workflow_entity_refs` persistence and reference endpoints that are not present in the PRD’s data-entity list. That is scope contamination from adjacent library work and expands the attack surface without corresponding security requirements, validation rules, or deletion/visibility controls in the SF-5 plan. (SYSTEM-DESIGN)
- [medium] The starter-template bundle is read from checked-in filesystem assets, but there is no integrity or trust-boundary task for validating those assets beyond runtime schema validation. If starter content is compromised in-repo, the design has no signing, provenance, or restricted loading mechanism. (SYSTEM-DESIGN)
- [medium] The PRD says exported YAML can leave the platform and SF-5 should not store secrets in workflow YAML, but there is no implementation task to detect or block secret-like content before save/import/export. The security profile states the risk; the plan does not implement a control. (PRD)
- [medium] No explicit validation or normalization rules are defined for search and cursor parameters on list endpoints. User-facing query parameters exist across workflows, roles, schemas, and templates, but the artifacts omit constraints needed to prevent abusive pagination or pathological search inputs. (SYSTEM-DESIGN)
- [medium] The plan requires Alembic migrations but provides no rollback/backout steps. The review criteria explicitly call this out, and the artifacts do not describe downgrade coverage, rollback sequencing, or recovery from a failed migration-before-serve deployment. (PLAN)
- [low] Security logging is required to include request correlation, but there is no explicit plan for propagation of correlation IDs across frontend, backend, and auth-service interactions. The requirement is therefore underspecified and unverifiable. (PRD)
- [low] The artifacts do not specify whether validation errors returned from YAML import/validate should be scrubbed to avoid echoing sensitive prompt content or raw parser internals back to clients. Path/message shaping exists, but safe error-detail boundaries do not. (SYSTEM-DESIGN)
- [low] The system design references external subfeatures and services for security-critical behavior (`iriai-compose` validation models, auth-service, adjacent SFs) without isolating the exact contract SF-5 depends on. Given the task scope, this is contamination that leaves SF-5 security behavior partially delegated rather than planned within the reviewed artifact set. (SYSTEM-DESIGN)
- [gap/high] No actionable plan steps for implementing JWT/JWKS auth on every non-health endpoint. (Authentication)
- [gap/high] No explicit implementation coverage for `user_id` scoping and mandatory 404-on-cross-user access across all user-owned resources. (Authorization)
- [gap/high] Missing explicit CORS policy for the backend serving separate authenticated SPAs. (Web Security)
- [gap/high] Missing CSRF posture/decision for browser-based authenticated mutation flows. (Web Security)
- [gap/high] Missing concrete size/shape validation for import YAML, save/version payloads, metadata text fields, and query parameters. (Input Validation)
- [gap/medium] Per-user rate limiting is required but not translated into plan tasks or verification; public health/readiness endpoints also have no abuse controls. (Rate Limiting)
- [gap/medium] Structured security logging is required, but there is no plan to prevent YAML/prompt content leakage into logs or verbose error responses. (Logging & Privacy)
- [gap/high] Database/storage design is inconsistent with the PRD security profile: PostgreSQL/Railway vs SQLite. (Deployment & Data Security)
- [gap/medium] Alembic migration rollback/downgrade steps are missing. (Migration Safety)
- [gap/medium] `workflow_entity_refs` and related reference APIs/tables expand the security surface beyond the PRD’s stated 5-table SF-5 scope. (Scope Control)

### dag-loader-runner [FAIL]

**completeness** [FAIL]: Not approvable. The PRD and design do align on the D-GR-22 direction (nested phases, edge-based hooks, live schema), but the plan and system-design are effectively empty, key runtime interfaces remain unspecified, and several acceptance criteria depend on out-of-scope or stale cross-subfeature contracts.

- [blocking] `PLAN — dag-loader-runner` is marked complete even though `steps`, `journey_verifications`, `file_manifest`, `architectural_risks`, `testid_registry`, `dependencies`, `implementation_steps`, and `risks` are all empty, and the only scoped file is the plan document itself. There is no executable implementation or verification plan for any SF-2 requirement. (PLAN — dag-loader-runner)
- [blocking] `SYSTEM-DESIGN — dag-loader-runner` is also marked complete while `services`, `connections`, `api_endpoints`, `call_paths`, `entities`, `entity_relations`, `decisions`, and `risks` are empty. That omits the required `validate()`/`run()` API surface, `RuntimeConfig`/`HierarchicalContext`/`ExecutionResult` entities, and the schema-endpoint interface. (SYSTEM-DESIGN — dag-loader-runner)
- [high] The execution model for top-level sibling phases is ambiguous. The PRD still defines `WorkflowConfig.edges` and REQ-3 requires DAG execution for sibling phases, but the design only describes recursion through `WorkflowConfig.phases -> PhaseDefinition.nodes/children` and never explains where root-container edges live or how top-level phase ordering/wiring is represented. (DESIGN — dag-loader-runner)
- [high] REQ-8/D-GR-23 requires ContextVar-backed hierarchical context merged in workflow -> phase -> actor -> node order without changing `AgentRuntime.invoke()`, but the design only mentions this in passing and neither the system-design nor the plan defines the context carrier, merge algorithm, or runtime integration. A central compatibility requirement is currently unplanned. (DESIGN — dag-loader-runner)
- [high] REQ-10/AC-6/AC-8 require a concrete `ExecutionResult` plus `ExecutionHistory`/phase-metrics contract, yet the PRD still leaves the exact `ExecutionHistory` public shape open and the design/system-design/plan provide no schema or verification for that output. Downstream testing cannot bind to this interface from the current artifacts. (PRD — dag-loader-runner)
- [high] REQ-14/AC-9 depends on explicit runtime ownership boundaries for configured plugin runtimes, separate test runtimes, and external stdio MCP servers, but the design/system-design/plan contain no tasks or contract definitions for that boundary. The decision is cited but not operationalized. (PRD — dag-loader-runner)
- [high] The PRD remains contaminated with out-of-scope composer/editor behavior. AC-4, AC-5, AC-10 and journeys J-2/J-4 specify backend/editor endpoint and UI behavior even though the plan architecture says this revision is library-only and the plan's file scope never leaves SF-2. Those acceptances are not verifiable within the plan's own scope. (PRD — dag-loader-runner)
- [high] Cross-subfeature contract drift is unresolved. The provided SF-3/SF-4 summaries still assume checkpoint/resume is implemented in SF-2 and that runtimes may take a `node_id` kwarg; SF-6 summaries still model `edge.port_type`, loose top-level nodes/implicit root phases, and even an Error node. The current plan includes no dependency or migration work to realign those consumers to D-GR-22/23/24/25. (PLAN — dag-loader-runner)
- [high] The design lists five components (`Recursive workflow loader`, `Container-local execution graph builder`, `Hook edge classifier`, `Validation facade`, `Schema export bridge`) but leaves `component_defs` and `verifiable_states` empty, and the plan has no tasks for any of them. The design decomposition is not actionable. (DESIGN — dag-loader-runner)
- [medium] The validation error contract is inconsistent across subfeatures. SF-2 defines `ValidationError { field_path, message, severity }`, while the provided SF-5 API contract still returns `{ path, message }`. No mapping or interface note exists in the design, system-design, or plan, so validation consumers do not have a stable error schema. (PRD — dag-loader-runner)
- [medium] The design introduces behavior not grounded in the PRD: it bans transforms on hook edges and declares empty workflows / empty phases invalid. Those are additional validation rules that are not stated in SF-2 requirements or acceptance criteria. (DESIGN — dag-loader-runner)
- [medium] The design adds a `Schema export bridge` component without clarifying ownership between SF-2 and SF-5. REQ-12 says `/api/schema/workflow` is the canonical composer schema source, but the endpoint itself belongs to composer/backend and is not modeled in the system-design or plan. Ownership is ambiguous. (DESIGN — dag-loader-runner)
- [medium] Two PRD open questions remain unresolved despite the artifact being marked complete: authentication expectations for `/api/schema/workflow` and the exact public shape of `ExecutionHistory`. Both directly affect acceptance criteria and cross-service integration, so the subfeature is not actually closed. (PRD — dag-loader-runner)
- [medium] The system-design overview says the artifact was written and validated with `jq`, but the actual body is empty. That completeness claim is misleading and masks missing design work. (SYSTEM-DESIGN — dag-loader-runner)
- [medium] Key citations are not reviewable from the supplied artifact set. The PRD/design depend on D-GR-22/23/24/25 and code references such as `.iriai/artifacts/features/beced7b1/plan-review-discussion-4.md`, `.iriai/artifacts/features/beced7b1/broad/architecture.md`, and `iriai-compose/iriai_compose/runner.py`, but those source artifacts are not actually present in context for verification. (PRD — dag-loader-runner)
- [gap/high] No plan work covers REQ-1/REQ-12 live-model validation and `/api/schema/workflow` contract/documentation. (Requirement coverage)
- [gap/high] No plan work covers REQ-2/REQ-3 recursive nested phase loading and DAG construction across workflow and child containers. (Requirement coverage)
- [gap/high] No plan work covers REQ-4/REQ-5/REQ-11 hook-edge classification, stale-field rejection, and actionable field-path validation errors. (Requirement coverage)
- [gap/high] No plan work covers REQ-6/REQ-7 canonical Ask/Branch/Plugin dispatch and recursive sequential/map/fold/loop phase execution. (Requirement coverage)
- [gap/high] No plan work covers REQ-8 hierarchical context propagation and non-breaking `AgentRuntime.invoke()` compatibility. (Requirement coverage)
- [gap/high] No plan work covers REQ-9 public `validate(workflow)` and `run(workflow, config, *, inputs=None)` API delivery. (Requirement coverage)
- [gap/high] No plan work covers REQ-10/REQ-13 observability outputs and additive backward-compatible namespace behavior. (Requirement coverage)
- [gap/medium] No plan work covers REQ-14 external runtime/MCP ownership boundaries. (Requirement coverage)
- [gap/high] J-1 has no verification block, test IDs, or mapped files. (Journey verification)
- [gap/high] J-2 has no verification block, test IDs, or mapped files. (Journey verification)
- [gap/high] J-3 has no verification block, test IDs, or mapped files. (Journey verification)
- [gap/high] J-4 has no verification block, test IDs, or mapped files. (Journey verification)
- [gap/high] `Recursive workflow loader`, `Container-local execution graph builder`, `Hook edge classifier`, `Validation facade`, and `Schema export bridge` have no implementation tasks or file mappings. (Design component coverage)
- [gap/high] No risk/dependency entries address upstream SF-1/SF-3/SF-5/SF-6 contract reconciliation, despite the PRD explicitly depending on those subfeatures. (Architecture coverage)
**security** [FAIL]: Security review fails. The SF-2 artifacts declare the subfeature complete, but the plan is effectively empty and does not implement or verify the PRD security profile. The most serious omission is the missing expression-evaluation security contract for runner-executed workflow code.

- [high] `PLAN` is marked complete while `steps`, `implementation_steps`, `journey_verifications`, `dependencies`, `risks`, and `architectural_risks` are all empty. That leaves no implementable or testable coverage for any security-profile requirement in this subfeature. (PLAN — dag-loader-runner)
- [critical] The runner still permits executable workflow expressions (`transform_fn` and mode/branch expressions), but the SF-1 expression-evaluation security contract is missing from the SF-2 artifacts. There is no planned AST allowlist, builtin blocking, size/depth limit, timeout, or scoped-evaluation control, which leaves arbitrary code-execution risk unaddressed. (DESIGN — dag-loader-runner)
- [high] The PRD security mitigation depends on rejecting stale wire formats and keeping hook behavior explicit and inspectable, but there is no implementation task to hard-fail `edge.port_type`, separate hook sections, or transforms on hook edges. Ambiguous or hidden execution behavior could pass validation. (PRD — dag-loader-runner)
- [high] `/api/schema/workflow` is treated as a canonical interface, but `SYSTEM-DESIGN` leaves `api_endpoints` empty, the PRD leaves authentication as an open question, and `PLAN.dependencies` is empty. There is no tracked owner or requirement for auth enforcement on the endpoint. (SYSTEM-DESIGN — dag-loader-runner)
- [medium] No rate-limiting or abuse-control requirement is attached to `/api/schema/workflow`. If the unresolved auth question results in anonymous access, the endpoint can be scraped or hammered without any planned protection. (PRD — dag-loader-runner)
- [medium] The browser-consumed schema endpoint has no explicit origin/CORS contract in the SF-2 artifacts. Because endpoint security is delegated cross-feature and untracked here, there is no guarantee the endpoint will inherit the expected browser-access restrictions when shipped. (PRD — dag-loader-runner)
- [high] REQ-14 limits third-party exposure to configured plugin runtimes or external stdio MCP servers, but the plan contains no runtime-reference validation, allowlist enforcement, or negative tests to reject inline transport/process ownership. The runner's network/process attack surface is therefore unspecified. (PRD — dag-loader-runner)
- [medium] Sensitive data handling is underspecified. The PRD classifies workflow definitions, prompts, and execution metadata as sensitive and leaves the public `ExecutionHistory` shape open, but the plan has no minimization, redaction, or retention-control tasks for `ExecutionResult`/`ExecutionHistory` payloads. (PRD — dag-loader-runner)
- [medium] The PRD's mitigation requires the loader, validator, and `/api/schema/workflow` export to share one model source, yet there is no dependency or verification task ensuring `validate()`, `run()`, and schema export stay locked to the same models. Validation drift would weaken input validation and contract enforcement. (PLAN — dag-loader-runner)
- [gap/high] The plan is marked complete without any implementation, dependency, risk, or verification items, so security requirements have no executable coverage. (planning)
- [gap/critical] Runner-side expression sandboxing from the SF-1 contract is entirely missing despite executable workflow expressions remaining in scope. (code-execution)
- [gap/high] No planned validation rejects deprecated `edge.port_type`, separate hooks metadata, or transforms on hook edges. (input-validation)
- [gap/high] Authentication for `/api/schema/workflow` is unresolved and delegated outside scope without a tracked dependency or acceptance gate. (auth)
- [gap/medium] The canonical schema endpoint has no rate-limit or abuse-control requirement. (rate-limiting)
- [gap/medium] No explicit CORS/origin contract exists for the browser-consumed schema endpoint. (cors)
- [gap/high] The plan does not enforce that integrations come only from configured runtimes or external stdio servers, leaving process/network ownership undefined. (third-party-exposure)
- [gap/medium] Sensitive prompt and execution metadata may be exposed through `ExecutionResult`/`ExecutionHistory`, but no minimization, redaction, or retention controls are planned. (data-exposure)
- [gap/medium] No verification ensures loader, validator, and schema export use the same live model source, allowing schema drift to bypass intended validation rules. (validation-integrity)

### declarative-schema [FAIL]

**completeness** [FAIL]: Not approved. The SF-1 bundle is still split between a D-GR-22 PRD and stale implementation artifacts: the plan changes the actor and root-schema contracts, never actually plans the canonical `/api/schema/workflow` delivery path, leaves multiple acceptance criteria and journeys unverified, and the system-design artifact remains pre-D-GR-22 in several core areas (`workflow-schema.json` runtime source, `phases` recursion, stale root/entity shapes).

- [high] STEP-1 rewrites `ActorDefinition` to `type: 'agent'|'interaction'` with `resolver`, while PRD REQ-2, AC-13, and the data entities require a discriminated `actor_type: 'agent'|'human'` union with `identity`/`channel` on the human branch. This is a direct wire-contract change, not an implementation detail. (PLAN — declarative-schema)
- [high] STEP-1 leaves hook ports typed as `any` and never defines the PRD's `HookPortEvent` payload model. PRD REQ-15/REQ-22 explicitly apply typed-port rules to hook ports, and the data entities introduce `HookPortEvent` as the hook payload contract; the plan defers that contract instead of implementing it. (PLAN — declarative-schema)
- [high] STEP-1/STEP-2 introduce schema surface not present in the approved PRD contract: `stores`, `plugin_instances`, workflow-level `inputs`/`outputs`, `context_text`, and node/phase `input_type`/`output_type` hints. REQ-1 gives an explicit top-level field list, and these additions materially expand the serialized schema without PRD approval. (PLAN — declarative-schema)
- [medium] STEP-2 adds BranchNode `merge_function`, but PRD REQ-3 and AC-23 through AC-26 define the branch contract as `condition_type` + `condition` + `paths` and explicitly reject alternate routing surfaces. This reintroduces unapproved branch behavior. (PLAN — declarative-schema)
- [high] No implementation step actually covers the canonical composer delivery path from REQ-16/AC-16/J-4/J-6. STEP-2 only generates a local schema dict and optional `workflow-schema.json`; nothing in the plan modifies backend/frontend code to serve or consume `/api/schema/workflow` at runtime. (PLAN — declarative-schema)
- [high] The plan's verification matrix stops at J-5. PRD J-6 (schema endpoint failure must not silently fall back) and J-7 (stale migration output must fail fast and be corrected) have no verification blocks, and J-4 is only reduced to local `generate_json_schema()` checks rather than endpoint behavior. (PLAN — declarative-schema)
- [medium] STEP-4 never adds a negative fixture for the stale flat-shape case from REQ-17, AC-1, and J-3 step 1. There is no test proving that root-level `nodes` are rejected after the D-GR-22 nested containment rewrite. (PLAN — declarative-schema)
- [medium] AC-14 requires explicit rejection of inter-phase cycles outside loop mode, but STEP-4 and the journey verifications never add a failing cycle fixture or a verification block for `detect_cycles()`. The behavior exists only as planned code, not as proved coverage. (PLAN — declarative-schema)
- [high] REQ-21 and AC-18 require the full expression sandbox contract to be specified at the schema/runtime boundary: AST allowlist, blocked builtins, timeout, size/complexity bounds, and explicit scope contexts, plus the `output_field` carve-out. STEP-3 only mentions expression-length checks and otherwise defers evaluation details to SF-2, so the artifact is not actionable for downstream implementers. (PLAN — declarative-schema)
- [medium] REQ-19 and AC-11 require the planning, develop, and bugfix workflows to remain fully representable. STEP-4 only proposes one `pm_fold_map_loop.yaml` fixture inspired by planning and bugfix patterns; there is no develop-specific fixture or verification proving the full litmus trio. (PLAN — declarative-schema)
- [medium] STEP-2 creates `templates.py` and `plugins.py`, but STEP-4 provides no template or plugin-interface fixtures/verification blocks. REQ-9 and REQ-10 are therefore implemented on paper but not proved by the test plan. (PLAN — declarative-schema)
- [medium] The design artifact annotates J-1 'Step 9', but PRD J-1 only has 8 steps. That breaks traceability from design annotations back to the approved journey source. (DESIGN — declarative-schema)
- [medium] The design/plan depend on unresolved references. `D-GATE-2` and `D-GR-23` are not defined anywhere in the provided artifact set, and multiple citations point to out-of-scope files such as `plan-review-discussion-4.md` that are not included here. Those references cannot be reviewed or validated from the supplied context. (DESIGN — declarative-schema)
- [high] PRD cross-service impacts explicitly say stale downstream SF-3/SF-4/SF-5/SF-6 wording and fixtures must be updated, but the plan's file manifest and `files_to_modify` only touch `iriai-compose` files. The broader contract-alignment objective that motivated D-GR-22 is therefore not actually planned. (PLAN — declarative-schema)
- [high] The system-design artifact still makes `workflow-schema.json` the editor-facing source of truth via SVC-4, SVC-8, related connections, and CP-2. That directly contradicts PRD REQ-16, the design rationale, and the plan's own 'build/test only' stance for the static file. (SYSTEM-DESIGN — declarative-schema)
- [high] The system-design entity model still recurses phases through `PhaseDefinition.phases`, and its entity relations/call paths depend on that stale field. PRD REQ-5/REQ-20 and the revised design/plan require `children` as the only canonical recursive field. (SYSTEM-DESIGN — declarative-schema)
- [high] The system-design root/entity contract is materially stale: `WorkflowConfig` omits required PRD fields such as `workflow_version`, `metadata`, and `cost_config`, adds list-based `inputs`/`outputs` with `name` fields, and `PhaseDefinition` omits required fields like `name`, `metadata`, and `cost`. This is not a serialization-compatible variant of the approved PRD. (SYSTEM-DESIGN — declarative-schema)
- [high] The system-design artifact repeats the same actor-model split as the plan by defining `type: 'agent'|'interaction'` with `resolver`, instead of the PRD's `actor_type: 'agent'|'human'` union with `identity`/`channel`. The subfeature still lacks one stable actor contract. (SYSTEM-DESIGN — declarative-schema)
- [medium] System-design traceability is broken because services, connections, and call paths reference journeys J-8 through J-16, but the approved PRD only defines J-1 through J-7. Large parts of the artifact cannot be traced back to the current journey set. (SYSTEM-DESIGN — declarative-schema)
- [medium] The validation API is inconsistent across artifacts. PLAN STEP-3 declares one 26-code list, SYSTEM-DESIGN call paths return a different set of codes (`invalid_plugin_ref`, `invalid_branch_config`, `invalid_branch_outputs`, `missing_required_field`, etc.), and the provided SF-3/SF-7 summaries still consume a 21-code SF-1 contract. The exported validation surface is not stable. (SYSTEM-DESIGN — declarative-schema)
- [medium] The mode-config wire shape is inconsistent across SF-1 artifacts. PRD data entities define `mode_config: Optional[ModeConfig]`, while PLAN STEP-2 and SYSTEM-DESIGN serialize separate `sequential_config`/`map_config`/`fold_config`/`loop_config` fields. Consumers cannot infer the canonical serialized shape from the bundle. (PLAN/SYSTEM-DESIGN — declarative-schema)
- [gap/high] Actor union, hook payload typing, mode-config shape, and the root schema fields diverge across PRD, plan, and system-design. (Contract)
- [gap/high] The canonical composer runtime contract around `/api/schema/workflow` is required by the PRD but is not actually implemented or verified in the plan scope. (Delivery)
- [gap/high] The test/verification plan misses stale top-level nodes, cycle rejection, full sandbox coverage, template/plugin verification, and full planning/develop/bugfix litmus coverage. (Verification)
- [gap/medium] Cross-service cleanup promised in `cross_service_impacts` is not reflected in the plan's modification scope, so downstream stale artifacts remain unresolved. (Alignment)
- [gap/medium] Design/system-design references cannot be fully traced because of unresolved decision IDs, missing cited artifacts, and nonexistent journey IDs. (Traceability)
**security** [FAIL]: Not approved. The SF-1 artifacts leave the only explicit PRD auth requirement unimplemented, reintroduce an unauthenticated static schema distribution path, and add new secret-bearing/configuration surfaces without matching security controls. The expression-sandbox contract is also materially under-specified in the implementation plan.

- [high] The PRD security profile requires `/api/schema/workflow` to inherit the backend's authenticated API policy, but STEP-2 only says a backend route can return `generate_json_schema()` and includes no implementation task, acceptance criterion, or verification that the route is protected by backend auth/CORS middleware. The subfeature's primary security requirement is therefore not implemented in the plan. (PLAN — declarative-schema)
- [high] `SYSTEM-DESIGN` reintroduces `workflow-schema.json` as a runtime UI source via `json-schema-artifact` and the SVC-4 -> SVC-8 static import/HTTP flow. That is stale cross-feature contamination and directly bypasses the authenticated backend delivery path required by the PRD security profile, making internal workflow structure easy to expose as a public bundle/static asset. (SYSTEM-DESIGN — declarative-schema)
- [high] STEP-2 and `SYSTEM-DESIGN` add `PluginInstanceConfig` / `plugin_instances` with arbitrary `config` blobs even though the PRD root model only calls for plugin interfaces. No masking, encryption, secret-reference, or credential-separation work accompanies this addition, creating a new path for API keys or other secrets to be embedded in portable YAML/JSON contrary to the PRD's 'structure and types only; no embedded credentials or secrets' security posture. (PLAN — declarative-schema)
- [high] REQ-21's expression sandbox contract is not carried into implementation. STEP-3 preserves only expression-length checks; there is no task or test for AST allowlists, blocked builtins, complexity bounds, execution timeout, or defined evaluation scopes. `SYSTEM-DESIGN` further defers evaluation to a downstream runner. That leaves the security boundary under-specified for the consumers the PRD says should implement it from SF-1. (PLAN — declarative-schema)
- [medium] `SYSTEM-DESIGN` models internal helpers such as `load_workflow`, `dump_workflow`, and `validate_workflow` as GET/POST endpoints with `auth: N/A`. If implemented literally, that publishes parsing and validation surfaces without authentication. The artifact also specifies no rate limiting or abuse controls for those modeled endpoints. (SYSTEM-DESIGN — declarative-schema)
- [medium] The model/validation steps never require `extra='forbid'` or any denylist for credential-bearing fields on actor/plugin models. Fields such as `api_key`, `token`, or `password` are therefore not guaranteed to fail fast, despite the PRD's requirement that workflow YAML not carry environment-specific credentials or embedded secrets. (PLAN — declarative-schema)
- [medium] YAML parsing is hardened only to `yaml.safe_load`; there are no payload-size, nesting-depth, or similar resource bounds on `load_workflow()` / `load_workflow_lenient()`. Because these are public package entry points and are also described as import/HTTP surfaces, oversized or deeply nested YAML can still cause CPU or memory exhaustion. (PLAN — declarative-schema)
- [gap/high] Missing implementation and verification that the canonical `/api/schema/workflow` delivery path inherits backend authentication instead of being served anonymously or outside backend middleware. (auth)
- [gap/high] `SYSTEM-DESIGN` reintroduces a static frontend schema distribution path (`workflow-schema.json` via SVC-4/CP-2), which is both out-of-scope contamination and a direct bypass of the PRD's authenticated runtime delivery requirement. (contamination)
- [gap/high] The added `plugin_instances` / `PluginInstanceConfig.config` surface creates a new place for credentials to live in YAML without any masking, encryption, or secret-reference design. (secret-handling)
- [gap/high] The REQ-21 expression-evaluation security contract is not implemented beyond a size check, leaving downstream evaluators without the authoritative sandbox rules the PRD requires. (sandbox)
- [gap/medium] Internal schema helpers are modeled as unauthenticated HTTP endpoints with no rate limits, which could lead downstream implementers to expose parsing/validation APIs without protections. (auth-surface)
- [gap/medium] The plan does not require strict rejection of unexpected or credential-bearing fields, so secret-like keys are not guaranteed to fail fast. (validation)
- [gap/medium] YAML load paths lack payload-size and nesting/resource bounds, leaving the package vulnerable to oversized or deeply nested document exhaustion. (dos)

### libraries-registries [FAIL]

**completeness** [FAIL]: Rejected. The revised artifacts still mix old SF-7 numbering and stale scope with the new `workflow_entity_refs` contract. Critical gaps remain around actor-slot persistence, API alignment, schema/template delete coverage, migration scope, and acceptance verification.

- [high] Requirement traceability is broken. The plan and system design still map work to legacy IDs such as `REQ-8/9/10/14/15/16/17/18/19/20/21/22/33/34/35/39/42/50/51`, while the current PRD defines only `REQ-1..REQ-7`. System-design also cites the sidebar contract as `PRD REQ-2`, but current `REQ-2` is the pre-delete UX requirement, not sidebar structure. (PLAN / SYSTEM-DESIGN (libraries-registries))
- [high] Journey traceability is broken across artifacts. The PRD defines only `J-1..J-4`, but the design and plan use `J-5/J-6/J-8/J-16/J-17/J-20`, and the system design uses `J-23..J-29`. The verification blocks and call paths therefore do not trace back to the approved PRD journeys. (DESIGN / PLAN / SYSTEM-DESIGN (libraries-registries))
- [high] `REQ-4` is not planned end-to-end. The PRD requires an Alembic migration plus API persistence/return of `custom_task_templates.actor_slots`, but the plan only scopes frontend template-editor work. No migration, backend model/schema update, or template router work is assigned for actor-slot persistence. (PLAN (libraries-registries))
- [high] The system design contradicts `REQ-4` and `AC-2`: its `TaskTemplate` entity omits `actor_slots`, and its API inventory does not define template create/update responses that persist and return `actor_slots`. The documented backend contract cannot satisfy the PRD's actor-slot persistence requirement. (SYSTEM-DESIGN (libraries-registries))
- [high] Database migration scope is missing. The plan creates a new `Tool` model and depends on `workflow_entity_refs`, but no Alembic migration files are listed for the new `tools` table, the `actor_slots` column, or any SF-7 schema changes. The declared file scope cannot realize the stated data model. (PLAN (libraries-registries))
- [high] Schema and template delete coverage is incomplete. `REQ-1/REQ-2` apply to roles, schemas, and task templates, but the plan only scopes role list UI plus tool flows. There are no schema/template list or detail surfaces in scope to actually launch `EntityDeleteDialog`, and STEP-1 acceptance only mentions role/tool delete endpoints. (PLAN (libraries-registries))
- [high] `AC-1` is not verifiable from this plan. The acceptance criterion requires workflow saves to immediately clear stale reference blocks, but the plan treats workflow reindexing as an external SF-5 dependency and provides no workflow save/delete file scope or verification proving the post-save reopen transitions from blocked to confirm-delete. (PLAN (libraries-registries))
- [medium] `REQ-5` has no implementation or verification coverage. The PRD requires 500ms warm-cache loads, 2s cold loads, and TanStack stale-while-revalidate behavior for library pages, but the plan only specifies `staleTime: 0` for delete preflight checks, which does not address page-load performance. (PLAN (libraries-registries))
- [high] `REQ-6` is not traced into implementation or tests. The plan and system design mention Bearer auth, but they do not cover the required cross-user `404` behavior for library endpoints, nor do the verification blocks test tenancy scoping. (PLAN / SYSTEM-DESIGN (libraries-registries))
- [high] `REQ-7` and `AC-5` are not properly planned. The artifacts do not scope server-side payload-size enforcement, entity-name sanitization, `413/422` responses, or matching frontend guards across library entities. The only mention is tool-endpoint prose in system design, with no step ownership or verification. (PLAN / SYSTEM-DESIGN (libraries-registries))
- [high] The delete-dialog contract is internally inconsistent. Design and plan require workflow-backed delete dialogs to show SF-1 validation-code chips, but the PRD only requires listing referencing workflows, and the system-design references endpoint returns only `total` plus `referenced_by`. This adds unplanned UI/API scope with no documented backend payload. (DESIGN / PLAN / SYSTEM-DESIGN (libraries-registries))
- [high] The tool delete contract is inconsistent across artifacts. Design and plan assume a preflight `GET /api/tools/references/{id}`, but system design never defines that endpoint and call path `CP-5` still issues `DELETE /api/tools/{id}` as the first request. The implementation target is ambiguous. (DESIGN / PLAN / SYSTEM-DESIGN (libraries-registries))
- [medium] STEP-8 and the design's promotion annotations are scope creep. `PromotionDialog` plus `AskInspector` and `NodePalette` changes are not covered by the current PRD requirements or acceptance criteria, which focus on delete/reference safety, tool CRUD, and actor-slot persistence. (PLAN / DESIGN (libraries-registries))
- [medium] `CMP-104 PluginNodePrimitive` is contamination from a different scope. The current PRD is about libraries/registries and explicitly keeps plugin-library scope out, yet the design still carries forward D-60 plugin cleanup and defines a plugin primitive with no corresponding implementation task in the plan. (DESIGN (libraries-registries))
- [medium] The PRD still marks key tool decisions as open, but downstream artifacts implement them as settled behavior. System design hardcodes built-in tool catalog handling in backend constants and keeps tool references name-based in `Role.tools`, even though both topics remain open questions in the PRD. (PRD / SYSTEM-DESIGN (libraries-registries))
- [high] The documented API surface is incomplete for the PRD journeys. System design omits the role/schema/template list/get/create/update endpoints needed for library CRUD journeys, and template endpoints do not expose `actor_slots`. `J-1` and `J-4` cannot be satisfied from the published API inventory. (SYSTEM-DESIGN (libraries-registries))
- [medium] Traceability citations are stale. The PRD and design still cite `libraries-registries/plan.md`, but the revised artifact under review is declared as `plan-revised.json`. Those citations no longer resolve to the current plan artifact. (PRD / DESIGN (libraries-registries))
- [medium] The PRD relies on unresolved citations. It references `D-GR-7`, `R6-1`, and `D-11`, but none of those decisions are defined in the provided artifact set or cross-feature summaries. Requirements for tool CRUD and actor-slot persistence therefore depend on missing authority. (PRD (libraries-registries))
- [medium] The design relies on unresolved citations. `CMP-104` cites `D-60` and `SF-5 Plan D-A11`, but neither appears in the provided context; the SF-5 plan summary only exposes `D-A1` through `D-A10`. The component is not grounded in a resolvable decision log. (DESIGN (libraries-registries))
- [medium] The plan's step ownership does not match its file manifest. `roles.py`, `schemas.py`, and `templates.py` are listed under `files_to_modify`, but STEP-1 scope only names `tool.py`, `references.py`, and `tools.py`. The role/schema/template delete-guard backend work is not attached to a concrete step or step-level acceptance criteria. (PLAN (libraries-registries))
- [high] Acceptance verification coverage is insufficient. The plan's `journey_verifications` only cover stale `J-8` and `J-17` delete paths; there is no verification block for `AC-2` actor-slot reload, `AC-3` tool edit invalidation into the Role editor, or the `AC-4` requirement that deleted tools disappear from later checklist renders. (PLAN (libraries-registries))
- [high] The tool source contract is inconsistent. System design says `GET /api/tools` returns built-in tools with `source="built_in"`, but both the PRD Tool entity and the system-design Tool table constrain `source` to `mcp | custom_function`. Unless a separate response union is defined, the API shape contradicts its own schema. (SYSTEM-DESIGN (libraries-registries))
- [medium] Actor-slot serialization remains underspecified even though implementation is assumed. The PRD still leaves the YAML shape for template actor-slot bindings as an open question, and neither plan nor system design resolves that serialization contract. Consistent persistence and reload behavior cannot be guaranteed. (PRD / PLAN / SYSTEM-DESIGN (libraries-registries))
- [medium] Component naming is inconsistent across artifacts. System design describes the third picker as `TemplatePicker`, while the plan and PRD references use `TemplateBrowser`. That mismatch will leak into imports, route wiring, and traceability. (SYSTEM-DESIGN / PLAN (libraries-registries))
- [gap/high] Requirement IDs, journey IDs, and citations still point at legacy SF-7 artifacts instead of the current revised PRD/plan set. (Traceability)
- [gap/high] No concrete migration/model/router scope exists for `actor_slots`, and no migration scope exists for the new tools table. (Backend persistence)
- [gap/high] The documented API surface is missing required CRUD endpoints and uses conflicting delete/reference contracts for tools. (API contract)
- [gap/high] Multiple acceptance criteria remain unverifiable, especially actor-slot round-trip, post-save reference freshness, and tool checklist invalidation. (Verification)
- [gap/high] Cross-user `404`, payload-size/name validation, and performance targets are not mapped to concrete implementation or tests. (Security/NFR)
- [gap/medium] Promotion-flow and plugin-primitive work persists even though the revised PRD does not trace it as current scope. (Scope control)
**security** [FAIL]: The revised SF-7 artifacts fix the workflow-backed delete preflight flow, but they do not carry the PRD's security contract through implementation. Auth enforcement, tenant isolation, 404-on-cross-user behavior, 413/422 validation, rate limiting, and migration rollback are mostly implied or missing rather than planned and verified.

- [high] STEP-1 creates `routers/tools.py` and modifies `routers/references.py`; `files_to_modify` also includes `roles.py`, `schemas.py`, and `templates.py`, but no step or acceptance criterion requires attaching JWT auth dependencies or verifying unauthorized access behavior. The system design's `auth: JWT Bearer` labels are declarative only, so PRD REQ-6 has no explicit implementation task. (PLAN — libraries-registries)
- [high] No plan item or journey verification requires queries to be scoped by JWT `sub` and to return 404 on cross-user access. `/api/tools/{id}`, `/api/{entity}/references/{id}`, and the delete routes therefore have no verified protection against tenant enumeration via ids, workflow names, or role names. (PLAN — libraries-registries)
- [high] PRD REQ-7 and AC-5 require server-side JSON payload size limits with 413 responses, but STEP-1/4/5/6 never add request-size validators or middleware for `input_schema`, `json_schema`, `metadata`, `actor_slots`, or other JSON-bearing bodies. Oversized payload handling is unplanned. (PLAN — libraries-registries)
- [high] The plan only covers duplicate-name availability checks. It does not define any backend entity-name sanitization rule or 422 rejection path for roles, schemas, templates, or tools, even though PRD REQ-7 makes name sanitization a must-have security control. (PLAN — libraries-registries)
- [high] Tool delete preflight is internally inconsistent. STEP-4 and J-17 require `GET /api/tools/references/{id}` before delete, but the system-design API inventory omits that endpoint entirely and call path CP-5 still sends `DELETE /api/tools/{id}` as the first request. That reintroduces destructive probing for tool references and leaves auth/scoping/rate-limit/error semantics undefined. (SYSTEM-DESIGN — libraries-registries)
- [medium] Cross-service impacts call for Alembic migrations for `tools`, `workflow_entity_refs`, and `actor_slots`, but the file manifest contains no migration files and no downgrade or rollback steps. That makes the schema changes operationally unsafe and unreviewable from a security/recovery standpoint. (PLAN — libraries-registries)
- [medium] REQ-7 requires matching frontend guards, but the frontend steps only build views and hooks. There is no task to block oversize payloads or invalid names client-side, nor any acceptance coverage for the required 413/422 UX in editors or promotion flows. (PLAN — libraries-registries)
- [medium] The new lookup endpoints (`/api/*/check-name`, `/api/{entity}/references/{id}`, and the planned `/api/tools/references/{id}`) have no SF-7 rate-limit task or verification. Given their enumeration and relationship-scan value, relying on inherited SF-5 defaults implicitly is a gap. (SYSTEM-DESIGN — libraries-registries)
- [medium] The PRD security profile promises 30-day recoverability for soft-deleted library entities, but the plan never verifies that the new Tool routes perform soft delete, hide deleted rows from normal reads, and retain them for the stated window. The modal's recoverability copy is not backed by implementation work. (PLAN — libraries-registries)
- [medium] Security traceability is broken: STEP requirement mappings cite stale IDs such as `REQ-33`, `REQ-35`, and `REQ-51`, and none of the steps explicitly map to the current PRD's REQ-6, REQ-7, or AC-5. That makes it impossible to prove the required auth and validation controls are actually scheduled. (PLAN — libraries-registries)
- [low] The design artifact spends scope on out-of-scope `PluginNodePrimitive` / D-60 cleanup. That is contamination under the review instructions and displaces detail that should have been used to specify the current subfeature's missing auth and validation behavior. (DESIGN — libraries-registries)
- [gap/high] No implementation task or verification for JWT auth dependencies on newly created or modified library routers. (authentication)
- [gap/high] No explicit tenant-scoping or 404-on-cross-user coverage for detail, reference, and delete endpoints. (authorization)
- [gap/high] Server-side payload-size enforcement and 413 responses required by AC-5 are missing. (input_validation)
- [gap/high] Backend entity-name sanitization and 422 responses across library entities are missing. (input_validation)
- [gap/medium] No client-side guards or 413/422 UX handling for oversize or invalid inputs. (frontend_validation)
- [gap/medium] Lookup endpoints that enable enumeration or repeated reference scans have no explicit rate-limit policy in this subfeature. (rate_limiting)
- [gap/high] `GET /api/tools/references/{id}` is required by plan/design but absent from the system-design API inventory, while CP-5 still uses direct DELETE probing. (api_contract)
- [gap/medium] Alembic upgrade/downgrade artifacts are missing for the new Tool table, canonical reference table adoption, and actor-slot persistence. (migrations)
- [gap/medium] The 30-day soft-delete recovery promise is not backed by implementation or verification for Tool deletion. (data_retention)
- [gap/low] Out-of-scope plugin primitive material remains in the design artifact instead of current-scope security detail. (scope_contamination)

### testing-framework [FAIL]

**completeness** [FAIL]: Not approved. The planning set is still centered on the superseded `AgentRuntime.invoke(..., node_id=...)` contract, while the PRD/design/system-design now require unchanged `invoke()` plus ContextVar-based node propagation and canonical merge order. Traceability is also broken: plan/system-design use obsolete requirement and journey IDs, several designed APIs/components have no implementation path, and current acceptance criteria are not verifiable from the plan.

- [high] The plan still treats `AgentRuntime.invoke(..., node_id=...)` as the canonical SF-2/SF-3 boundary. That directly violates PRD REQ-1/REQ-3/REQ-4 and the revised non-breaking contract; implementing from this plan would reintroduce the breaking interface the PRD explicitly marks stale. (PLAN — testing-framework)
- [high] REQ-2 and AC-3 have no compliant implementation path. STEP-2 explicitly rejects hidden/context-based node lookup, and nowhere in the plan is there work to consume a runner-owned ContextVar or verify hierarchical context merging in `workflow -> phase -> actor -> node` order. (PLAN — testing-framework)
- [high] Requirement and journey traceability is stale throughout the plan. Steps and verifications reference obsolete IDs (`R8`, `R13`, `R14`, `R23`, `J-7`..`J-10`) instead of the current PRD IDs (`REQ-1`..`REQ-4`, `J-1`, `J-2`), so there is no auditable mapping from the current PRD to implementation. (PLAN — testing-framework)
- [high] PRD REQ-4/J-2 require stale `node_id` plan language to be removed before implementation proceeds, but this artifact is itself still non-compliant and includes no remediation step or exit criterion for cleaning the plan. The failure-path journey is therefore unresolved in the planning artifact itself. (PLAN — testing-framework)
- [high] Design/system-design both include `MockPluginRuntime` and plugin-node test-double behavior, but the plan never creates `mock_plugin.py`, never schedules plugin matcher implementation, and never adds plugin-runtime tests. CMP-3 and related API/entity surface are unplanned. (PLAN — testing-framework)
- [high] The planned mock runtime API and data model diverge from the PRD/design contract. STEP-2 introduces `MockRuntime`/`MockInteraction`, a generic `.when(node_id=None, role=...)`, `.handle()`, `.with_default()`, and ad hoc call dicts, instead of `MockAgentRuntime`/`MockCall` with `when_node()`, `when_role()`, `default_response()`, and ContextVar-derived `node_id` capture. (PLAN — testing-framework)
- [high] Several designed capabilities have no implementation task: `role+prompt` matching, `respond_with(prompt, context)`, `respond_sequence()`, `raise_error()`, `then_crash()`, `with_cost()`, `MockConfigurationError`, `MockExhaustedError`, and `assert_loop_iterations()` appear in design/system-design entities and call paths but are absent from the plan steps. (PLAN — testing-framework)
- [medium] The interaction mock contract is still on the old constructor-driven model. STEP-2 acceptance uses `MockInteraction(approve=False)`, while design/system-design require a no-arg `MockInteractionRuntime` with fluent `when_node()` matching. Consumers cannot infer the intended public API. (PLAN — testing-framework)
- [high] `run_test()` is not stable across artifacts. PLAN STEP-6 defines `runtime`, `interaction`, `plugins`, `inputs`, `feature_id`, and `history`, while SYSTEM-DESIGN documents `plugin`, `artifacts`, `plugins`, `inputs`, and no `history` parameter. This is a direct public-API contradiction. (PLAN — testing-framework)
- [medium] `WorkflowBuilder.build()` behavior is contradictory. PLAN STEP-3 explicitly says not to call `validate_workflow()` from `build()`, but SYSTEM-DESIGN defines `build()` as validating and surfacing `ValidationError`. Test authors would get different semantics depending on which artifact they follow. (PLAN — testing-framework)
- [medium] SYSTEM-DESIGN is still mapped to obsolete journeys `J-7`..`J-10`, none of which exist in the current PRD. Service definitions, call paths, and entity journey mappings therefore cannot be traced back to the approved PRD journey model. (SYSTEM-DESIGN — testing-framework)
- [medium] Citation hygiene is broken. The plan still cites stale PRD revisions (`R13`/`R14`), out-of-scope review docs (`plan-review-cycle-3.md`, `plan-review-discussion-2.md`), and even self-cites the plan as 'research'. Those references do not resolve within the current review scope and cannot support the claimed decisions. (PLAN — testing-framework)
- [high] Scope is not rebased to the current PRD. The PRD marks new testing capabilities beyond the R16 contract correction as out of scope, yet the plan/system-design still schedule the broader framework build-out: builders, validation re-exports, YAML snapshots, loop helpers, many fixtures, and extensive new test surfaces. (PLAN — testing-framework)
- [medium] Current acceptance coverage is not verifiable from the plan. Journey verifications target obsolete journeys, do not verify PRD J-2 at all, and do not prove AC-3's merge-order behavior or the removal of stale `node_id` assumptions from the planning artifact. (PLAN — testing-framework)
- [medium] Component/file ownership is inconsistent. DESIGN places CMP-1/CMP-2 in `iriai_compose/runner.py` and CMP-3 in `iriai_compose/testing/mock_plugin.py`, while the plan creates only `iriai_compose/testing/mock_runtime.py`-based mocks and never schedules the plugin file. Implementation location is ambiguous before coding starts. (DESIGN — testing-framework)
- [gap/high] No compliant implementation path exists for ContextVar-based node routing or canonical context merge order; the plan still specifies the rejected `node_id` kwarg boundary. (Contract)
- [gap/high] Plan and system-design trace to obsolete requirement/journey taxonomies rather than the current PRD, so requirement coverage and acceptance evidence cannot be audited. (Traceability)
- [gap/high] Designed surfaces such as `MockPluginRuntime`, prompt-aware matcher APIs, error/exhaustion handling, and loop-iteration assertions have no implementation steps or file coverage. (Implementation Coverage)
- [gap/high] The plan does not verify PRD J-2, does not verify AC-3 merge-order behavior, and does not include an exit criterion proving stale `node_id` plan text was removed. (Verification)
- [gap/medium] Downstream artifacts still describe a broad SF-3 build-out even though the current PRD limits this revision to contract correction. (Scope)
- [gap/medium] Decision rationale depends on stale or out-of-scope sources, including obsolete PRD revision labels, external review documents, and self-citations. (Citation Hygiene)
**security** [FAIL]: Blocked. The SF-3 plan does not implement the PRD security profile for synthetic-only test data and no-third-party exposure, and it leaves multiple leakage and isolation paths unguarded.

- [high] The PRD security profile says mocks, fixtures, and snapshots should use public synthetic data with no PII, but the plan only adds fixtures, snapshots, and call recording. It never adds any secret/PII review gate, redaction utility, or fixture-policy enforcement, so real prompts or credentials can be committed or emitted in test artifacts without detection. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [high] STEP-2 requires mock call recording of raw `prompt`, `workspace`, and `session_key`, and the design/system-design require rich diagnostics for matcher failures. There is no masking or truncation task for these fields, so assertion failures and CI logs can leak prompt contents, workspace paths, session identifiers, and other sensitive execution data. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [high] Design and system-design make `MockPluginRuntime` the safe plugin isolation boundary, but the plan never implements it. That leaves plugin-node tests without an in-scope mock and pushes them toward real plugin registries or ad hoc doubles, which conflicts with the PRD security profile's `third_party_exposure: None` expectation. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [high] STEP-6 forwards caller-supplied `plugins` directly into `RuntimeConfig` and does not require in-memory artifact/session stores or any no-network default. Combined with the missing plugin mock, `run_test()` can execute real plugins or touch shared stores/workspaces, creating outbound-call and data-leakage paths that the PRD explicitly says should not be required. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [medium] The system design exposes full merged invocation context to `respond_with(prompt, context)` handlers and records raw plugin `inputs` in mock call history, but the plan has no data-minimization or redaction rule for those payloads. One accidental real secret in workflow/phase/actor/node context becomes available to arbitrary callbacks and debug output. (SYSTEM-DESIGN — testing-framework)
- [medium] STEP-7 publishes `assert_yaml_round_trip(path)` as a public helper that reads an arbitrary filesystem path and emits unified diffs of file contents on failure, but the plan adds no fixture-root restriction, path normalization rule, or diff redaction. A mispointed test can disclose unrelated local files or secrets in CI output. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [medium] `run_test()` defaults `feature_id` to the constant `"test"` and the plan does not require per-test namespacing or isolated stores/checkpoints. If a persistent or shared store implementation is supplied, histories and artifacts from separate tests can collide under the same identifier and bleed data across runs. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [medium] STEP-8 adds regression tests for interface drift, but none verify the PRD security invariants: no external/plugin calls, no sensitive data persisted in mock histories or diagnostics, and fixtures remaining synthetic-only. The intended security posture is therefore neither implemented nor pinned by tests. (/Users/danielzhang/src/iriai/.iriai/artifacts/features/beced7b1/subfeatures/testing-framework/plan.md)
- [gap/high] No step enforces synthetic-only test inputs or redacts prompts, session keys, workspace paths, `Pending` payloads, or plugin inputs before they are stored or printed. (PII handling)
- [gap/high] Plugin-node isolation is incomplete because `MockPluginRuntime` exists in design/system-design but not in the implementation plan, while `run_test()` still accepts live plugin registries. (External-call isolation)
- [gap/medium] Full hierarchical invocation context is exposed to callbacks and diagnostics with no filtering or masking policy. (Data minimization)
- [gap/medium] Filesystem snapshot helpers do not constrain input paths to approved fixture roots and can emit raw file diffs. (Input validation)
- [gap/medium] Static `feature_id` defaults and missing mandatory in-memory stores allow cross-test state leakage when shared stores/checkpoints are supplied. (Test isolation)
- [gap/medium] The self-test plan does not include checks for secret leakage, synthetic-data compliance, or accidental outbound plugin calls. (Security verification)

### workflow-editor [FAIL]

**completeness** [FAIL]: Rejected. The workflow-editor artifacts capture the D-GR-22 intent at a high level, but traceability is broken, the persistence/runtime contract is internally inconsistent, the plan targets the wrong application, and the verification story is largely non-executable.

- [high] Step-to-requirement traceability is broken. The plan tags work to REQ-13/REQ-14/REQ-15, but the PRD defines only REQ-1 through REQ-13. REQ-14 and REQ-15 do not exist, and the existing requirements are not mapped in a way that can be audited. (PLAN — workflow-editor; PRD — workflow-editor)
- [high] Journey traceability is broken. The plan and journey verification sections use J-16/J-17/J-20/J-21/J-22, while the PRD defines only J-1 through J-4. None of the PRD journeys has a matching verification block. (PLAN — workflow-editor; PRD — workflow-editor)
- [high] Design journey annotations are stale and misbound. They introduce J-8, reuse J-4 for hook-edge behavior even though PRD J-4 is schema-endpoint failure, and reference step numbers that exceed the PRD journey step counts. The design walkthrough no longer traces back to the PRD journeys. (DESIGN — workflow-editor; PRD — workflow-editor)
- [high] The implementation target is the wrong application. The plan creates the editor in `tools/iriai-workflows/frontend` and assumes `tools/iriai-workflows/backend`, but the provided SF-5 summaries place the composer in `platform/compose/frontend` and `platform/compose/backend`, with the tools hub as a separate static app. This would land SF-6 in the wrong product/repo. (PLAN — workflow-editor; composer-app-foundation summaries)
- [high] Schema bootstrap behavior contradicts the PRD and design. PRD AC-1/AC-10 and design CMP-69 require a blocking gate when `/api/schema/workflow` is unavailable, but the plan architecture and STEP-15 explicitly keep a view-only canvas with a retry banner. That reintroduces the partially usable state the PRD forbids. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [high] AC-7's synthetic root phase behavior is missing. The PRD requires loose top-level nodes to normalize under a synthetic root phase on save, but neither the design nor the serializer steps define synthetic-root creation, serialization, or reload behavior. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [high] STEP-1 under-specifies the persisted contract. It defines `WorkflowDefinition` with only `phases` and `edges`, and `PhaseDefinition` with only `nodes`, `edges`, and `children`. That omits required persisted fields from the PRD such as `schema_version`, `name`, `description`, `actors`, `types`, `plugins`, `plugin_instances`, `stores`, `context_keys`, `context_text`, plus phase `id`, `mode`, configs, ports, and positions. Lossless round-trip cannot hold with this type model. (PLAN — workflow-editor; PRD — workflow-editor)
- [high] The branch-editing model drifts back to a removed API. The plan adds `SwitchFunctionEditor.tsx` and `branch-inspector-{id}-switch-editor`, but the design and cited D-GR-12 model Branch as per-port conditions with optional `merge_function` and explicitly say 'No switch_function'. (PLAN — workflow-editor; DESIGN — workflow-editor)
- [high] The design reintroduces a fourth atomic node type. CMP-63 says ErrorNode is 'the fourth atomic node type', which directly contradicts PRD REQ-2/D-7 limiting direct node placement to Ask, Branch, and Plugin. (DESIGN — workflow-editor; PRD — workflow-editor)
- [medium] The plan expands into template authoring/promotion scope that is not justified by the PRD. `TemplateFlowNode`, `SaveAsTemplateDialog`, `PromotionDialog`, detach flows, and read-only template-child mutation rules are added even though no workflow-editor requirement or journey covers template creation/promotion, and SF-7 owns that domain in the provided summaries. (PLAN — workflow-editor; SF-7 summaries)
- [high] Several design components have no implementation task in the plan: CMP-63 `ErrorFlowNode`, CMP-65 `MergeFunctionEditor`, CMP-66 `PortConditionRow`, and CMP-69 `SchemaBootstrapGate`. The plan cannot realize the design as written. (DESIGN — workflow-editor; PLAN — workflow-editor)
- [medium] Component ID traceability is broken. The PRD out-of-scope list explicitly removes 'MiniCanvasThumbnail / CMP-64', but the design reuses CMP-64 for `OutputPathsEditor`. Reassigning a retired component ID makes historical citations ambiguous. (PRD — workflow-editor; DESIGN — workflow-editor)
- [high] The verification plan is non-executable as scoped. The plan defines detailed browser/API verification blocks and a large `data-testid` registry, but the file manifest includes no unit, integration, or E2E test files and no harness updates. The acceptance criteria are not actually implementable/verifiable within the planned work. (PLAN — workflow-editor)
- [high] REQ-11 is only partially planned. The plan mentions snapshot undo and 30-second auto-save, but it does not specify how auto-save avoids interrupting active text/code editing, nor how undo/redo forces open inspectors back into sync with reverted graph state, even though AC-9 requires that behavior. (PRD — workflow-editor; PLAN — workflow-editor)
- [medium] REQ-12/AC-8 are not verifiable. The PRD requires responsive interaction at 50+ visible nodes, but the plan only lists qualitative mitigations and DOM hiding. There is no benchmark, threshold test, or performance harness. (PRD — workflow-editor; PLAN — workflow-editor)
- [medium] The artifact set relies on unresolved decision citations. Many D-* references are used (`D-2`, `D-3`, `D-6`, `D-7`, `D-8`, `D-9`, `D-10`, `D-11`, `D-13`, `D-16`, `D-19`, `D-22`, `D-23`, `D-24`, `D-25`, `D-27`, `D-28`, `D-29`, `D-33`, `D-34`, `D-35`, `D-58`, `D-GR-11/12/13/22`), but no in-scope decision register is provided, so the citations are not auditable. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [medium] Key citations point outside the provided review scope: `system-design-source.md`, `broad/architecture.md`, `plan-review-discussion-4.md`, repo files, and external research URLs. For a single-subfeature artifact review, those are contamination and leave important claims unsupported in-context. (DESIGN — workflow-editor; PLAN — workflow-editor)
- [high] The SYSTEM-DESIGN artifact is effectively empty. `services`, `connections`, `api_endpoints`, `call_paths`, `entities`, and `entity_relations` are all blank despite the PRD depending on `/api/schema/workflow`, validation/import/export flows, and nontrivial editor-state/serialization architecture. It is not a usable system design. (SYSTEM-DESIGN — workflow-editor)
- [medium] Import behavior conflicts with PRD J-3. The PRD says parseable but structurally invalid YAML should load with warnings so users can repair it in the editor, but STEP-10/11 explicitly reject parseable legacy files containing `port_type`, separate hooks sections, or stale nesting keys instead of hydrating them. (PRD — workflow-editor; PLAN — workflow-editor)
- [high] The artifacts understate blocking external interface drift. Workflow-editor assumes SF-1/SF-2/SF-5 already honor D-GR-22, but the provided summaries still expose stale contracts (`phases` nesting, `from_ref`/`to_ref`, `switch_function`, and the `platform/compose/*` repo layout). With `open_questions: []`, the current artifacts pretend this dependency is settled when it is not. (PRD — workflow-editor; external subfeature summaries)
- [high] Edge ownership is internally inconsistent. The PRD data entities say cross-phase edges live at the workflow root, and design J-1 says only cross-phase connections survive in `workflow.edges[]`, but the plan architecture/STEP-5 uses lowest-common-ancestor ownership and explicitly stores sibling nested-phase edges in parent `phase.edges`. This changes the persistence contract. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [medium] Hook-edge transform serialization is ambiguous. PRD edge constraints say hook edges must not carry `transform_fn`, but the design and plan repeatedly serialize hook edges with `transform_fn: null`. The contract does not clearly state whether null is allowed or the field must be omitted. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [medium] AC-4 is not planned in full. The PRD requires fold-only context variables to become available inside child inspectors after changing a phase to fold mode, but neither the design nor the plan describes context-scope propagation into Ask/Branch/Plugin inspectors or any verification for it. (PRD — workflow-editor; DESIGN — workflow-editor; PLAN — workflow-editor)
- [medium] AC-2's one-shot palette placement behavior is missing. The PRD requires drag-and-drop placement mode to end after a single drop, but the plan never specifies or verifies the one-shot placement rule. (PRD — workflow-editor; PLAN — workflow-editor)
- [medium] The plan is contaminated with out-of-scope filesystem work. STEP-1/STEP-8/file_manifest explicitly reach into unrelated repos and templates (`iriai-compose`, `iriai-build-v2`, `templates/*`, `platform/deploy-console`, `first-party-apps/events`, `PACKAGES.md`) even though this is a single-subfeature artifact and the review scope is supposed to stay local to the provided artifacts. (PLAN — workflow-editor)
- [gap/high] Requirement and journey IDs in the plan/design do not align with the PRD, so coverage and verification cannot be audited. (Traceability)
- [gap/high] The planned type/serialization model omits core workflow and phase fields, never specifies synthetic-root handling, and conflicts on cross-phase edge ownership and hook-edge `transform_fn` behavior. (Persistence Contract)
- [gap/high] Schema endpoint failure behavior is contradictory: PRD/design require a blocking gate, while the plan leaves a view-only canvas exposed. (Runtime Bootstrap)
- [gap/high] No test files or harness work are scoped, and critical acceptance criteria for undo/redo, auto-save editing safety, one-shot placement, fold-context variables, and performance remain unverifiable. (Verification)
- [gap/medium] The plan adds template/library behaviors outside the PRD and depends on out-of-scope repos/files, violating the single-subfeature boundary. (Scope Discipline)
- [gap/high] The workflow-editor artifacts assume upstream D-GR-22 alignment that the provided SF-1/SF-2/SF-5 summaries do not yet satisfy, but they record no open question or blocker. (External Dependency Alignment)
**security** [FAIL]: Security coverage is incomplete. The plan relies on inherited platform controls without binding them to this editor’s actual call paths, leaves export/error surfaces unprotected for sensitive workflow content, omits server-side hardening for save/import/validate payloads, and is contaminated by stale traceability and off-scope design elements that weaken security reviewability.

- [high] The plan relocates the editor to `tools/iriai-workflows/frontend` with an assumed sibling backend, which conflicts with the provided SF-5 compose foundation (`platform/compose/*`). That drift breaks the inheritance chain for JWT auth, CORS, rate limiting, and audited workflow APIs promised by the PRD security profile. (PLAN — workflow-editor / Repository Context)
- [high] `PUT /api/workflows/:id`, `POST /api/workflows/:id/validate`, and `GET /api/workflows/:id/export` are used throughout the plan, but there is no explicit backend auth/authorization task for these endpoints. The plan only gates the frontend route, which does not satisfy the PRD requirement for backend auth enforcement and user scoping. (PLAN — workflow-editor / STEP-11 and STEP-15)
- [medium] `GET /api/schema/workflow` is a mandatory runtime dependency, but the plan never states whether it is protected by the same auth/CORS policy as workflow-scoped endpoints. Given the PRD security profile’s authenticated-session requirement, the schema endpoint’s exposure is left undefined. (PLAN — workflow-editor / STEP-1 and STEP-15)
- [high] The PRD explicitly says exported YAML must not embed secrets, but the plan’s export flow serializes the same workflow payload used for save with no secret classification, redaction, or export blocking rules for plugin configs, prompts, or context fields. (PRD — workflow-editor / security_profile + PLAN — workflow-editor / STEP-11)
- [medium] Workflow definitions are marked sensitive because they may contain proprietary prompts, roles, and process logic, yet the plan’s parse-error toasts, validation panel, and migration errors have no masking/redaction rules. Sensitive content can leak through line-numbered error messages and validation output. (PLAN — workflow-editor / J-3, STEP-10, STEP-11)
- [high] Server-side validation is optional at save time. The plan validates on explicit Validate and import preflight, then allows save/auto-save to `PUT /api/workflows/:id` without a required server-side schema/legacy-field rejection step. A malicious client can bypass the UI and persist invalid or stale payloads. (PLAN — workflow-editor / STEP-10 and STEP-11)
- [medium] Import and validate paths parse arbitrary YAML and compile AJV validators, but the plan specifies no payload-size caps, no YAML alias/anchor limits, and no backend body limits. That leaves both client and server validation paths open to resource-exhaustion attacks. (PLAN — workflow-editor / STEP-10 and STEP-11)
- [medium] No rate-limit or backoff coverage is defined for `POST /api/workflows/:id/validate` or repeated `GET /api/schema/workflow` retries. Since validate is CPU-heavy and the UI adds retry, hotkey, and auto-save flows, abuse protection is assumed rather than planned. (PLAN — workflow-editor / STEP-10, STEP-11, STEP-15)
- [medium] The plan introduces editor-specific browser-to-API calls but never states that these endpoints inherit SF-5’s CORS policy or confirms a bearer-token-only request model that avoids CSRF on save/import/validate. With the repo-topology drift, cross-origin protection is not actually specified for this subfeature. (PLAN — workflow-editor / STEP-15)
- [medium] The system-design artifact leaves `services`, `connections`, `api_endpoints`, and `call_paths` empty even though the PRD and plan depend on schema, workflow, validate, and export endpoints. That omission prevents auth decorators, rate limits, CORS rules, and sensitive-data handling from being reviewed or implemented from system design. (SYSTEM-DESIGN — workflow-editor)
- [medium] Security traceability is broken by contamination: the plan uses undefined `REQ-14`/`REQ-15` and `J-16`/`J-17`/`J-20`/`J-22` instead of the current workflow-editor PRD IDs. That makes it impossible to prove the actual security requirement coverage for REQ-13 and the PRD security profile. (PLAN — workflow-editor / STEP-1, STEP-5, STEP-9, journey_verifications)
- [medium] Stale/off-scope surfaces reappear in the revised artifacts: `ErrorFlowNode` is described as a fourth atomic node type, and the plan adds `TemplateFlowNode` and `SwitchFunctionEditor`. Those elements are outside the current REQ-2 contract and broaden the input surface beyond what the revised validation/security rules cover. (DESIGN — workflow-editor / component_defs + PLAN — workflow-editor / STEP-4 and STEP-9)
- [gap/high] No explicit backend auth/authorization coverage for workflow save, validate, export, or schema endpoints despite the PRD security profile requiring backend auth enforcement. (auth)
- [gap/high] No implementation task ensures exports and UI error surfaces avoid leaking secrets or other sensitive workflow content. (data-protection)
- [gap/high] Save/auto-save are not forced through server-side validation, and import/validate lack size/parser hardening against malicious payloads. (input-validation)
- [gap/medium] Rate limiting, CORS, and CSRF protections are assumed from SF-5 but never attached to the editor’s actual endpoints; repo-topology drift makes that inheritance especially unsafe. (platform-controls)
- [gap/medium] The plan is contaminated by undefined REQ/J IDs and stale/off-scope elements (`ErrorFlowNode`, `TemplateFlowNode`, `SwitchFunctionEditor`), which breaks security traceability against the current PRD. (scope)

### workflow-migration [FAIL]

**completeness** [FAIL]: Not approved. The PRD was narrowed to the D-GR-23 runtime-contract revision, but the plan and system design still describe a much older, broader migration program. Traceability is broken, several contracts are internally inconsistent, and multiple dependencies/citations are stale or unresolved.

- [high] STEP-1 through STEP-10 trace to REQ-1..REQ-49, but the provided PRD only defines REQ-34, REQ-40, and REQ-53. Most of the implementation plan therefore has no source requirement in the current PRD, so scope and coverage cannot be audited. (PLAN - workflow-migration)
- [high] The PRD defines only J-1, but the design and plan still treat J-2/J-3/J-4 as first-class journeys. `journey_verifications` is also empty, so even J-1 is not explicitly verified against the plan structure. (PLAN - workflow-migration)
- [high] AC-23 requires `run_declarative()` or a CLI `--declarative` flag, but STEP-10 and the system design implement `--yaml`. That is a direct PRD-to-plan contract mismatch on the user-facing integration path. (PLAN - workflow-migration)
- [high] The PRD still lists open questions about phase tracking, adapter placement, workflow file location, live-smoke plugin strategy, and transform-name resolution, but STEP-7, STEP-8, STEP-9, and STEP-10 silently choose answers. The plan is depending on decisions that the PRD still marks unresolved. (PRD - workflow-migration)
- [medium] STEP-3 reads `tests/fixtures/workflows/templates/gate_and_revise.yaml`, `broad_interview.yaml`, and `interview_gate_review.yaml`, but STEP-6 is where those same files are created. The implementation order is impossible as written. (PLAN - workflow-migration)
- [high] The plan creates standalone template YAML files, while the system design describes template usage as `intra-file $ref`, and the SF-1 summaries do not establish an external file-template resolution contract here. Template loading is underspecified and contradictory across artifacts. (SYSTEM-DESIGN - workflow-migration)
- [high] The plan's cross-subfeature contract still requires stale SF-3 symbols (`MockRuntime`, `MockInteraction`) and cites `iriai-compose/tests/conftest.py` as the producer contract. REQ-40 and the SF-3 summaries require fluent `MockAgentRuntime`, `MockInteractionRuntime`, and `MockPluginRuntime` from `iriai_compose.testing` instead. (PLAN - workflow-migration)
- [high] STEP-7 says `create_plugin_runtimes()` returns a plugin-runtime map consumable by `RuntimeConfig`, but the system design's `RuntimeConfig` entity only exposes `plugin_registry` and never shows where the runtime adapter map is passed. The bridge architecture is missing the integration point for the adapters it plans to build. (SYSTEM-DESIGN - workflow-migration)
- [high] D-SF4-31 and STEP-10 require tuple-based `nodes_executed`, but the system design's `ExecutionResult` entity defines `nodes_executed` as `list[str]`. Tests and consumers cannot implement one stable contract from these artifacts. (SYSTEM-DESIGN - workflow-migration)
- [high] STEP-9 and large sections of the system design introduce SF-5 database seeding, API surface, and example-content packaging. None of that is present in the PRD requirements or acceptance criteria, and it is outside the stated workflow-migration runtime-contract revision scope. (PLAN - workflow-migration)
- [high] The seed package still targets plugin types and plugin instances in SF-5, but the SF-7 design summary removes the Plugins library/entities (`D-20`, `REQ-28`). The migration artifacts are planning to populate a downstream model that the current library artifacts say should not exist. (SYSTEM-DESIGN - workflow-migration)
- [medium] Core claims rely on decision citations such as `D-GR-10`, `D-GR-14`, `D-GR-23`, and `.iriai/artifacts/features/beced7b1/plan-review-discussion-4.md:30-34`, but those decision artifacts are not included in the provided review package. Given the task's 'all artifacts are provided' constraint, these citations are not actually auditable. (workflow-migration artifacts)
- [medium] Path references are internally inconsistent. The artifacts alternate between `iriai-compose/iriai_compose/runner.py` and `iriai_compose/iriai_compose/runner.py`, and SF-3 citations oscillate between bare `subfeatures/testing-framework/prd.md` and fully qualified `.iriai/artifacts/.../subfeatures/testing-framework/prd.md`. At least some cited evidence is therefore ambiguous or stale. (workflow-migration artifacts)
- [medium] The system design still cites AST-validated `exec()` / D-GR-5 semantics for transform execution in its risks, but the SF-2 design summary explicitly supersedes that with an eval-only expression contract. The migration artifacts are not aligned to the current runner security model. (SYSTEM-DESIGN - workflow-migration)
- [medium] Design component CMP-1 changes node-card `reads` metadata semantics, but no plan step updates any implementation artifact or verification target responsible for producing that node-card presentation. CMP-1 is effectively orphaned from the implementation plan. (DESIGN - workflow-migration)
- [gap/high] Plan steps are mostly untraceable to the provided PRD because they reference undefined REQ IDs and extra journeys. (traceability)
- [gap/high] The plan provides no explicit journey verification block for PRD J-1 or AC-17/AC-4/AC-23. (verification)
- [gap/high] The CLI/API entry contract for the declarative bridge is inconsistent (`--declarative` in PRD vs `--yaml` in plan/system design). (contract)
- [gap/high] The plan encodes answers to PRD open questions without first resolving them in the PRD. (dependency)
- [gap/medium] Workflow authoring depends on template files that are only created later in the plan. (ordering)
- [gap/high] The adapter bridge produces plugin runtimes, but the declared `RuntimeConfig` model has no stable field for them. (architecture)
- [gap/high] `nodes_executed` is tuple-based in the plan but `list[str]` in the system design. (internal-consistency)
- [gap/high] Seed-package and SF-5 database work materially expand scope beyond the provided PRD. (scope)
- [gap/high] Plugin type/instance seeding conflicts with the SF-7 artifacts that remove plugin library/entities. (cross-feature-conflict)
- [gap/medium] Critical decisions and evidence paths used by the artifacts are missing or internally inconsistent in the provided package. (citation)
- [gap/medium] CMP-1 is not backed by any implementation or verification task. (design-coverage)
**security** [FAIL]: Security review fails. The artifacts add new execution, plugin, and seed-loading surfaces without defining authentication, authorization, rate limiting, CORS/CSRF, or input-validation controls, and they do not implement the PRD security-profile promises around PII-free fixtures, environment-only secret handling, or limited data retention.

- [high] The PRD security profile says there must be no PII in YAML workflow files or test fixtures, but STEPs 2-6, 8, and 9 generate YAML fixtures, live smoke tests, and seed JSON without any sanitization, redaction, or CI enforcement. Real bug reports, review text, hosted annotations, or reproduction evidence could be committed directly into version-controlled artifacts. (PLAN — workflow-migration / PRD — workflow-migration / SYSTEM-DESIGN — workflow-migration / call paths and entities sections.)
- [high] STEP-9 and the seed package introduce a new persistent SF-5 database retention surface even though the PRD security profile only accounts for version-controlled YAML/PRD artifacts. No retention window, purge path, or rollback/unseed procedure is defined for the seeded records. (PLAN — workflow-migration)
- [high] Secret-bearing `env_config` / `env_overrides` plugin instances are treated as normal workflow and seed content, but no task requires opaque references-only storage or rejects inline secret values. That makes it easy to serialize credentials into YAML, JSON seed data, or the SF-5 database. (PLAN — workflow-migration / SYSTEM-DESIGN — workflow-migration)
- [critical] The system-design endpoint catalog leaves `auth` blank for execution and mutation surfaces including `/declarative/run`, `/plugins/*`, `/testing/run_test`, and `/seed/load`. That does not implement the PRD's inherited runner-auth model; it documents unauthenticated remote execution/mutation endpoints instead. (SYSTEM-DESIGN — workflow-migration)
- [high] `/schema/io/load_workflow` and `/declarative/load_workflow` accept workflow paths, but the plan defines no path sandbox, repository-root allowlist, or normalization rules. If exposed as documented, they can be abused to read arbitrary local files. (SYSTEM-DESIGN — workflow-migration)
- [critical] `/plugins/subprocess/execute` has no command allowlist, argument validation, or operation scoping. Exposing it as designed grants arbitrary command execution. (SYSTEM-DESIGN — workflow-migration)
- [high] `/plugins/http/request` has no URL allowlist, method/header validation, or egress restrictions. That turns the workflow runtime into an SSRF and data-exfiltration surface instead of keeping third-party exposure limited to configured integrations. (SYSTEM-DESIGN — workflow-migration)
- [high] `/plugins/mcp/call_tool` has no tool allowlist, tenant scoping, or auth binding. Any caller that reaches the endpoint can invoke arbitrary MCP tools on preview/playwright services. (SYSTEM-DESIGN — workflow-migration)
- [high] `/plugins/config/resolve` has no key allowlist, secret scoping, or audit requirements. As designed, it can become a generic secret-enumeration/exfiltration endpoint. (SYSTEM-DESIGN — workflow-migration)
- [high] No rate limiting is defined for expensive or side-effecting HTTP-style surfaces such as `/declarative/run`, `/testing/run_test`, `/plugins/*`, and `/seed/load`. A caller can drive denial-of-service, cost amplification, or external-service abuse without any throttling control. (SYSTEM-DESIGN — workflow-migration)
- [medium] No CORS policy or CSRF protection is defined for the documented HTTP-style endpoints. If these endpoints are browser-reachable, cross-origin pages can trigger workflow execution or plugin actions. (SYSTEM-DESIGN — workflow-migration)
- [high] The PRD leaves live-test isolation as an open question, yet STEP-8 commits to `test_live_smoke.py` and the system design routes those tests through real preview, hosting, MCP, and HTTP integrations. There is no requirement for synthetic data, non-production credentials, or mock/test-mode isolation, so live tests can leak internal data to third parties. (PRD — workflow-migration / PLAN — workflow-migration / SYSTEM-DESIGN — workflow-migration)
- [high] STEP-1 and STEP-7 introduce generic `http`, `subprocess`, `mcp`, and `config` plugin interfaces but never constrain which operations, destinations, or instances migrated workflows may bind. That widens the attack surface beyond the PRD claim that external exposure remains limited to configured environment integrations. (PLAN — workflow-migration)
- [medium] STEP-9 and the seed-data package are contamination relative to the current SF-4 workflow-migration scope. Pulling SF-5 database seeding and `/seed/load` into this subfeature adds an extra auth, retention, and secret-handling surface that is not part of the stated runtime-contract migration problem. (PLAN — workflow-migration / SYSTEM-DESIGN — workflow-migration)
- [medium] The seed loader is documented as `python seed_loader.py [--database-url URL] [--seed-file PATH]`. Supplying database credentials on the command line leaks them into shell history and process listings; no safer secret-loading mechanism is specified. (SYSTEM-DESIGN — workflow-migration)
- [gap/high] No plan step enforces the PRD rule that YAML workflow files and test fixtures remain free of PII, despite extensive fixture, smoke-test, and seed generation. (PII handling)
- [gap/critical] HTTP-style execution, plugin, testing, and seed-loading endpoints are specified with blank auth fields and no authorization model. (Authentication / authorization)
- [gap/high] The artifacts do not prevent secret-bearing plugin config from being serialized into workflows, seed files, or databases, and the seed loader invites DB URL secrets on the CLI. (Secrets management)
- [gap/critical] No allowlists or validators are defined for workflow file paths, subprocess commands, HTTP destinations, MCP tools, or config-secret keys. (Input validation / egress control)
- [gap/high] No rate limiting, CORS policy, or CSRF protection is defined for the documented HTTP-style execution surfaces. (Abuse prevention)
- [gap/high] The new SF-5 seed database path adds persistent storage with no retention policy, purge flow, or rollback/unseed procedure. (Data retention / rollback)
- [gap/high] The PRD leaves test/mock-mode strategy unresolved, but the plan still adds live smoke tests against real integrations without requiring synthetic data or non-production credentials. (Live-test isolation)
- [gap/medium] SF-5 database seeding and `/seed/load` are outside the stated workflow-migration runtime-contract scope and add unrelated security surface area. (Scope contamination)

## Cross-Subfeature Edge Findings


### SF-1 → SF-2 (python_import) [FAIL]

The SF-1 -> SF-2 import contract is not internally consistent. The edge description says the consumer imports `NodeDefinition`, `EdgeDefinition`, `PhaseDefinition`, and node-type enums, but the declared producer exports only concrete node models plus `Edge` and `Phase`, and says all exports are Pydantic `BaseModel` subclasses. That is a blocking mismatch at the boundary.

- [blocker] The documented consumer contract requires `NodeDefinition`, `EdgeDefinition`, and `PhaseDefinition`, but the declared export list from `iriai_compose.declarative.schema` does not include those symbols. It instead lists `AskNode`, `MapNode`, `FoldNode`, `LoopNode`, `BranchNode`, `PluginNode`, `Edge`, and `Phase`. A consumer importing the documented names would not match the declared producer surface.
- [blocker] The runner is described as importing node-type enums for dispatch, but the declared data contract provides no enum export. This is reinforced by the statement that all exported symbols are Pydantic `BaseModel` subclasses; enums are not `BaseModel` subclasses. The consumer assumption does not match the producer contract.
- [high] The contract does not define the workflow-level node shape that connects parsing to dispatch. Listing only concrete node models is insufficient to prove compatibility unless the boundary also specifies a base type, discriminated union, or tagged field the runner can rely on.
- [medium] Error behavior at the boundary is underspecified. The contract mentions Pydantic validation and `model_json_schema()`, but it does not define whether loader failures surface as raw `ValidationError`, wrapped exceptions, or another stable interface the runner is expected to handle.
- [gap/high] The authoritative symbol set for `iriai_compose.declarative.schema` is unclear because the narrative names and export list diverge.
- [gap/high] The runner's dispatch dependency on a node-type enum or equivalent discriminator is not represented in the declared producer contract.
- [gap/medium] Boundary error semantics for YAML parse/validation/import failures are not defined.

### SF-1 → SF-3 (python_import) [FAIL]

Blocked. No SF-1 or SF-3 artifacts were provided in scope, so the Python import contract cannot be verified. The edge metadata also contains out-of-scope contamination by referencing SF-2.

- [critical] No producer artifact for SF-1 was provided. Without the actual schema module or interface definition, it is impossible to verify that SF-1 exports the models, fields, or APIs described by the edge.
- [critical] No consumer artifact for SF-3 was provided. Without the test/import code, it is impossible to verify that SF-3 imports the correct module, calls model_json_schema(), or accesses Edge.transform_ref and Node.output_type as described.
- [high] The edge description references SF-2 ('Same schema module as SF-2 consumes'). SF-2 is outside the current review scope, so this is contamination under the task rules.
- [high] Import paths and dependency structure are unspecified. The review cannot confirm that the producer module resolves from the consumer, or that this edge avoids circular imports.
- [gap/critical] Missing SF-1 producer code or interface artifact for the schema module.
- [gap/critical] Missing SF-3 consumer code or interface artifact showing imports and validation usage.
- [gap/high] Out-of-scope reference to SF-2 in the edge description contaminates the review context.
- [gap/high] Missing import path and dependency structure prevents resolution and circular-dependency checks.

### SF-2 → SF-3 (python_import) [FAIL]

Blocked: the context does not include the producer or consumer artifacts for SF-2 -> SF-3, so the interface contract cannot be verified.

- [high] No producer artifact was provided for `iriai_compose.declarative.run(...)` or `ExecutionResult`, so the documented import path, function signature, and returned fields (`nodes_executed`, `artifacts`, `branch_paths_taken`, `cost_summary`) cannot be confirmed.
- [high] No consumer artifact was provided for the testing framework wrapper/assertion hooks, so it cannot be verified that SF-3 imports the documented symbol, passes arguments in the expected order, or consumes `ExecutionResult` with the documented shape.
- [medium] Import resolution and circular-dependency analysis cannot be completed because no module definitions or import statements were included for either side of the edge.
- [gap/blocker] Missing SF-2 producer implementation for `iriai_compose.declarative.run` and `ExecutionResult`.
- [gap/blocker] Missing SF-3 consumer implementation for the testing wrapper/assertion hooks that call `run()` and inspect `ExecutionResult`.

### SF-1 → SF-4 (yaml_schema) [FAIL]

Blocked. The provided context contains only a textual edge description, not the SF-1 WorkflowConfig schema, SF-4 migration artifacts, or the three workflow examples needed to verify compatibility. The contract is also internally unstable because it assumes valid YAML output while simultaneously allowing migration-discovered schema gaps that would require SF-1 revision.

- [high] SF-1's producer artifact is missing from scope. The WorkflowConfig JSON Schema is not provided, so its fields, required properties, enums, and YAML shape cannot be checked against the edge contract.
- [high] SF-4's consumer/producer artifact is missing from scope. No migration implementation, emitted YAML examples, or validation step is provided, so there is no evidence that SF-4 consumes SF-1's schema or produces conformant YAML.
- [high] The expressiveness requirement cannot be verified. The contract says the schema must represent all patterns in iriai-build-v2's three workflows, but none of those workflow artifacts are present in context.
- [high] The boundary definition is unstable: it requires migration output to validate against WorkflowConfig, yet also states that migration may surface schema gaps requiring SF-1 revisions. Without a pinned schema version or an explicit fallback/error contract, producer and consumer assumptions are not aligned.
- [medium] Error handling, import resolution, and circular dependency checks are not reviewable from the provided artifacts because no in-scope modules, paths, or dependency references are included.
- [gap/high] Missing SF-1 WorkflowConfig JSON Schema artifact.
- [gap/high] Missing SF-4 migration implementation and/or representative YAML outputs.
- [gap/high] Missing the three workflow artifacts needed to prove schema expressiveness.
- [gap/high] Missing explicit schema versioning and error semantics for migration-discovered schema gaps.

### SF-2 → SF-4 (python_import) [FAIL]

Blocked. No producer or consumer artifacts were present in the provided context, so the SF-2 -> SF-4 python import contract cannot be verified. The edge description also relies on the SF-3 `run()` contract without supplying that contract inline, which makes compatibility unprovable within the stated scope.

- [blocker] No SF-2 producer artifact was provided showing that migration code exports or invokes the claimed `run()` interface, or that it registers transforms/hooks via `TransformRegistry.register(name, fn)` and `HookRegistry.register(name, fn)`. The producer side of the interface cannot be validated.
- [blocker] No SF-4 consumer artifact was provided showing imports, call sites, expected argument/return shapes, or error handling for the shared `run()` interface. The consumer side of the interface cannot be validated.
- [blocker] The edge description says this uses the same `run()` interface as SF-3 consumes, but that contract is not included in the supplied scope. Because the review is limited to provided artifacts and must not inspect other features, type/shape compatibility with `run()` is not verifiable.
- [high] Import-path resolution and circular-dependency checks require concrete module paths/import statements from both subfeatures. Those artifacts are absent, so unresolved imports or cycles introduced by this edge cannot be ruled out.
- [gap/blocker] Missing SF-2 producer code/spec for migration execution and registry registration.
- [gap/blocker] Missing SF-4 consumer code/spec for importing and calling `run()`.
- [gap/blocker] Missing in-scope definition of the shared `run()` interface referenced indirectly via SF-3.
- [gap/high] Missing import/dependency information needed to verify module resolution and absence of cycles.

### SF-3 → SF-4 (python_import) [FAIL]

Blocked: no producer or consumer source artifacts were included in the provided context, so the SF-3 -> SF-4 python import contract cannot be verified within scope.

- [blocker] The review requires checking whether SF-3 actually exports `MockRuntime`, `assert_node_reached`, `assert_artifact_produced`, `assert_branch_taken`, and `WorkflowTestCase`, and whether SF-4 imports and uses them compatibly. No source files, signatures, tests, or module layout were provided in context, so none of those checks can be performed without violating the instruction not to search the filesystem.
- [gap/blocker] Producer-side source for `iriai_compose.testing` is absent from context.
- [gap/blocker] Consumer-side source/tests that import or rely on `iriai_compose.testing` are absent from context.
- [gap/blocker] No signatures, module paths, or behavioral examples were provided for the exported test helpers and base class.

### SF-1 → SF-6 (json_schema) [FAIL]

Blocked: the provided context contains only the edge description, not the SF-1 producer artifact or the SF-6 consumer artifact, so compatibility across the boundary cannot be verified.

- [blocker] No SF-1 artifact was provided in context. I cannot confirm that a JSON Schema is actually generated from the Pydantic models, where it is published, or what its concrete shape is.
- [blocker] No SF-6 artifact was provided in context. I cannot confirm that the workflow editor/YAML pane/validation logic consume the schema fields, edge definitions, and configuration options described by the contract.
- [blocker] Without concrete producer and consumer artifacts, type compatibility, required-field handling, error behavior, import/module resolution, and circular-dependency risk are all unverifiable.
- [gap/blocker] Missing SF-1 JSON Schema artifact or endpoint definition needed to verify the published contract.
- [gap/blocker] Missing SF-6 implementation/spec showing how the schema is loaded and applied in the editor, YAML pane, validation, and export flows.
- [gap/high] Missing concrete examples for validation errors, schema versioning, and supported edge/node definitions at the interface.

### SF-5 → SF-6 (api_and_components) [FAIL]

Blocked: only the edge description was provided. No SF-5 producer artifacts or SF-6 consumer artifacts were included, so the interface contract cannot be verified.

- [high] No implementation artifacts, interface definitions, or module references for SF-5 or SF-6 were provided. The review cannot confirm that the producer actually exposes the described API client, auth context, design-system components, or workflow endpoints, nor that the consumer uses them correctly.
- [high] The API/data contract cannot be checked. There are no request/response schemas, TypeScript types, or handler/client code for `GET/PUT /api/workflows/:id`, `POST /api/workflows/:id/versions`, or `POST /api/workflows/:id/validate`, so payload compatibility and error-handling alignment are unverified.
- [medium] Import-path resolution and circular-dependency analysis are impossible without actual file/module references for `useAuth`, `XPButton`, `XPPanel`, `XPInput`, `XPToolbar`, `XPSidebar`, and the router/editor integration.
- [gap/high] SF-5 producer implementation/spec artifacts are absent.
- [gap/high] SF-6 consumer implementation/spec artifacts are absent.
- [gap/high] Typed API schemas, component prop contracts, and error models for the edge are not present.

### SF-5 → SF-7 (api_and_components) [FAIL]

Blocked. The provided context does not include the producer or consumer artifacts needed to verify the SF-5 -> SF-7 interface, and the edge description is internally inconsistent and contaminated by out-of-scope SF-6 references.

- [critical] No producer artifacts for SF-5 or consumer artifacts for SF-7 were provided in context. Without the actual API definitions, component exports, route wiring, or consuming code, the interface cannot be verified for compatibility, types, error handling, imports, or dependency structure.
- [critical] The edge description says SF-5 provides CRUD API endpoints for 'all 8 entity types', but the data contract enumerates only 6 endpoints: /api/roles, /api/schemas, /api/templates, /api/phases, /api/plugins, and /api/transforms. Two entity types/endpoints are missing from the contract, so the boundary is underspecified and internally inconsistent.
- [high] The contract depends on 'the same infrastructure as SF-6' and 'Same React context and component library as SF-6'. SF-6 is outside the current review scope, and these references are contamination per the task instructions. The contract is therefore not self-contained and cannot be validated from the provided scope.
- [high] Error-handling expectations are absent from the provided contract. The list/detail success shapes are described, but there is no error response schema, authentication failure behavior, authorization behavior, or empty/not-found handling defined for the producer/consumer boundary.
- [high] Import paths and module references cannot be verified because no actual component library exports, React context modules, or consumer import statements were provided.
- [medium] Circular dependency risk cannot be assessed because no module graph, route structure, or producer/consumer imports were provided.
- [gap/critical] Missing SF-5 producer artifacts for API routes, authenticated client, router shell, and component/context exports.
- [gap/critical] Missing SF-7 consumer artifacts showing actual API calls, route usage, context consumption, and component imports.
- [gap/high] Edge contract is not self-contained because it references SF-6 behavior instead of defining the shared infrastructure directly.
- [gap/critical] Entity coverage is ambiguous: contract claims 8 entity types but specifies only 6 endpoint families.
- [gap/high] No error-response schema or boundary failure semantics are defined.

### SF-7 → SF-6 (react_components) [FAIL]

Blocked: the provided context contains only the edge description, not the producer or consumer artifacts needed to verify the SF-7 -> SF-6 React component contract. I cannot confirm component exports, prop signatures, data shapes, error behavior, imports, or dependency direction from the supplied material.

- [blocker] No producer artifact for SF-7 is present in context. The review cannot verify that `RolePicker`, `SchemaPicker`, `TemplateBrowser`, `PluginPicker`, and `TransformPicker` are actually implemented or exported with the described props.
- [blocker] No consumer artifact for SF-6 is present in context. The review cannot verify that editor/node inspector code imports and uses the described components with compatible props and behaviors.
- [blocker] The edge description mentions that each component fetches from its own API endpoint and renders in the XP design system, but no endpoint contracts, response schemas, loading/error states, or design-system dependencies are provided to validate boundary compatibility.
- [high] No shared type definitions or import paths are provided, so compatibility of callback signatures (`onSelect`, `onCreateInline`, `onDrag`, `edgeType`) and module resolution cannot be established.
- [high] Circular dependency risk cannot be ruled out because no module graph or import statements are included in the supplied artifacts.
- [gap/blocker] Missing SF-7 producer implementation/export artifacts for the five picker/browser components.
- [gap/blocker] Missing SF-6 consumer implementation artifacts showing how the editor imports and uses those components.
- [gap/high] Missing prop type definitions and callback payload shapes for `onSelect`, `onCreateInline`, `onDrag`, and `edgeType`.
- [gap/high] Missing endpoint contracts and response/error schemas for the component-level fetches.
- [gap/high] Missing import/export references needed to verify module resolution and absence of circular dependencies.